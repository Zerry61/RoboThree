import type { AdminRouteMeta } from '../../src/app/route-meta';

export const badRouteMeta: AdminRouteMeta = {
  module: 'models',
  navKey: 'models',
  pageTitle: 'Bad',
  implementationGate: 'ready'
};
