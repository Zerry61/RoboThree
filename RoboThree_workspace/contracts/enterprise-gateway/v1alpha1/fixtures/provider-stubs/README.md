# Model Provider Stub Fixtures

These JSON documents are test-only Adapter fixtures. They model private
Anthropic-compatible and OpenAI-compatible stream frames and their one shared
provider-neutral projection.

They are not Enterprise Gateway Wire Contract objects, are not returned to
Local Core or Desktop, and must never contain credentials, authorization
headers, real Provider endpoints, or production request/response bodies.
