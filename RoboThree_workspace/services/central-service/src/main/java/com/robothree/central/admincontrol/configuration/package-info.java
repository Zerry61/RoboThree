/**
 * Admin Control Central configuration.
 *
 * <p>Only development and test profiles install the test-only principal
 * projection service. Production installs a guard that proves the test-only
 * provider is absent.</p>
 */
package com.robothree.central.admincontrol.configuration;
