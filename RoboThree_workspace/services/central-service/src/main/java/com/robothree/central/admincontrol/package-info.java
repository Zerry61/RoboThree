/**
 * Central-private Admin Control principal and capability projection foundation.
 *
 * <p>AAPI-0.2 does not expose HTTP runtime, browser session handling, or
 * production RBAC. The development principal in this package is explicitly
 * test-only and must never close production identity readiness.</p>
 */
package com.robothree.central.admincontrol;
