# CGF-1.3A Independent QA Report

| 项目 | 值 |
| --- | --- |
| RUN_ID | `2026-07-27-cgf.1.3a-qa` |
| 版本 | `0.0.0-cgf.1.3a` |
| 结论 | **INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING** |
| Node 全量 | 89 files / 570 tests PASS |
| Materializer + Registry 专项 | 2 files / 26 tests PASS |
| Central 回归 | 53 tests / 0 failures / 0 skipped |
| 执行时间 | 2026-07-27 14:57~14:58 |
| P0/P1/P2/P3 | 0 |

## 执行环境

```text
Node.js：v24.13.0 (nvm) / pnpm：11.11.0
Java：OpenJDK 21.0.12 / Central：0.0.0-cgf.1.2c-SNAPSHOT
macOS：Darwin 24.4.0
```

## 执行结果

| 命令 | 结果 |
| --- | --- |
| `pnpm run clean` | 9 paths ✅ |
| `pnpm run build` | TS/Preload/Renderer ✅ |
| `CI=true pnpm exec vitest run` | 89 files / **570 tests PASS** |
| Materializer + RegistryBuilder 专项 | 2 files / **26 tests PASS** |
| `check:central` | **53 tests / 0 failures / 0 skipped** |

Architecture boundary + ESLint PASS。Core/Desktop smoke ready。Electron sandbox Preload smoke ready。

## 专项测试明细（2 files / 26 tests）

### RegistryBuilder（11 tests）

| 测试 | 验证点 |
| --- | --- |
| stable record revisions from canonical JSON | revision 确定性 ✅ |
| rejects revision drift | typed conflict ✅ |
| same sorted snapshot regardless of order | 排序无关 ✅ |
| deep-freezes finalized snapshot | 深层不可变 ✅ |
| rejects duplicate IDs/names | fail-closed ✅ |
| rejects missing bindings/adapters | fail-closed ✅ |
| rejects multi-binding | 无静默 failover ✅ |
| rejects kind/port mismatch | typed validation ✅ |
| consumes builder at finalize | 不可复用 ✅ |
| rejects untrusted records | allowlist ✅ |
| validates CapabilityLock revision | lock 一致性 ✅ |

### Enterprise Registry Materializer（15 tests）

| 测试 | 验证点 |
| --- | --- |
| builds deterministic frozen Registry (InMemory/SQLite) | 精确 generation ✅ |
| narrows disabled/permission/credential-unavailable | 能力收窄 ✅ |
| fails closed on invalid enterprise session | Session gate ✅ |
| fails closed on no Storage Active generation | 缺失 generation ✅ |
| rebuilds after SQLite close/reopen | 可恢复 ✅ |
| rejects scope drift | 四因素隔离 ✅ |
| rejects materialization digest/byte drift | digest 失败关闭 ✅ |
| rejects kind/ID drift | 不发明 binding ✅ |
| rejects duplicate IDs even when disabled | strict ✅ |
| reports all 5 LocalExecutable checks | 五项判定 ✅ |
| fails closed on Agent Definition revision drift | revision 锁定 ✅ |

## 20 项独立 QA 逐条验证

### 1-2. clean build 完整门禁 + Materializer/Registry 专项 ✅

89/570 + 2/26 + 53/0/0 全部独立重跑。

### 3. Materializer 只读取精确 sealed Storage Active ✅

`EnterpriseRuntimeRegistrySource` 只暴露读取当前精确 generation 的语义，不提供 CRUD 或 Runtime Activation。不改写 active pointer ✅

### 4. Session gate 在任何持久配置读取和 Registry 构建前执行 ✅

测试 "fails closed before Registry construction when the enterprise session is invalid"（InMemory + SQLite 双侧）✅

### 5. scope/Compatibility/Snapshot/Package/file/materialization digest 漂移失败关闭 ✅

测试覆盖：scope drift、materialization digest drift、byte drift、kind/ID drift、revision drift——全部 fail-closed ✅

### 6. Model/Tool 映射可复算 ✅

`core:`/`local:`/remote 三种边界正确。descriptor revision/digest + generation configurationRef 确定性生成 ✅

### 7. 远程 descriptor 不携带原始 endpoint/Token/Credential/PID/Runtime Handle ✅

远程 `AdapterDescriptor` 只保留稳定资源引用和精确 configurationRef ✅

### 8. disabled/credential/permission 缺失只收窄不 fallback ✅

测试 "narrows disabled, revoked-by-permission and credential-unavailable resources" ✅

### 9. 重复 ID/kind mismatch/port mismatch/missing reference/multi-binding/revision drift 全部失败关闭 ✅

RegistryBuilder 11 tests 全覆盖 ✅

### 10. Agent/Skill/Knowledge 不进入 Capability Registry ✅

只有 Model/Tool 进入 Capability Registry。Agent/Skill Package 保留 sealed/digest-verified 精确引用。Knowledge 保留独立 revision/digest 投影 ✅

### 11. Registry 排序无关 + deep freeze ✅

测试 "computes stable record revisions" + "same sorted snapshot regardless of registration order" + "deep-freezes the finalized snapshot" ✅

### 12. 五项 LocalExecutable 检查 + 远程不可用不切换 ✅

测试 "reports all five LocalExecutable checks without model or binding fallback" ✅

### 13. InMemory/SQLite 同一语义 + close/reopen ✅

测试 "builds one deterministic frozen Registry"（双侧）+ "rebuilds the same Registry after SQLite close/reopen" ✅

### 14. Contracts/Kernel 无新增 enterprise materializer/Runtime Activation ✅

### 15. 无新增 public Runtime Handle/Central API/Schema/Fixture/migration ✅

Central 53/0/0，版本保持 `0.0.0-cgf.1.2c-SNAPSHOT` ✅

### 16. 未实现 CGF-1.3B/1.3C ✅

| 检查项 | 结果 |
| --- | --- |
| activation intent | 未实现 ✅ |
| Controlled Restart | 未实现 ✅ |
| runtimeActive commit | 未实现 ✅ |
| Task generation 切换 | 未实现 ✅ |
| 四状态 Desktop Projection | 未实现 ✅ |
| GC | 未实现 ✅ |

### 17-18. 全量回归 + 独立 evidence ✅

89/570 + Central 53/0/0 全量通过。

### 19-20. P0=0 P1=0 / PASS 后等待用户接受 ✅

## 结论

**P0=0 P1=0 P2=0 P3=0。CGF-1.3A INDEPENDENT_QA_PASS。**

- 89/570 + 2/26 + Central 53/0/0 全部独立重跑 ✅
- Materializer 只读精确 Storage Active generation ✅
- Session/scope/Compatibility/digest 门禁完整 ✅
- Model/Tool 映射确定性可复算 ✅
- disabled/credential/permission 只收窄不 fallback ✅
- Agent/Skill/Knowledge 不进 Capability Registry ✅
- Registry deep freeze + 排序无关 ✅
- InMemory/SQLite 同语义 + close/reopen ✅
- 无超前 CGF-1.3B/1.3C ✅
- Contracts/Kernel/Central/Schema 未变 ✅

## 用户接受

2026-07-27，用户正式接受本报告结论：

- CGF-1.3A：`PASS/CLOSED`；
- CGF-1.3B：`AUTHORIZED`；
- CGF-1.3C：继续 `GATED`。
