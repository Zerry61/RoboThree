# R2D-3.1 — Claude Code 独立 QA 报告

## 基本信息

| 属性 | 内容 |
|---|---|
| RUN_ID | `2026-08-26-1922-version-0.0.0-r2d.3.1` |
| 验收对象 | R2D-3.1：Entitlement Snapshot / Agent Resource Decision strict schemas、Runtime Selection v1alpha3 私有 Contract、SubmitTurn coordination v1alpha4 私有 Contract、create/revalidate/single-dispatch helpers、exact private subpath |
| 日期 | 2026-08-26 |
| 验收者 | Claude Code（独立 QA，只读） |
| 环境 | Node v24.13.0（nvm，`.node-version`）/ pnpm 11.11.0 / JDK 21.0.12（openjdk@21）/ Docker 29.6.2 |
| 开发版本 | Root / Core / Contracts `0.0.0-r2d.3.1`；Desktop/Admin/Central/Document Worker 版本不变 |
| 上游 | R2D-0 PASS/CLOSED、R2D-1 PASS/CLOSED、R2D-2 PASS/CLOSED、R2D-3 Revision 1 方案 PASS；本批由用户单独授权 |

---

## 一、门禁复跑结果（串行独立执行，Node 24.13.0 + JDK 21 + Docker）

| # | 门禁 | 结果 |
|---|---|---|
| 1 | `CI=true pnpm run harness:r2d3.1` | **PASS 8 files / 91 tests**；`evidenceDigest=sha256:6252b1fa…ddd21ef` 与实施报告逐字一致；`legacyContractDriftCount=0`、`privateMaterialLeakCount=0`、`productionR2dConsumerCount=0`、`productionTaskResourceEntitlementSourcePresent=false`、`builtInGeneralAgentReady=false`、`providerInvoked=false`、13 项 production/downstream 状态全 false |
| 2 | `env -u ELECTRON_RUN_AS_NODE CI=true pnpm run check` | **274/275 files、1896/1897 tests 通过**；唯一失败为 dcf13c 稳定性 harness 并发偶发（单独复跑 PASS） |
| 3 | `CI=true pnpm run check:central`（JDK 21 + Docker） | **PASS 404 / 0 / 0 / 0 / BUILD SUCCESS** |
| 4 | `CI=true pnpm run check:central:offline`（JDK 21 + Docker） | **PASS 404 / 0 / 0 / 0 / BUILD SUCCESS** |
| 5 | `CI=true pnpm run lint` | **PASS**（eslint + Architecture boundary checks passed） |
| 6 | `CI=true pnpm run audit:dtp4` | **PASS** |
| 7 | `CI=true pnpm install --frozen-lockfile --offline` | **PASS**（Already up to date） |
| 8 | 边界 | lockfile `c47641ac…f815a07` 未变；migration 仍止 26；Core/Contracts `0.0.0-r2d.3.1` |

> 注：完整 `check` 的 `smoke:preload` 在会话 shell 带 `ELECTRON_RUN_AS_NODE=1` 时报 `app.whenReady undefined`（既知环境伪象），`env -u` 复跑；dcf13c 稳定性 harness 在完整套件并行下偶发 `snapshot.final_convergence_failed`，单独复跑 PASS——与既往多批一致，非本批缺陷。

---

## 二、重点核查项（对照 R2D-3 Revision 1 §4 + QA-001～QA-120）

| # | 核查项 | 结论 |
|---|---|---|
| 1 | **Entitlement Snapshot strict schema + 独立 digest domain** | ✅ [task-resource-entitlement.ts:19-20](services/core/src/application/task-resource-entitlement.ts#L19) `TASK_RESOURCE_ENTITLEMENT_SNAPSHOT_DIGEST_DOMAIN="robothree.task-resource-entitlement-snapshot.v1\n"`；Material/Schema 双 `.strict()`；digest 覆盖 `subjectBindingDigest/authorityRevision/observedAt/四类 refs/ordinals/identityEvidence` |
| 2 | **test identity 不能冒充 production** | ✅ [task-resource-entitlement.ts:54-65](services/core/src/application/task-resource-entitlement.ts#L54) `TaskResourceIdentityEvidenceV1Schema.superRefine` 强制 `testIdentityUsed && productionIdentityReady` → reject；production activation 要求 `testIdentityUsed=false && productionIdentityReady=true` |
| 3 | **stable ordinal 约束** | ✅ [task-resource-entitlement.ts:200-227](services/core/src/application/task-resource-entitlement.ts#L200) `validateOrderedResourceRefs`：ID 唯一、ordinal 唯一、非负有界（`StableOrdinalSchema` int+nonnegative+max 2^31-1）、**必须按 ordinal 升序 + exact ID tie-break 排列**（`stableOrdinal` 唯一 authority，杜绝 allowlist authored order 语义泄漏） |
| 4 | **四类上限 + portable refs** | ✅ Model/Skill/Knowledge `.max(64)`、Tool `.max(128)`；Model ref `{modelId, revision===digest, digest}`；Skill/Knowledge `{id, revision, contentDigest}` 复用 R2D-2 portable schema（无 `materializedRef`） |
| 5 | **AgentResourceDecision 独立 digest domain + explicit-only requested ID** | ✅ `AGENT_RESOURCE_DECISION_DIGEST_DOMAIN="robothree.agent-resource-decision.v1\n"`；[task-resource-entitlement.ts:245-255](services/core/src/application/task-resource-entitlement.ts#L245) `explicit !== (requestedModelId!==undefined)` 且 `explicit && requestedModelId===resolvedModelRef.modelId`；preference/fallback 禁 requested ID |
| 6 | **v1alpha3 完全删除 `agentDefaultModelId`** | ✅ [runtime-selection/v1alpha3/index.ts:29-54](packages/contracts/src/runtime-selection/v1alpha3/index.ts#L29) Fields 不含 `agentDefaultModelId`/`allowModelOverride`；v1alpha2 才继承旧 default-model 语义（v1alpha3 是独立 material） |
| 7 | **v1alpha3 explicit requested Model exact 绑定** | ✅ [v1alpha3/index.ts:89-99](packages/contracts/src/runtime-selection/v1alpha3/index.ts#L89) `isExplicit === (requestedModelId!==undefined)` 且 explicit 时 `requestedModelId===resolvedModelLock.capabilityId` |
| 8 | **v1alpha3 lock 唯一 + kind** | ✅ [v1alpha3/index.ts:107-116](packages/contracts/src/runtime-selection/v1alpha3/index.ts#L107) Model/Tool capability lock IDs 与 capability IDs 均唯一；Model/Tool 必须 `model.*`/`tool.*`；Skill/Knowledge 用 R2D-2 portable refs |
| 9 | **Reasoning lock 独立 identity + exact Model binding** | ✅ [v1alpha3/index.ts:127-153](packages/contracts/src/runtime-selection/v1alpha3/index.ts#L127) `reasoningModeLock.taskId===value.taskId`、`reasoningModeLock.modelLockRef.lockId===resolvedModelLock.lockId` 且 `lockDigest` 匹配、`reasoningModeLockId` 不在 capability lock IDs |
| 10 | **selection digest 重算** | ✅ `hasValidTaskRuntimeSelectionV1Alpha3` 排除 `selectionDigest` 后按 `TaskRuntimeSelectionV1Alpha3MaterialSchema.parse` 重算 `sha256CanonicalJson`；**与 v1/v2 selection digest 惯例一致**（v1/v2 也用 plain `sha256CanonicalJson(material)`，不套 domain wrapper——selection family 与 entitlement/decision 不同，domain 分离只用于新 facts，符合方案 §4.4 line 353「覆盖 entitlement/decision refs、全部 exact locks、Skill/Knowledge refs 和既有运行事实」的 intent） |
| 11 | **create/revalidate 共用 material schema** | ✅ `createTaskRuntimeSelectionV1Alpha3` / `hasValid...` 都先 `MaterialSchema.parse` 再投 canonical JSON；load-time 重算不跳过 |
| 12 | **single-version dispatch（v1/v2/v3）** | ✅ [runtime-selection-v1alpha3.ts:46-67](services/core/src/application/runtime-selection-v1alpha3.ts#L46) `readSchemaVersion` 一次 → v1/v2/v3/其余 throw `"TaskRuntimeSelection schema version is unsupported"`；无 fallback |
| 13 | **coordination v1alpha4 精确绑定** | ✅ [submit-turn-coordination/v1alpha4/index.ts:24-49](packages/contracts/src/submit-turn-coordination/v1alpha4/index.ts#L24) `SubmitTurnResourcePlanV1Alpha4Schema` 绑定 entitlement/decision/selection/authorization/execution/Task bundle/Instruction Binding digests + `modelLockId`/`toolLockIds`/`reasoningModeLockId` 独立；[index.ts:129-172](packages/contracts/src/submit-turn-coordination/v1alpha4/index.ts#L129) cross-field 断言 resourcePlan ↔ plannedSelectionDigest ↔ reasoningPlan ↔ authorizationPlan ↔ capabilityLockIds 全一致 |
| 14 | **coordination v1alpha4 复用既有状态机、不新增表/migration** | ✅ v1alpha4 `SubmitTurnRecordV1Alpha4Schema` spread v1alpha3 的 `.shape`（含既有 status/lastFailure/loopStartedAt 语义）+ `resourcePlan`；`ReadableSubmitTurnRecordV1Alpha4Schema` 是 v1～v4 union；harness 断言 migration max id=26 |
| 15 | **v4 single-version dispatch** | ✅ [submit-turn-coordination-v1alpha4.ts:46-74](services/core/src/application/submit-turn-coordination-v1alpha4.ts#L46) v1/v2/v3/v4 单次 dispatch；损坏 v4 不 fallback v3 |
| 16 | **frozen source bytes 零漂移** | ✅ [r2d3.1-contract-boundary.test.ts:53-66](services/core/tests/r2d3.1-contract-boundary.test.ts#L53) 5 个 frozen 文件 sha256 逐字节一致：runtime-selection.ts=`8fce0bfb…`、v1alpha2.ts=`c8ecbed8…`、coordination v1alpha1=`7fd694d5…`、v1alpha2=`a66f734b…`、v1alpha3=`cbdae54d…`（R2D-3.1 编码完全未触碰 v1/v2 selection 与 v1～v3 coordination 源文件） |
| 17 | **Contracts roots 零扩宽** | ✅ boundary test 断言 root/index/runtime-selection index/submit-turn-coordination index 不含 `TaskRuntimeSelectionV1Alpha3`/`SubmitTurnRecordV1Alpha4`/`runtime-selection/v1alpha3`/`coordination/v1alpha4` |
| 18 | **两个 built subpath 真实可导入** | ✅ [r2d3.1-contract-boundary.test.ts:10-24](services/core/tests/r2d3.1-contract-boundary.test.ts#L10) `await import(...)` 两个 subpath + `access(dist/**/index.{js,d.ts})`；harness 另用 `node --input-type=module -e` 在 `services/core` 目录真实 import 断言导出常量 |
| 19 | **production consumer count=0** | ✅ boundary test allow-list 仅 4 个文件（v3/v4 两个 helper + task-resource-entitlement + index），其余 `services/core/src` 出现 `TaskRuntimeSelectionV1Alpha3\|SubmitTurnRecordV1Alpha4\|TaskResourceEntitlementSnapshotV1\|AgentResourceDecisionV1` 即 fail |
| 20 | **Desktop/Admin/Central/Document Worker 零引用** | ✅ boundary test 扫 4 个目录，断言 `unexpected.toEqual([])` |
| 21 | **production Entitlement Source 不存在 + 无 `agent.general` 抢跑** | ✅ `productionTaskResourceEntitlementSourcePresent=false`；grep `BuiltInGeneralAgentSource\|system_builtin` production src 零命中（P3-1 `agent.general` exact 中文 material 未决定/实现） |
| 22 | **migration / lockfile / 版本** | ✅ migration 止 26、lockfile `c47641ac…f815a07`、Core/Contracts `0.0.0-r2d.3.1` |
| 23 | **测试反查** | ✅ 无 `.skip`/`.only`/`@Disabled`/sleep；4 个测试文件共 51 个 `it(`，无恒真/空断言 |

---

## 三、发现

### P0 = 0，P1 = 0，P2 = 0，P3 = 1

**P3 — 完整 root check 在并发负载下命中 dcf13c stability harness 偶发失败（已既知、非 R2D-3.1、非阻断）**

- **现象**：完整 `check`（274/275 files，1896/1897 tests）唯一失败 `scripts/run-dcf13c-stability.test.mjs`，`snapshot.final_convergence_failed`。
- **定位**：dcf13c 是 Core child + SQLite + SSE 稳定性 harness，与 R2D-3.1（Contracts v3/v4 私有 subpath + Core domain helpers + 4 测试文件）无代码关联。R2D-3.1 未触碰 dcf13c 任何路径。
- **复现**：单测单独复跑 **PASS**；Central 本轮双跑均 404 全绿（无 CGF-2B3.2 偶发）。与既往多批（KAF-3.3/CPC-3/R2D-1/R2D-2）一致。
- **影响**：不影响 R2D-3.1；本环境未发现 R2D-3.1 自身缺陷。

---

## 四、结论

```text
INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING
P0 = 0，P1 = 0，P2 = 0，P3 = 1（非 R2D-3.1、非阻断）
```

R2D-3.1 完成 Entitlement / Decision / 私有 revision Contract 闭环：Entitlement Snapshot 与 AgentResourceDecision 各用独立 digest domain、stable ordinal 唯一 authority 且严格升序、`testIdentityUsed=true` 必须配 `productionIdentityReady=false`、四类 portable exact refs 无本机 handle；v1alpha3 完全删除 `agentDefaultModelId`、explicit 才允许 `requestedModelId` 且 exact 匹配 resolved Model、Model/Tool lock 唯一 + Reasoning lock 独立 identity 且 exact 绑定 resolved Model；coordination v1alpha4 通过 `SubmitTurnResourcePlanV1Alpha4Schema` + cross-field 断言精确绑定 selection/authorization/execution/Task bundle/Instruction Binding/Model-Tool-Reasoning lock 全部 identities，复用既有四阶段状态机且不新增表/migration。两个 built private subpath 真实可导入；v1/v2 selection + v1～v3 coordination 源文件 sha256 逐字节冻结；Contracts roots 零扩宽；production consumer count=0；production Entitlement Source 不存在；无 `agent.general` production 抢跑（P3-1 已记录，留 R2D-3.2 单独确认）。

门禁独立复跑：harness:r2d3.1 8/91 且 evidenceDigest 与实施报告逐字一致；完整 check 仅 dcf13c 并发偶发（单独复跑 PASS）；lint / Architecture boundary / audit:dtp4 / frozen offline install 全 PASS；Central online/offline 均 404 / 0 / 0 / 0 / BUILD SUCCESS；lockfile `c47641ac…f815a07` 未变、migration 止 26、Core/Contracts `0.0.0-r2d.3.1`。production CPC activation、production enterprise entitlement、R2D core delta 全部继续 false。

唯一 P3 为 dcf13c stability harness 的既知并发偶发，非 R2D-3.1 缺陷、非阻断。**R2D-3.1 可进入用户接受流程；接受后 R2D-3.2～R2D-3.3、R2D-4、DFI-5.3、AAPI-0.3～0.4、TGM、Knowledge Provider、Memory、Effect Reconciliation、Agent Lifecycle 与 Desktop/Admin v2 consumption 继续 GATED，不自动解锁。**

— Claude Code（独立 QA，只读）