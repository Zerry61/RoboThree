# 0.0.0-arh.2.3-repair.1 — Claude Code 独立 QA 报告

## 基本信息

| 属性 | 内容 |
|---|---|
| RUN_ID | `2026-08-13-1508-version-0.0.0-arh.2.3-repair.1` |
| 验收对象 | ARH-2.3 repair.1：修复 W6 并发恢复测试 flaky |
| 日期 | 2026-08-13 |
| 验收者 | Claude Code（独立 QA） |
| 环境 | Node v24.13.0 / pnpm 11.11.0 / Java 21 (Central) |
| 开发版本 | Core `0.0.0-arh.2.3-repair.1` |
| 上一轮 | `0.0.0-arh.2.3` QA 发现 P2-1（W6 flaky） |

---

## 一、根因核实（比超时更精确）

上一轮 P2-1 我判断为"10s 超时过紧"，但 developer 指出实际根因更复杂，本轮核实**三者都成立**：

| 根因 | 核实 | 修复 |
|---|---|---|
| 两个 owner 并发初始化 WAL，失败进程提前退出被误报 timeout | ✅ 确认 | child fixture 增加提前退出诊断（"child exited before IPC result" + code/signal） |
| 两个测试 owner 复用相同 ID 序列 → 伪 idempotency conflict | ✅ 确认 | `ownerOrdinal` argv 派生独立 ID（`800 + ordinal*100`），owner A/B 不冲突 |
| 10s 超时过紧 | ✅ 确认 | 保留 30/40s 门槛 |

**生产代码零改动**：`services/core/src/**` 近 2 小时无任何 `.ts` 改动；仅 `arh2.3-process-recovery.test.ts` + `arh23-compaction-child.mjs` 两个测试/fixture 文件变更。

---

## 二、门禁复跑结果

| # | 门禁 | 结果 |
|---|---|---|
| 1 | W6 连续复跑 10 次 | **PASS 10/10**（上轮 2/3 偶发） |
| 2 | `harness:arh2.3` | **PASS** 52/52 场景，EXIT=0 |
| 3 | 完整 `pnpm run check` | **PASS** 160 files / 1087 tests（上轮 1 flaky 已消除） |
| 4 | Central online | **PASS** BUILD SUCCESS (3:10) |
| 5 | Central offline | **PASS** BUILD SUCCESS (3:08) |

---

## 三、结论

```text
结论：PASS（P0=0 / P1=0 / P2=0 / P3=0）
```

上一轮 P2-1（W6 flaky）已彻底修复，根因从"单纯超时"修正为"WAL 初始化 + ID 序列复用 + 超时过紧"三重因素，全部解决。生产代码零改动，纯 Harness 建模 + 诊断修复。W6 连续 10/10 稳定，完整 Workspace 1087/1087 无失败。

**ARH-2.3 建议正式 PASS/CLOSED。ARH-3 继续 GATED。**

— Claude Code（独立 QA，只读）
