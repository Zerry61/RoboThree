# 0.0.0-arh.2.1 — Claude Code 独立 QA 报告

## 基本信息

| 属性 | 内容 |
|---|---|
| RUN_ID | `2026-08-12-1632-version-0.0.0-arh.2.1` |
| 验收对象 | ARH-2.1：Atomic Planning、Compacted Context View、CompactionExecutionBinding |
| 日期 | 2026-08-12 |
| 验收者 | Claude Code（独立 QA） |
| 环境 | Node v24.13.0 / pnpm 11.11.0 / Java 21 (Central) |
| 开发版本 | Core `0.0.0-arh.2.1`；Contracts/Desktop/DW 保持原版本 |
|---|---|

---

## 一、门禁复跑结果（全部独立执行）

| # | 门禁 | 结果 |
|---|---|---|
| 1 | ARH-2.1 harness | **PASS** 7 files / 70 tests |
| 2 | 完整 Workspace（`pnpm run check`） | **PASS** 153 files / 1056 tests + 3 smoke |
| 3 | Central online | **PASS** BUILD SUCCESS |
| 4 | Central offline | **PASS** BUILD SUCCESS |

---

## 二、8 项重点核查（全部通过）

| # | 核查项 | 结论 | 证据 |
|---|---|---|---|
| 1 | **Tool Call Batch / Result / waiting_confirmation 原子边界** | ✅ | `ConversationAtomicGroupPlanner`：assistant tool call → ToolCallBatchEvidence 验证（disposition identity/batchId/ordinal 匹配）；result_committed 期望 Result 出现在同组且 owner 不跨组；非终态 disposition（非 cancelled/denied 且非 result_committed）→ `blocked=true` → 组保持 open |
| 2 | **首次/滚动 source range + latest/open group 保留** | ✅ | `CompactionSourceRangePlanner`：`groups.slice(0, -1)` 仅取闭组 → latest open group 强制留在 raw tail；`sourceStartSequence` 永远为 1（完整前缀）；eligible end 必须 > active end；T1 mid-fault 回滚通过 |
| 3 | **Summary 低权限派生不升为 system instruction/执行事实** | ✅ | 静态扫描：Summary 不写 `ConversationMessage`、不使用 `system_instruction` sourceKind（命中仅为既有 context source kind）；不新增 `ExecutionFact` 类型 |
| 4 | **CompactionExecutionBinding 原子写入** | ✅ | `compaction_execution_bindings` 表 16 列，12 个为无凭据锁标识（compactionJobId/sessionId/taskId/runtimeSelectionId+digest/modelLockId+digest/capabilityId/registryRevision/adapterDescriptorId+revision/externalTargetDigest/summarizerPromptRevision + createdAt + bindingDigest 自校验）；字段不含 RuntimeHandle/PID/Endpoint/Credential/Token/Prompt |
| 5 | **binding 幂等重放 / 漂移冲突 / close+reopen** | ✅ | bindingDigest = sha256(material) 自校验；strict schema；`ON DELETE CASCADE` 对 compaction_jobs；SQLite migration 18 + InMemory conformance |
| 6 | **T1 中间故障整笔回滚** | ✅ | schema-preflight `rollback()` 路径；compaction-coordinator 测试覆盖 `summary_obtained_before_commit` crash + post-commit replay |
| 7 | **无 ARH-2.2 超前实现** | ✅ | 无 `ContextPreparationCoordinator`、无自动 trigger、无 `ModelBackedCompactionSummarizer`、无真实 admission 接线 |
| 8 | **公共 Contracts / Kernel reducer / Desktop / Central 生产路径未改** | ✅ | Contracts 未改（2026-08-06）；Kernel reducer 未改；Desktop 未改；Central 改动为 ARH-1 Java conformance（非本批） |

---

## 三、发现

### P0 = 0，P1 = 0，P2 = 0，P3 = 0

---

## 四、结论

```text
结论：PASS（P0=0 / P1=0 / P2=0 / P3=0）
```

ARH-2.1 的 ConversationAtomicGroupPlanner、CompactionSourceRangePlanner、CompactionExecutionBinding 实现正确且安全边界完整。ARH-2.1 可进入用户接受流程。**ARH-2.2 继续保持 GATED。**

— Claude Code（独立 QA，只读）
