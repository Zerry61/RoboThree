# DCF-2C Independent QA Report

| 项目 | 值 |
| --- | --- |
| RUN_ID | `2026-07-27-dcf.2.3-qa` |
| 版本 | `0.0.0-dcf.2.3` |
| 结论 | **INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING** |
| Node 全量 | 98 files / 630 tests PASS |
| harness:dcf2c | 4 files / 18 tests PASS |
| 执行时间 | 2026-07-27 20:07 |
| P0/P1/P2/P3 | 0 |

## 执行环境

```text
Node.js：v24.13.0 / pnpm：11.11.0 / macOS：Darwin 24.4.0
```

## 执行结果

| 命令 | 结果 |
| --- | --- |
| `CI=true pnpm run clean` | 9 paths ✅ |
| `CI=true pnpm run lint` | Architecture boundary + ESLint PASS ✅ |
| `pnpm run build` | TS/Preload/Renderer PASS ✅ |
| `CI=true pnpm exec vitest run` | 98 files / **630 tests PASS** ✅ |
| `CI=true pnpm run harness:dcf2c` | 4 files / **18 tests PASS** ✅ |
| Core/Desktop/Preload smoke | ready ✅ |

## 15 项独立 QA 逐条验证

### 1. clean build + pnpm run check ✅

98/630 + harness 4/18。Architecture boundary + ESLint + build。

### 2. harness:dcf2c 实际执行 ✅

4 files / 18 tests PASS。含真实 loopback Desktop/Core/SSE E2E、SQLite close/reopen、运行代切换、cursor 续接、Snapshot 恢复。

### 3. running/waiting_input/waiting_user_confirmation SQLite close/reopen 精确恢复 ✅

Task 恢复矩阵覆盖 running、waiting_input、waiting_user_confirmation 状态和 revision 精确恢复。

### 4. provide input + confirmation decision 重放不重复写消息/决定 ✅

补充输入使用 Conversation prepared-message/commit 事务。相同命令重放不重复写入 ✅

### 5. rejected 不创建 Effect / allowed 仍执行实时收窄 ✅

reject 不创建 Effect。allow 继续受 disabled/revoked/health/credential/permission 实时状态收窄检查 ✅

### 6. cancel terminal restart 后不复活 / retry 使用新 Run ✅

cancel 终态重启后保持 terminal。retry 始终恢复到新 Run，旧 Run 不改写 ✅

### 7. 迟到 Observation 不改变新 Run/state revision / 只追加 rejected audit Event ✅

`commitRejectedCommandEvent` Port：旧 Run 迟到 Observation 只追加审计 Event，不改变 state revision 或 checkpoint。审计 Event 不含 Observation 输出正文 ✅

### 8. InMemory/SQLite rejected Event Port 语义一致 ✅

InMemory/SQLite 共享 Conformance：拒绝审计 Event 只推进 Event sequence，不改变 state revision 或 checkpoint ✅

### 9. Desktop restart 使用新 clientInstanceId 后 Snapshot 一致 ✅

E2E 验证 Desktop Main 重建后 Snapshot 保持一致 ✅

### 10. Core restart 后 runtimeInstance 改变 / Task 从同一 SQLite 恢复 ✅

runtimeInstance 变化后 Task 从同一 SQLite 恢复，旧 token/SSE/ephemeral 失效 ✅

### 11. SSE cursor 续接无 duplicate durable Event / ephemeral 不回放 ✅

复用 DCF-1.3 的去重和 cursor 收敛能力。Harness 结束后 cursor 正确推进，无重复 durable Event ✅

### 12. slow consumer/heartbeat/dedupe/连接资源全部有界释放 ✅

Harness 结束后 active SSE、poll timer、heartbeat timer 和临时订阅全部归零 ✅

### 13. Renderer 只展示安全 guidance / 不持有内部对象 ✅

waiting_input、waiting_confirmation、recovering、manual_attention 均有明确用户指引。不持有 reducer/Effect/Receipt/Output/Credential/原始 Tool 参数 ✅

### 14. Kernel reducer / Enterprise Gateway / CGF-2 未修改 ✅

Kernel 保持纯函数。Central `0.0.0-cgf.1.2c-SNAPSHOT` 不变。CGF-2 未实现 ✅

### 15. 用户现场体验门槛独立 ✅

自动化 QA PASS 不替代用户现场体验。DCF-2C 需二者均被用户接受后才关闭 ✅

## 结论

**P0=0 P1=0 P2=0 P3=0。DCF-2C INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING。**

- 98/630 + harness 4/18 clean+build 独立重跑 ✅
- running/waiting_input/waiting_user_confirmation SQLite close/reopen 精确恢复 ✅
- provide input + confirmation 重放不重复 ✅
- rejected 不创建 Effect / allowed 实时收窄 ✅
- cancel terminal 不复活 / retry 新 Run ✅
- 迟到 Observation 不改变新 Run / rejected audit Event 不含输出 ✅
- Desktop/Core restart Snapshot 一致 ✅
- SSE cursor 续接无重复 / 资源全部归零 ✅
- Renderer 安全 guidance / Kernel/CGF-2 未变 ✅

**DCF-2 建议关闭。CGF-2 GATED。用户现场体验仍为 PENDING。**
