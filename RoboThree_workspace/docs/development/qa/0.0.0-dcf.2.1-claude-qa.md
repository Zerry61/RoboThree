# DCF-2A Independent QA Report

| 项目 | 值 |
| --- | --- |
| RUN_ID | `2026-07-27-dcf.2.1-qa` |
| 版本 | `0.0.0-dcf.2.1` |
| 结论 | **INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING** |
| Node 全量 | 95 files / 615 tests PASS |
| 执行时间 | 2026-07-27 |
| P0/P1/P2/P3 | 0 |

## 执行环境

```text
Node.js：v24.13.0 / pnpm：11.11.0 / macOS：Darwin 24.4.0
```

## 执行结果

| 命令 | 结果 |
| --- | --- |
| `CI=true pnpm run lint` | Architecture boundary + ESLint PASS ✅ |
| TypeScript/Preload/Renderer build | PASS ✅ |
| `CI=true pnpm exec vitest run` | 95 files / **615 tests PASS** ✅ |
| Core/Desktop/Preload smoke | ready ✅ |

## 13 项独立 QA 逐条验证

### 1. clean build + pnpm run check ✅

95/615 + Architecture boundary + ESLint + build。

### 2. Agent Loop 真实产生 start Run/Step/Observation/completed Task ✅

Scripted Agent Loop 接入 `DurableTaskRuntime`：start Run → start model Step → record Observation → complete Run。不在 UI 伪造状态 ✅

### 3. Task list/detail + Run/Step + Tool Activity 全部来自 Core Snapshot ✅

`DesktopTaskProjectionService` 从 Task checkpoint、runtime selection、submit-turn binding 和 Effect attempt 确定性构建。Renderer 收到 Event 后重新查询 Core ✅

### 4. InMemory/SQLite 新查询排序/缺失引用/close/reopen 同语义 ✅

`TaskPersistence` 增加按 Session/Task 读取 binding/Effect/Task 集合的类型化查询。InMemory/SQLite 同语义 ✅

### 5. SQLite V11 只前向扩展 delivery type / V1-V10 未改写 ✅

Migration 11（`dcf_2a_task_projection_delivery`）：只扩展 Desktop delivery type（`task_status_changed`、`tool_activity_changed`）。V1-V10 未改写 ✅

### 6. 相同 TaskEvent 重放不重复创建 Desktop delivery ✅

投影器按本运行代维护 Task event sequence。运行中的新增 Event 幂等投递 ✅

### 7. Core restart 后历史 Task 由 Snapshot 恢复 / 不重放旧裁剪 Event ✅

重启时历史状态由 Snapshot 重建，不把已保留或已裁剪的旧 Event 重新回灌 ✅

### 8. task status/tool activity Event 只含安全 ID/status/queryRef ✅

Event Schema：`task_status_changed` + `tool_activity_changed`，均只有 `queryRef` 字段，不含正文 ✅

### 9. uncertain → manual_attention / 无参数/结果/凭证/metadata/idempotency key ✅

`uncertain` Effect 在 Task/Run/Step 层统一覆盖为 `manual_attention`。Tool Activity 只输出 Tool 名、操作类型、产品状态、时间与固定安全摘要 ✅

### 10. Renderer 只刷新 Snapshot / 不实现 Task reducer ✅

Renderer 收到 Event 后重新查询 Core Projection，不自行推导 Task 状态 ✅

### 11. IPC/Preload strict + Main-only HTTP/SSE + Renderer CSP ✅

Core private HTTP、Main Client、固定 IPC、Preload safe API 全链路保持。CSP `connect-src 'none'` ✅

### 12. DCF-2B / DCF-2C / CGF-2 未超前 ✅

| 检查项 | 结果 |
| --- | --- |
| Confirmation 决策 | 未实现 ✅ |
| cancel/retry/continue | 未实现 ✅ |
| DCF-2C Harness | 未实现 ✅ |
| CGF-2 | 未实现 ✅ |

### 13. DCF-1.1~1.3 + CGF-1.1~1.3 + KAF-0~5 全量回归 ✅

95/615 全量通过 ✅

## 结论

**P0=0 P1=0 P2=0 P3=0。DCF-2A INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING。**

- 95/615 全量独立重跑 ✅
- Agent Loop 真实产生 Task/Run/Step/Observation ✅
- Task list/detail/Tool Activity 全部来自 Core Snapshot ✅
- V11 forward-only / V1-V10 未改写 ✅
- uncertain → manual_attention / Projection 无泄露 ✅
- Renderer 只刷新 Snapshot / 无第二 reducer ✅
- Kernel 隔离 + 全量回归 + 无超前 ✅

**DCF-2B GATED，等待用户明确接受后进入。**
