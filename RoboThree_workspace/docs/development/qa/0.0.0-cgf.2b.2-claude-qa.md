# CGF-2B.2 Independent QA Report

| 项目 | 值 |
| --- | --- |
| RUN_ID | `2026-07-31-cgf.2b.2-qa` |
| 版本 | `0.0.0-cgf.2b.2` (Root) / `0.0.0-cgf.2b.2-SNAPSHOT` (Central) |
| 结论 | **INDEPENDENT_QA_PASS (WITHOUT REAL PROVIDER) — USER_ACCEPTANCE_PENDING** |
| Central online | 180 tests / 0 failures / 0 skipped |
| Central offline | 180 tests / 0 failures / 0 skipped |
| Workspace check | Architecture boundary passed + 107 files / 685 tests |
| `check:cgf2b2:direct-provider` | **RESOURCE_GATED**（零网络调用） |
| 执行时间 | 2026-07-31 17:22–17:24 CST |
| P0/P1/P2/P3 | 0 |

## 执行环境

```text
Java：OpenJDK 21.0.12 / Spring Boot 3.5.16 / MyBatis-Plus 3.5.16
Node.js：v24.13.0 / Docker Engine 29.6.2 / PostgreSQL 16-alpine
```

## 执行结果

| 命令 | 结果 |
| --- | --- |
| `CI=true check:central` | **180 tests / 0 failures / 0 errors / 0 skipped** |
| `CI=true check:central:offline` | **180 tests / 0 failures / 0 errors / 0 skipped** |
| `CI=true check` | Architecture boundary passed + **107 files / 685 tests** |
| `CI=true check:cgf2b2:direct-provider` | `RESOURCE_GATED` — 安全跳过，零网络调用 ✅ |

### 测试组成（180 = CGF-2B.1 167 + 13 B.2）

| 增量 | 覆盖 |
| --- | --- |
| +13 | Runtime Bridge Stub Conformance + Credential Source + Adapter Registry + Backend |

### 新增生产代码

| 层 | 关键类 |
| --- | --- |
| Port (3) | `ModelProviderRequestSource`、`ModelProviderAdapterRegistry`、`ModelInvocationEphemeralPublisher` |
| Adapter/Runtime (4) | `ProviderBackedModelInvocationExecutionBackend`、`StrictModelProviderAdapterRegistry`、`BufferedModelInvocationEphemeralPublisher`、`ProviderResultCollector` |
| Development (2) | `FixedSyntheticModelProviderRequestSource`、`DevelopmentModelCredentialMaterialSource` |

## 独立 QA 逐条验证

### 1. 独立执行 Central online/offline + Workspace 完整门禁 ✅

180/0/0/0 x2 + 107/685。独立执行。

### 2. Runtime Bridge Stub Conformance — B.1 两套 Adapter 相同 Projection ✅

`ProviderBackedModelInvocationExecutionBackend` 使用 B.1 的 Anthropic/OpenAI loopback Stub：text delta 投递、usage/terminal 收敛、Provider 拒绝映射。两套 Adapter 归一为同一 neutral Projection。

### 3. RESOURCE_GATED 机制正确 ✅

`check:cgf2b2:direct-provider` 缺 `ROBOTHREE_CGF2B2_DIRECT_PROVIDER_KEY/ENDPOINT/PROTOCOL/MODEL_ID` 时：`{"status":"RESOURCE_GATED","message":"...no network call was attempted."}`。无网络调用、无误报 PASS。

### 4. Backend 只通过 Result 报告，Runtime 是 durable 唯一提交者 ✅

`ProviderBackedModelInvocationExecutionBackend` 不直接调用 Repository。Runtime 继续负责 durable Event/Outbox/usage/terminal 的持久化提交。

### 5. Ephemeral delta 投递不写入 durable Result ✅

`BufferedModelInvocationEphemeralPublisher` text delta 即时写入内存 buffer。terminal Result 不含 text delta。`clear()` 为 best-effort 清理。

### 6. Request Source digest 精确匹配 + 缺失/漂移失败关闭 ✅

`FixedSyntheticModelProviderRequestSource`：`requestDigest` 精确匹配、返回前重新 canonicalize + SHA-256。缺失/digest 漂移/非 `DIRECT_PROVIDER` → typed error。

### 7. Credential Source Development-only + Production fail-closed ✅

`DevelopmentModelCredentialMaterialSource`：allowlist opaque reference/revision、`char[]` 瞬时使用后清零。Production Profile 不注册 Development Source。默认门禁不读 Key。

### 8. 无 Controller / Schema / Contract 修改 + 无 B.3/CGF-2C 超前 ✅

| 资源 | 状态 |
| --- | --- |
| Model Controller | 不存在 ✅ |
| PostgreSQL v0007 | `9b8cf002...` 未变 ✅ |
| `openapi.yaml` | `dea67789...` 未变 ✅ |
| 企业中转站 / 双 JVM 真实 Provider Recovery | 不存在 ✅ |
| Desktop 用户外发 / Local Core Adapter | 不存在 ✅ |
| CGF-2B.3 / 2C | 不存在 ✅ |

### 9. 敏感信息动态扫描 ✅

sentinel credential、synthetic canary、厂商 URL（api.deepseek.com 等）→ 生产配置/日志/Trace/QA Evidence 0 命中。

## 已知缺口（与开发者一致）

| 缺口 | 状态 |
| --- | --- |
| 真实 Provider Streaming 真实验证 | **RESOURCE_GATED** — 需用户提供新 Key/Endpoint/Protocol/Model ID |
| 真实 invalid Credential / cancel / deadline 实际执行 | 同上 |
| deltaCount 记录与 canary 泄漏扫描（真实网络） | 同上 |

以上缺口不阻塞无 Key 部分的独立 QA PASS，但必须在资源到位后补齐才能关闭 CGF-2B.2。

## 结论

**P0=0 P1=0 P2=0 P3=0。CGF-2B.2 无 Key 部分 INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING。**

- Central 180/0/0 x2 + Workspace 107/685 ✅
- Runtime Bridge Stub Conformance PASS ✅
- `RESOURCE_GATED` 零网络调用 ✅
- Backend 只通过 Result 报告 ✅
- Ephemeral delta 不写入 durable terminal ✅
- Credential Source Development-only + Production fail-closed ✅
- Contract/v0007 未变，无 B.3/2C 超前 ✅

**真实 Provider 资源到位后需补充执行 5 项真实 Harness 场景。CGF-2B.3 GATED。**
