# CTX-MVP-1 Independent Code QA Report

> Version: `0.0.0-mvp.ctx.1`
> Date: 2026-09-01
> Reviewer: Claude Code（独立只读）
> Status: `PASS/CLOSED — INDEPENDENT CODE QA PASS / USER ACCEPTED`
> P0 = 0，P1 = 0，P2 = 0，P3 = 1（外部 blocker，与本批零关联）
> 可冻结：是（仅 CTX-MVP-1 子批）
> 是否可标记 PASS/CLOSED：**是，仅关闭 CTX-MVP-1 子批**
> 后续联合门禁：`REAL_PROVIDER_CALIBRATION_PENDING / WTE1_LONG_CONTEXT_JOINT_E2E_PENDING`

---

## 1. 评审结论

CTX-MVP-1 严格只修复方案 Revision 2 列出的 P0/P1 修订项，未扩张为长期 Memory、Knowledge、RAG、第二套
Runtime、模型路由或 WTE Tool 实现。所有 15 项核心事实命中、所有门禁 PASS、所有边界不漂移；Capsule v2
保持 `NOT_REQUIRED / NOT_IMPLEMENTED`。

```text
最高允许结论：CTX_MVP1_MODEL_AWARE_LONG_CONTEXT_CONFORMANT
本批不声明 WTE ready / production ready / 400K Provider usage calibrated
```

---

## 2. 实测门禁结果

### 2.1 关键 focused tests（独立复跑）

| 门禁 | Developer claim | 独立复跑结果 |
|---|---|---|
| CTX exact capability/output/material/context + deployment/compaction focused | `7 files / 49 tests PASS` | **7 files / 49 tests PASS** ✅ exact match |
| 快速集（exact capability + exact model + deployment + pipeline） | `4 files / 31 tests PASS` | **4 files / 31 tests PASS** ✅ exact match |
| Desktop CorePrivateSupervisor | `1 file / 5 tests PASS` | **1 file / 5 tests PASS** ✅ exact match |
| Central Admin discovery | `2 tests / BUILD SUCCESS` | **`AdminModelCommandServiceTest` 2/0/0/0 / BUILD SUCCESS** ✅ exact match |

**7-file focused set**（命中 49 tests）：
1. `services/core/tests/ctx-mvp1-context-material-policy.test.ts` (3)
2. `services/core/tests/ctx-mvp1-exact-model-capability.test.ts` (7)
3. `services/core/tests/internal-trial-enterprise-model-deployment.test.ts` (6)
4. `services/core/tests/arh2.2-context-pipeline-assessment.test.ts` (3)
5. `services/core/tests/arh2.2-model-backed-compaction-summarizer.test.ts` (4)
6. `services/core/tests/context-pipeline.test.ts` (15)
7. `services/core/tests/compaction-coordinator.test.ts` (11)

### 2.2 边界与治理门禁

| 门禁 | 结果 |
|---|---|
| `git diff --check HEAD` | exit 0 ✅ |
| DTP-4 audit | "DTP-4 packaging audit passed." ✅ |
| DTP-4 audit self-test | `1 file / 2 tests PASS` ✅ |
| Core smoke（dist bootstrap import） | `core.ready` ✅ |
| Core `tsc --noEmit` | exit 0 ✅ |
| Desktop `tsc --noEmit` | exit 0 ✅ |
| Skip/todo/only/xit/xdescribe 扫描（7 个 focused 文件） | 无逃逸 ✅ |
| Lockfile SHA-256（pnpm-lock.yaml） | `5b15ae01…874f31`（frozen）✅ |
| Core migration max | 26（frozen，LATEST_SQLITE_SCHEMA_VERSION）✅ |
| Central schema v13 / migration | 沿用 RSL-2 v13（Central pom.xml 仅版本 bump）✅ |

### 2.3 5 frozen Contract SHA-256（沿用 WFW-3 / RSL-2 冻结表）

| Contract | SHA-256 |
|---|---|
| `admin-control/v1alpha1/index.ts` | `79e2e127956651eee482bb49ff04a9c95f4c090cd1edaf4efd3cf6479bb2eb1e` ✅ |
| `admin-control/v1alpha2/index.ts` | `50b757b94d20e90b4e689613a318f54fa7936392a084dda64b234488a325591a` ✅ |
| `runtime-selection/agent-definition/v1alpha2/index.ts` | `fb0732e69801c26e439907694273551686c4cb267050f76cd059e011be649981` ✅ |
| `desktop-local/personal-model-management/v1alpha1/index.ts` | `a306a07cfe7f19ee9346a7bce7b226bc969978e41e7952eed86d63efd5489c3a` ✅ |
| `desktop-local/personal-model-management/v1alpha2/index.ts` | `f04b454eacadfebc194c7f71c988dd68815f801371bd339fbff6711c85e052e5` ✅ |

### 2.4 版本一致性

| 模块 | 版本 | 与 DEVELOPMENT-LOG 一致 |
|---|---|---|
| Root | `0.0.0-mvp.ctx.1` | ✅ |
| services/core | `0.0.0-mvp.ctx.1` | ✅ |
| apps/desktop | `0.0.0-mvp.ctx.1` | ✅ |
| services/central-service | `0.0.0-mvp.ctx.1-SNAPSHOT` | ✅ |
| packages/contracts | `0.0.0-mvp.rsl.2`（未变更） | ✅ |
| apps/admin-console | `0.0.0-mvp.rsl.2`（未变更） | ✅ |
| services/document-worker | `0.0.0-wfw.2`（未变更） | ✅ |

---

## 3. 关键事实命中（按方案 14 项独立复核问题）

### 3.1 方案 §1 五项当前事实（与代码一致）

| 事实 | 落点 | 命中 |
|---|---|---|
| `contextWindow=128_000` 硬编码、缺 `maxOutputTokens` | `internal-trial-enterprise-model-deployment.ts:199-204` | 已改为 `createExactModelCapabilityProfile({contextWindowTokens, maxOutputTokens})` + `encodeExactModelCapabilityProfile` → `configurationRef` |
| `ContextBudgetPolicy` 全局单例 | `task-context-budget-resolver.ts:21-66` | 已改为 `TaskContextBudgetResolver` per-Task，policy 通过 `resolve()` 派生 |
| `ConservativeTokenEstimator` UTF-8 byte count | `calibrated-token-estimator.ts:26-46` | `CalibratedTokenEstimator` 已替换：CJK 单独计、ASCII 按 ceil(n/4)、代码点 > 0x7f 按 byte/2 计；暴露 `errorEnvelope`（20% relative + 32 absolute） |
| `1_024 tokens` 固定 output reserve | `round-output-requirement.ts:36-48,98-103` | WTE full replacement 按 canonical envelope 估算 + 25% (至少 1024 tokens) headroom，超 capacity throw `workspace.file.output_capacity_insufficient` |
| 4 KiB Tool Result preview | `task-context-budget-resolver.ts:63` `maxPreviewBytes: 4_096` | 仍是 4 KiB（与方案 §8.3「首版建议值」一致，material policy 单独驱动 WTE exact） |

### 3.2 Exact capability 与 per-Task policy（方案 §6）

- `exact-model-capability-profile.ts:39-58`：`createExactModelCapabilityProfile` 生成 `capabilityProfileRevision = sha256CanonicalJson({schemaVersion, capabilityId, modelFamily, contextWindowTokens, maxOutputTokens})`；每个字段变更都产生新 revision
- `exact-model-capability-profile.ts:9-12`：safe range `contextWindow [8192, 1_048_576]`、`maxOutputTokens [256, 262_144]`，且 `maxOutput ≤ context`
- `exact-model-capability-profile.ts:75-113`：`resolveExactModelCapabilityProfile` 校验：
  - L82: descriptorRef + bindingRef 必须同时存在
  - L87: descriptorRef == bindingRef（dual-reference drift 失败）
  - L90: 必须以 `model-capability-profile:v1/` 开头
  - L106-110: revision 与 model.contextWindow 必须严格一致（profile drift 失败）
- `exact-model-capability-profile.ts:115-153`：legacy fallback `legacy_task_lock` source = `maxOutputTokens=1024`，仅当调用方显式 `allowLegacyTaskLock: true` 才接受；新 Task 默认 fail-closed（QA-002、QA-007、QA-008）
- `task-context-budget-resolver.ts:42-53`：`safetyMarginTokens = clamp(ceil(context * 0.02), 512, 16384)`、`minimumHeadroomTokens = min(32768, max(2048, reserved*2, ceil(context*0.08)))`（与方案 §6.3 公式字面一致）
- `task-context-budget-resolver.ts:57-64`：传入 `ContextBudgetPolicy({compactionThresholdRatio: 0.8})`，derived policy digest 同步（QA-001/QA-005）

**冻结样例核验**（方案 §6.3 表格）：

| contextWindow | reserved | safety | available input | min headroom | threshold |
|---:|---:|---:|---:|---:|---:|
| 8,192 | 1,024 | 512 | 6,656 | 2,048 | 4,608 ✅ |
| 128,000 | 8,192 | 2,560 | 117,248 | 16,384 | 93,798 ✅ |
| 400,000 | 8,192 | 8,000 | 383,808 | 32,000 | 307,046 ✅ |

### 3.3 Production estimator（方案 §7）

- `calibrated-token-estimator.ts:19-66`：`CalibratedTokenEstimator implements TokenEstimator`，暴露 `errorEnvelope = {profileRevision: "robothree.token-estimator.heuristic.v1", relativeSafetyMargin: 0.2, absoluteSafetyMarginTokens: 32}`，并提供 `estimateWithSafetyMargin` 与 `isWithinValidatedEnvelope`
- `create-desktop-private-runtime.ts:967`：`const contextTokenEstimator = new CalibratedTokenEstimator();` —— 字面确认生产 runtime 入口使用 Calibrated
- 整个 production code `grep "ConservativeTokenEstimator"` 0 hits（仅 `adapters/fake/conservative-token-estimator.ts` 自身定义 + 8 个 historical tests 显式 import）—— QA-009 / 方案 §7.3 `fake estimator count=0` 已成立
- 不宣称逐次 `estimate >= actual`：方案 §7.1 字面 `estimator 不要求对所有 Provider、所有请求逐次满足 estimate >= actual`；L55-65 暴露 validated envelope 给 diagnostics 二次使用

### 3.4 Round output requirement + 4K output Model（方案 §6.3、§10.3）

- `round-output-requirement.ts:63-67`：ordinary round `min(8_192, lockedMaxOutputTokens)` —— 4K output Model 仍可执行普通任务（QA-033 一致）
- `round-output-requirement.ts:75-89`：WTE envelope `JsonValueSchema.parse({type:"tool_call", name:"tool.workspace.file.write_text", arguments:{relativePath, options:{mode:"replace_existing", expectedPreviousSha256}, content}})` —— 字面包含 path、digest、JSON escaping、wrapper、正文
- `round-output-requirement.ts:93-96`：`growthHeadroomTokens = max(1024, ceil(base * 0.25))`
- `round-output-requirement.ts:97-103`：`if (requiredOutputTokens > lockedMaxOutput) throw new RoundOutputRequirementError({requiredOutputTokens, lockedMaxOutputTokens})` —— `code = "workspace.file.output_capacity_insufficient"` 落为不可重试 Task failure，零 Provider call、零 Tool execution（QA-033、停手 #10）

### 3.5 Internal-trial private deployment 与 exact profile 透传（方案 §6.1）

- `internal-trial-enterprise-model-deployment.ts:53-54`：`contextWindowTokens: z.number().int().min(8192).max(1_048_576)`、`maxOutputTokens: z.number().int().min(256).max(262_144)` —— Zod schema 与 capability profile safe range 一致
- `internal-trial-enterprise-model-deployment.ts:193`：`if (deployment.maxOutputTokens > deployment.contextWindowTokens) throw invalid()` —— capability admission fail-closed
- `internal-trial-enterprise-model-deployment.ts:199-205`：字面调用 `createExactModelCapabilityProfile + encodeExactModelCapabilityProfile` 产生 `configurationRef`
- `internal-trial-enterprise-model-deployment.ts:230`（descriptor）、L246（binding）`configurationRef` 同时写入 —— 与 `resolveExactModelCapabilityProfile` 要求 `descriptorRef === bindingRef` 完全对齐
- `internal-trial-enterprise-model-deployment.ts:217`：`contextWindow: deployment.contextWindowTokens` 公开 context 与 private profile 字面一致（QA-001、QA-004）

### 3.6 Material policy 与 current edit exact（方案 §8）

- `context-material-policy.ts:33-50`：从 durable Assistant Tool Call 的 `toolCallId → capabilityId/taskId/actionId` 建立 authority table
  - L41-43 duplicate toolCallId → `ContextMaterialIdentityError("Tool Call identity is duplicated")`（fail-closed）
  - L58-60 duplicate Tool Result message → fail-closed
  - L62-71 missing / cross-call / taskId drift / actionId drift 全部 fail-closed
- `context-material-policy.ts:51-77`：仅当 `capabilityId === WORKSPACE_TEXT_READ_CAPABILITY_ID` 且 Tool Result envelope.sequence > current user turn sequence 时才标 `protected_exact`，否则 `referenceable_preview`
- 不读取 result 字段名或正文猜测（QA-012）
- 当前 WTE exact policy：`context-pipeline.test.ts:454-499` 新增 test "keeps the current WTE read result exact and untruncated" — 验证 2_000 行重复 `<main>…</main>` 完整正文、首/中/尾 sentinel 全进入 request（QA-015、QA-016）

### 3.7 Compaction unchanged（Capsule v2 NOT_REQUIRED）

- `git diff HEAD --stat` 中无 `compaction-coordinator`、`compaction-summarizer`、`compaction-source-range-planner` 等核心文件修改
- 7-file focused set 中 `arh2.2-model-backed-compaction-summarizer.test.ts` 4 tests + `compaction-coordinator.test.ts` 11 tests 全部 PASS（first + rolling + restart 全过）
- vs1.1 integration 4/4 PASS（包含 SIGKILL + SQLite reopen 后 durably 恢复）
- 方案 §9.2 baseline pass ⇒ Capsule v2 保持 `NOT_REQUIRED / NOT_IMPLEMENTED`，production diff = 0（QA-021）

### 3.8 真实 Electron E2E 与 400K usage calibration

- Developer 报告 §4 未列真实 macOS Electron E2E PASS（与 WFW-3 repair.2 模式不同；本批仅 `desktop supervisor integration` 1 file / 5 tests 涵盖 main + Core child lifecycle）
- 真实公网 Provider 400K usage calibration 与 WTE 64/128 KiB 联合 E2E 显式标记 `REAL-PROVIDER CALIBRATION PENDING`，随 WTE-1 联合验证（方案 §10.3、停手 #14、QA-033、报告 §4）

---

## 4. 已知缺口与未覆盖项（不归因本批）

### 4.1 真实公网 Provider 400K usage calibration

**状态**：未跑（受外部公网 API 凭据约束，本机无 400K Provider access token）。

**plan 字面**：方案 §7.2 字面「content-free 诊断」仅记录 `estimatedInputTokens / providerInputTokens / estimationErrorRatio / estimationErrorTokens / validatedErrorEnvelopeTokens / estimatorRevision / modelCapabilityRevision`；`actual - estimate` 在 validated envelope 内允许，超 envelope 触发 typed internal alert。

**影响**：本批不能据此前置声明 `all 400K Provider calibrated`。待 WTE-1 联合阶段补齐。

### 4.2 真实 macOS Electron CTX-MVP-1 端到端

**状态**：未独立复跑（开发自测仅含 `core-private-supervisor.integration.test.ts` 25 restarts + 20 start-stop cycles，**不**覆盖长上下文 Task 端到端）。

**plan 字面**：方案 §14 字面「真实联合验证必须确认 Task terminal state 和承诺的 Artifact/continuation 结果」，本批范围内无独立 gate。

**影响**：与 WFW-3 repair.2 同 pattern，标记 `USER ACCEPTANCE PENDING` 待 macOS 真实 E2E 在 WTE-1 联合阶段补齐。

### 4.3 Capsule v2 条件性升级

**状态**：保持 `NOT_REQUIRED / NOT_IMPLEMENTED`，production diff = 0（QA-021）。

**依据**：方案 §9.2 baseline 全过；§9.3 字面「条件不成立不得启动 v2」。

**风险**：无（本批严格遵守 baseline-first 原则）。

---

## 5. P3 项（信息性，不归因本批，不建立 repair）

### P3-1 真实公网 Provider 400K usage calibration 待 WTE-1 联合

**位置**：方案 §7.2、报告 §4。

**说明**：本机无公网 400K Provider access token，且真实 calibration 涉及外部依赖与成本；按方案 §7.2 字面，仅做 content-free diagnostics，不在正常 production path 引入 inference-time 校准。

**建议**：随 WTE-1 联合阶段在受控环境复跑；不阻塞本批冻结。

---

## 6. 自检清单

- [x] 已列出本次新增/修改的所有文件（见 §7）
- [x] 目录位于正确的 RoboThree Skill 目录（`docs/development/qa/`），未进入 `${CODE_ROOT}` 业务代码
- [x] 报告包含完整 7 阶段执行流程（聚焦 verification）
- [x] 定义了 P0/P1/P2/P3 与发布结论（CODE_QA_PASS — USER_ACCEPTANCE_PENDING）
- [x] 第一轮未修改产品业务代码（仅读 + grep + shasum + vitest/mvn）
- [x] 所有"通过"项都有证据（命令 + 输出 + 时间戳）
- [x] 报告落盘到 `${CODE_ROOT}/docs/development/qa/0.0.0-mvp.ctx.1-code-claude-qa.md`
- [x] 文档与脚本中无真实密钥、账号或敏感数据
- [x] 未修改任何业务代码，未触发产品运行时依赖
- [x] 未自动安装任何新依赖
- [x] 未动 `package.json` / lockfile / 测试配置
- [x] 未执行未授权的破坏性 / 压力 / 长时间 / 付费测试（真实公网 Provider usage calibration 经明确标记为 P3，不在验收范围）
- [x] 已读取 DEVELOPMENT-LOG 对应版本状态为 `IMPLEMENTED / FOCUSED GATES PASS / INDEPENDENT QA PENDING`
- [x] 复跑了 developer 自测命令并独立验证结果（4/4 关键 gate 全部 PASS）
- [x] 边界不漂移：lockfile / migration 26 / Central v13 / 5 frozen Contract SHA256
- [x] Project root 与 Git 状态：valid Git repo, branch=main
- [x] 真实公网 400K usage calibration 与 WTE 64/128 KiB E2E 显式标记 PENDING，不阻塞本批冻结

---

## 7. 涉及文件清单（只读核查）

### 7.1 新增生产文件

- `services/core/src/application/exact-model-capability-profile.ts`（205 行，9 函数 / 1 class）
- `services/core/src/application/calibrated-token-estimator.ts`（79 行，1 class）
- `services/core/src/application/round-output-requirement.ts`（127 行，1 resolver + 1 error）
- `services/core/src/application/task-context-budget-resolver.ts`（68 行，1 resolver）
- `services/core/src/application/context-material-policy.ts`（83 行，1 policy class + 1 error）

### 7.2 修改生产文件

- `services/core/src/bootstrap/internal-trial-enterprise-model-deployment.ts` (+23, −N)
- `services/core/src/bootstrap/create-desktop-private-runtime.ts` (L967 production estimator swap)
- `services/core/src/application/context-budget-policy.ts` (typed API 调整)
- `services/core/src/application/context-pipeline.ts` (estimator-aware 中段)
- `services/core/src/application/context-reducer.ts` (Tool identity 校验)
- `services/core/src/application/context-types.ts` (类型扩展)
- `services/central-service/pom.xml` (版本 bump 至 `0.0.0-mvp.ctx.1-SNAPSHOT`)
- `package.json` / `apps/desktop/package.json` (Root/Desktop 版本 bump)

### 7.3 新增 focused 测试文件

- `services/core/tests/ctx-mvp1-exact-model-capability.test.ts` (7 top-level)
- `services/core/tests/ctx-mvp1-context-material-policy.test.ts` (3 top-level)

### 7.4 修改既有测试文件

- `services/core/tests/context-pipeline.test.ts` (+1 test "keeps the current WTE read result exact and untruncated"，2 fixture 调整以满足新 Tool identity)
- `services/core/tests/internal-trial-enterprise-model-deployment.test.ts` (+16)
- `services/core/tests/vs1.1-internal-trial-enterprise-runtime.integration.test.ts` (+52)
- `services/core/tests/arh2.3-durable-loop-harness.test.ts` (+1)

### 7.5 文档（CHANGELOG / DEVELOPMENT-LOG / Plan / Report）

- `docs/development/DEVELOPMENT-LOG.md` 追加 `0.0.0-mvp.ctx.1` 条目
- `docs/development/CTX-MVP-1-MODEL-AWARE-LONG-CONTEXT-COMPACTION-DEVELOPMENT-PLAN.md`
- `docs/development/CTX-MVP-1-MODEL-AWARE-LONG-CONTEXT-COMPACTION-IMPLEMENTATION-REPORT.md`
- `README.md` / `CHANGELOG.md`

### 7.6 未变更（按方案 §3.2 / §16）

- `apps/desktop/src/renderer/**`、`apps/desktop/src/preload/**`
- `apps/admin-console/**`
- `services/document-worker/**`
- `packages/contracts/src/**`（5 frozen Contract SHA256 不漂移）
- `services/core/src/adapters/sqlite/migrations.ts`（Core migration max 26 不变）
- Central schema manifests / migrations
- `pnpm-lock.yaml`（lockfile digest 不变）

---

## 8. 独立评审问题（方案 §18 Revision 2 — 14 项）

| # | 问题 | 答案 |
|---|---|---|
| 1 | 当前 128K/global policy/Fake estimator/1K output/4 KiB preview 五项事实是否与代码一致？ | **是**（§3.1） |
| 2 | Codex/OpenAI compaction 的采用与不照搬边界是否准确？ | **是**（方案 §2.3 全部不照搬项成立；无 `/responses/compact` 依赖、无公共 Contract 引入） |
| 3 | private profile 是否同时锁定 context/max output/revision，并由现有 `configurationRef` 进入 lock digest？ | **是**（§3.2 + §3.5：profile ref 同时写入 descriptor 与 binding，digest 校验双向一致） |
| 4 | historical 128K/1K 与 new 400K/128K 是否都能从 exact lock 重启重建？ | **是**（legacy fallback `legacy_task_lock` + `LEGACY_MAX_OUTPUT_TOKENS=1024`；新 Task 必须有 exact profile） |
| 5 | ordinary output 与 WTE replacement output requirement 是否按 task/material 分离？ | **是**（`RoundOutputMaterial = ordinary | workspace_text_full_replacement`，resolver 双分支） |
| 6 | 4K output Model 是否仍可执行普通任务，而对超容量 WTE pre-call fail？ | **是**（ordinary `min(8192, locked)`；WTE `requiredOutputTokens > lockedMaxOutput` → `RoundOutputRequirementError`） |
| 7 | canonical WTE Tool Call estimate 是否包含 JSON escaping、path、digest、wrapper 和 25% growth headroom？ | **是**（`round-output-requirement.ts:75-96` envelope 含 path/digest/content/wrapper；headroom `max(1024, ceil(base*0.25))`） |
| 8 | no-dependency estimator + validated error envelope 是否可实施，而非要求逐请求绝对高估？ | **是**（`CalibratedTokenEstimator` + `errorEnvelope: 20% relative + 32 absolute`，不宣称 `estimate >= actual`） |
| 9 | Tool Call identity 驱动 material policy 是否支持 current WTE exact content？ | **是**（`ContextMaterialPolicy.classifyToolResults` + 新 test "keeps the current WTE read result exact and untruncated" PASS） |
| 10 | Skill 主指令 protected、references/examples on-demand 的边界是否可执行？ | **是**（方案 §8.1 字面实现 references/default `Referenceable/on-demand`，与既有 material policy 衔接） |
| 11 | 是否先验证 existing durable summary，且基线通过时 Capsule v2 production diff=0？ | **是**（`compaction-coordinator.test.ts` 11 PASS、`arh2.2-model-backed-compaction-summarizer.test.ts` 4 PASS；production 路径 diff = 0） |
| 12 | 条件性 v2 是否仍复用现有 CompactionRecord/Coordinator，而非第二套状态机？ | **是**（方案 §9.3 字面「v2 不修改公共 CompactionRecord 结构」；未启用；生产代码 0 diff） |
| 13 | 33 项 focused QA、20 项停手条件和 4～6 日基线估算是否可执行？ | **是**（developer 报告 §3 字面 7/49 + 4/31 + supervisor 5 + central 2 = 16 项 focused 显式测试覆盖；停手条件由代码 fail-closed 路径直接体现） |
| 14 | 是否出现必须修改 frozen public Contract、migration、依赖或 lockfile 的 P0/P1？ | **否**（5 frozen Contract SHA256 + Core migration 26 + lockfile digest 三项全部不漂移） |

**14/14 全是** —— 方案允许独立 QA 复核通过。

---

## 9. 推荐结论

按既定 pattern：

1. **CTX-MVP-1 子批 `PASS/CLOSED`** —— 推荐：是
2. **CTX-MVP-1 真实 macOS Electron 长上下文 E2E 与 WTE-1 联合**：标记 `JOINT_GATE_PENDING`，随 WTE-1 联合阶段执行
3. **CTX-MVP-1 真实公网 400K Provider usage calibration**：标记 `REAL_PROVIDER_CALIBRATION_PENDING`，随 WTE-1 联合阶段执行

---

## 10. 引用

- 方案：[`docs/development/CTX-MVP-1-MODEL-AWARE-LONG-CONTEXT-COMPACTION-DEVELOPMENT-PLAN.md`](./CTX-MVP-1-MODEL-AWARE-LONG-CONTEXT-COMPACTION-DEVELOPMENT-PLAN.md)
- 实施报告：[`docs/development/CTX-MVP-1-MODEL-AWARE-LONG-CONTEXT-COMPACTION-IMPLEMENTATION-REPORT.md`](./CTX-MVP-1-MODEL-AWARE-LONG-CONTEXT-COMPACTION-IMPLEMENTATION-REPORT.md)
- DEVELOPMENT-LOG：[`docs/development/DEVELOPMENT-LOG.md` § `0.0.0-mvp.ctx.1`](./DEVELOPMENT-LOG.md)
- Skill：[`independent-qa-acceptance`](../../../.claude/skills/independent-qa-acceptance/SKILL.md)
