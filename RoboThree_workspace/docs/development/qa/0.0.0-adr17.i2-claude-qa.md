# ADR17-I2 Independent QA Report

| 项目 | 值 |
| --- | --- |
| RUN_ID | `2026-08-02-adr17.i2-qa` |
| 版本 | `0.0.0-adr17.i2` (Root/Core) |
| 结论 | **INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING** |
| `harness:adr17i2` | **7 files / 84 tests PASS** (1.64s) |
| `pnpm run check` | Architecture PASS + **110 files / 721 tests PASS** + 3 smoke |
| P0/P1/P2/P3 | **P0=0 P1=0 P2=0 P3=0** |
| 执行时间 | 2026-08-02 23:01–23:03 CST |

## 执行环境

```text
Node.js：v24.13.0 / pnpm 11.11.0
Electron: 43.2.0 (Mac ARM)
```

## 1. 独立门禁

| 命令 | 独立结果 | 开发者声明 |
| --- | --- | --- |
| `harness:adr17i2` | **7 files / 84 tests PASS** | 7/84 ✅ |
| `CI=true pnpm run check` | Architecture PASS + **110 files / 721 tests** + 3 smoke | 110/721 ✅ |

独立执行，未复用开发者自测。

## 2. 核心场景逐项验收

### 2.1 串行分发与完整 Tool History ✅

`AgentLoopCoordinator` + `ToolCallBatchCoordinator` 组合验证：
- Tool Calls 按 ordinal 顺序串行执行
- 只有全部 Tool Call 都产生 Result 或 terminal disposition 后，才允许构造下一轮 Provider Message
- 50-round Tool loop 有界且完成在下一模型轮次
- 5 次完整 model-stream-to-completion 链产生同一 timeline digest

### 2.2 确认 allow/reject ✅

| 场景 | 断言 | 结果 |
| --- | --- | --- |
| allow | 持久化确认点、阻塞后续调用、allow 后按原序恢复 | PASS |
| reject | 当前与后续未分发调用收敛为 typed denied，无 Backend 执行 | PASS |

### 2.3 分发前取消 ✅

- 分发前取消直接写 terminal disposition（`cancelled_before_dispatch`）
- 禁止调用 Backend / 不创建 Effect Hook

### 2.4 Effect 链接与崩溃恢复 ✅

| 场景 | 断言 | 结果 |
| --- | --- | --- |
| Effect 已存在但 disposition 未链接 | 按 Action 身份重联，不创建第二 Effect | PASS |
| Effect-linked 调用崩溃后恢复 | 保留 Effect identity，不更换 | PASS |
| `onEffectPrepared` Hook | EFFECT PREPARED → 链接 effectAttemptId → Backend dispatch；Hook 失败时 Backend 不调用 | PASS |

### 2.5 Retry Run 隔离 ✅

旧 Run batch 在 Retry 创建新 Run 后不得分发——迟到事实不进入新 Run。

### 2.6 SQLite close/reopen ✅

精确 Task/Run batch 恢复一致；旧 Conversation 数据兼容。

### 2.7 旧路径失败关闭 ✅

含 Tool Calls 的 AgentLoopCoordinator 必须走 Batch Coordinator；旧 Conversation Writer 逐条 Tool Result 追加路径拒绝 tool-call message。

### 2.8 I1 回归全部保留 ✅

I2 harness 包含 I1 的 `tool-call-batch-persistence.conformance.test.ts` + `sqlite-tool-call-batch.integration.test.ts`（4 files / 52 tests），全部通过。

## 3. 边界验证

| 边界 | 状态 |
| --- | --- |
| Kernel | 未改 ✅ |
| Desktop | 未改 ✅ |
| Central | 未改 ✅ |
| Contracts | 未改 ✅ |
| Migration 13 | 未改（I1 已冻结） ✅ |
| ADR17-I3 超前 | 0 命中 ✅ |
| CGF-2C.1 超前 | 0 命中 ✅ |
| `onEffectPrepared` Hook 进入生产 | 内部 Application Hook，不暴露为公共 Port ✅ |

## 4. 结论

**P0=0 P1=0 P2=0 P3=0。ADR17-I2 INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING。**

- harness 7/84 PASS + check 110/721 + 3 smoke ✅
- 串行分发 + 完整 Tool History + 50-round bounded loop ✅
- 确认 allow/reject 双路径 + 取消分发前/后收敛 ✅
- Effect 链接/重联 + 崩溃窗口 + identity 不更换 ✅
- Retry Run 隔离 + SQLite close/reopen ✅
- I1 回归全量通过（harness 内含） ✅
- Kernel/Desktop/Central/Contracts 未变 ✅
- I3 / C.1 无超前 ✅

**建议关闭 ADR17-I2。ADR17-I3 GATED，等待用户单独授权。**
