# CGF-1.3C Independent QA Report

| 项目 | 值 |
| --- | --- |
| RUN_ID | `2026-07-27-cgf.1.3c-qa` |
| 版本 | `0.0.0-cgf.1.3c` |
| 结论 | **INDEPENDENT_QA_PASS — USER_ACCEPTED / CLOSED** |
| Node 全量 | 93 files / 600 tests PASS |
| harness:cgf13c | 3 files / 15 tests PASS |
| Central online | 53 tests / 0 failures / 0 skipped |
| Central offline | 53 tests / 0 failures / 0 skipped |
| 执行时间 | 2026-07-27 16:18~16:19 |
| P0/P1/P2/P3 | 0 |

## 执行环境

```text
Node.js：v24.13.0 / pnpm：11.11.0 / Java：OpenJDK 21.0.12
Central：0.0.0-cgf.1.2c-SNAPSHOT / macOS：Darwin 24.4.0
```

## 执行结果

| 命令 | 结果 |
| --- | --- |
| `CI=true pnpm exec vitest run` | 93 files / **600 tests PASS** |
| `pnpm run harness:cgf13c` | 3 files / **15 tests PASS** |
| `pnpm run check:central` | **53 tests / 0 failures / 0 skipped** |
| `pnpm run check:central:offline` | **53 tests / 0 failures / 0 skipped** |

Architecture boundary + ESLint PASS。TypeScript/Preload/Renderer build PASS。

Electron smoke（`smoke:preload`）在 headless CLI 环境跳过——已知环境限制。Vitest 93/600 + harness 3/15 + Central 双 53/0/0 为核心验收证据。

## 14 项独立 QA 逐条验证

### 1-2. clean build + harness:cgf13c 实际执行 ✅

93/600 + 3/15 + Central 53/0/0 x2。harness 包含双 SQLite close/reopen、Task generation 引用安全、GB blocker 和离线四状态测试。

### 3. enterprise DB 先于 Task DB / 无 ATTACH/跨库事务 ✅

`EnterpriseTaskGenerationRecoveryCoordinator`：固定先读 enterprise activation authority、再读 Task SQLite。无 ATTACH、无跨库事务 ✅

### 4. 旧/新 Task 保持旧/current generation / Selection/Lock 不漂移 ✅

harness 验证：旧 generation Task 保持旧 `TaskRuntimeSelection` + `TaskCapabilityLock`，新 generation Task 锁定当前 generation。Selection/Lock 不被改写 ✅

### 5. enterprise session invalid 时企业 Task waiting + 不重绑 ✅

企业会话无效时企业 Task 统一进入 `waiting`，不进入 Registry/Prompt，不静默重绑 ✅

### 6. revision 规范化唯一 / registry drift + 缺失 generation 失败关闭 ✅

Enterprise Snapshot revision（64 位 hex）与 Task `sha256:` digest 明确规范化。drift 失败关闭 ✅

### 7. GC blocker 覆盖完整引用范围 ✅

`EnterpriseGenerationReferenceAnalyzer` 覆盖：storage active/previous、runtime active、pending activation、failed target/fallback、非终态 Task、recovering Task、CapabilityLock ✅

### 8. 未实现自动删除/负引用计数/破坏性 GC ✅

Analyzer 永远只输出 `safeToDelete=false`。无删除 API、引用减计数、自动 GC ✅

### 9. 企业离线四状态 / 状态 4 不静默同步 ✅

`online` / `service unavailable` / `enterprise session invalid` / `recovered update waiting for application`。状态 4 只要求用户确认，不调用 sync/Storage Activation/Controlled Restart/Runtime Activation ✅

### 10. Runtime Activation attempt 列表 Memory/SQLite 同一 + close/reopen ✅

harness 验证双 SQLite close/reopen 后 attempt 列表一致 ✅

### 11. Contracts / Kernel / Central Java/API / migrations 未漂移 ✅

Central 53/0/0 x2，版本保持 `0.0.0-cgf.1.2c-SNAPSHOT`。Kernel reducer 未修改 ✅

### 12. Central 在线/离线各 53 tests ✅

online BUILD SUCCESS 53/0/0，offline BUILD SUCCESS 53/0/0 ✅

### 13. CGF-1.3A/B + CGF-1.2 + DCF-1.3 + KAF 全量回归 ✅

93/600 全量通过 ✅

### 14. P0=0 P1=0 / PASS 后等待用户接受 ✅

## 结论

**P0=0 P1=0 P2=0 P3=0。CGF-1.3C INDEPENDENT_QA_PASS — USER_ACCEPTED / CLOSED。**

- 93/600 + 3/15 + Central 53/0/0 x2 全部独立重跑 ✅
- enterprise DB 先于 Task DB / 无 ATTACH ✅
- 旧/新 Task 各保持 generation / Selection/Lock 不漂移 ✅
- enterprise session invalid → Task waiting + 不重绑 ✅
- GC blocker 全覆盖 / 无自动删除 ✅
- 离线四状态 / 状态 4 不静默同步 ✅
- attempt 列表 Memory/SQLite + close/reopen 一致 ✅
- Contracts/Kernel/Central/migrations 未变 ✅

**用户已于 2026-07-27 明确接受本报告；CGF-1.3C 与 CGF-1.3 正式
PASS/CLOSED。下一阶段继续 GATED，等待方案确认和明确授权。**
