# DCF-1.3B Independent QA Report

| 项目 | 值 |
| --- | --- |
| RUN_ID | `2026-07-26-dcf.1.3b-qa` |
| 版本 | `0.0.0-dcf.1.3b` |
| 结论 | **INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING** |
| Node 全量 | 87 files / 554 tests PASS |
| 专项（4 files） | 18 tests PASS |
| 执行时间 | 2026-07-26 22:47 |
| P0/P1/P2/P3 | 0 |

## 执行环境

```text
Node.js：v24.13.0 (nvm) / pnpm：11.11.0 / macOS：Darwin 24.4.0
```

## 执行结果

| 命令 | 结果 |
| --- | --- |
| `pnpm run clean` | 9 paths cleaned ✅ |
| `pnpm run build` | TS/Preload/Renderer PASS ✅ |
| `CI=true pnpm exec vitest run` | 87 files / **554 tests PASS** |
| 专项（4 files verbose） | **18 tests PASS** |
| Architecture boundary | PASS / ESLint PASS |

## 专项测试明细（4 files / 18 tests）

| 测试文件 | 关键验证 |
| --- | --- |
| `sse-backpressure-writer.test.ts` | write()===false→drain、30s deadline、ephemeral drop+heartbeat skip、20轮 drain/timeout |
| `desktop-event-reconnect-controller.test.ts` | dedupe 有界、abort 立即释放、durable-only 无 delta、100 次 reconnect |
| `dcf12a-core-private.e2e.test.ts` | 100 SSE 断连后 timer/subscription/response 回零、2nd SSE 拒绝 |
| `core-private-supervisor.integration.test.ts` | 25 restart + 20 start-stop + SQLite Session 不丢 |

## 20 项独立 QA 逐条验证

### 1. clean build 完整门禁 ✅

87 files / 554 tests PASS。专项 4 files / 18 tests PASS。

### 2. 只有 `response.write() === false` 才进入 backpressure ✅

`SseBackpressureWriter`：只有 `write()` 返回 `false` 才开始等待 `drain`。不使用 Event 空闲时间或 heartbeat 判断 slow consumer ✅

### 3. false 后 durable flush 等待 drain / 不继续 polling 或无界写 ✅

durable flush 保持单一 in-flight。等待 `drain` 期间 interval polling 不启动第二次 flush ✅

### 4. drain 恢复后从稳定 cursor 继续 / cursor 不提前推进 ✅

cursor 只在对应 durable frame 写入并恢复后推进 ✅

### 5. 30 秒是生产固定 deadline / 测试可通过 DI 缩短 ✅

`SLOW_CONSUMER_DEADLINE_MS = 30_000`（不可放大）。测试通过内部依赖注入缩短 ✅

### 6. deadline 到期只关闭当前 SSE / Main Snapshot-first reconnect ✅

slow timeout 只销毁受影响的 SSE response。不停止 Core、Task 或 Agent Loop。Main 使用原 cursor 和 Snapshot 收敛 ✅

### 7. durable Event 只留 SQLite / 无第二内存队列 ✅

durable Event 不复制到应用内存队列，继续保留在 SQLite ✅

### 8. backpressure 期间 10,000 ephemeral delta 有界丢弃 ✅

blocked 期间 ephemeral delta 直接丢弃（`ephemeralFramesDropped` 计数器）。测试覆盖 "drops ephemeral frames and skips heartbeat without building a queue" ✅

### 9. heartbeat blocked 时跳过 / 不持久化 / 不推进 cursor ✅

heartbeat 直接跳过（`heartbeatFramesSkipped` 计数器），不推进 cursor ✅

### 10. slow consumer 期间 durable Message 最终 replay/Snapshot 收敛 ✅

连接超时断开后由 Main 使用原 cursor 和 Snapshot 收敛，允许安全重复但不丢持久事实 ✅

### 11. drain recovery + timeout 各 20 轮 ✅

测试 "repeats drain recovery and slow timeout for 20 rounds without retained state" PASS ✅

### 12. 100 次 Main reconnect + abort 立即释放 backoff timer ✅

测试 "releases an abortable reconnect loop across 100 disconnects" + "aborts a pending reconnect backoff immediately" PASS ✅

### 13. 100 次真实 SSE connect/disconnect 后资源回零 ✅

测试 "returns SSE timers, subscriptions and responses to baseline after 100 disconnects" PASS。active response、poll/heartbeat timer 和 ephemeral subscription 全部回到零 ✅

### 14. 任意时刻 active SSE ≤ 1 ✅

第二条并发 SSE 返回 typed conflict ✅

### 15. 25 次 restart + 20 次 start-stop / runtimeInstance 唯一 ✅

测试 "keeps one durable SQLite view across 25 restarts and 20 start-stop cycles" PASS（4970ms）。runtimeInstance 唯一、active child ≤ 1、SQLite Session 不丢 ✅

### 16. 3,000 durable Event 下 maxDedupeSize=2048 / abort 后清理 ✅

`maxDedupeSize=2048`。测试 "keeps dedupe metrics bounded and releases the set on abort" PASS。`dedupeSetSize=0` 且 `cleanupCount` 增长 ✅

### 17. durable-only 只含 message_committed + task_status_changed ✅

测试 "supports a durable-only stream without synthesizing ephemeral deltas" PASS ✅

### 18. 四类 reset + duplicate durable + cursor replay 全量回归 ✅

全量 87/554 通过 ✅

### 19. transport/resource/dedupe metrics 不进入 Contract 或 Kernel ✅

`grep -rn "backpressure\|slow.consumer\|dedupe\|transport.*metric" services/core/src/kernel` → **0 命中** ✅

资源指标（`CorePrivateHttpResourceSnapshot`）为进程内实现，不进入公共 Desktop Local Contract ✅

### 20. 回归 DCF-1.3A/1.2/1.1/KAF + 无超前 1.3C/CGF-1.3 ✅

| 检查项 | 结果 |
| --- | --- |
| DCF-1.3C 30/60min Harness | 未实现 ✅ |
| CGF-1.3 | 未实现 ✅ |
| Kernel reducer 未修改 | ✅ |
| KAF-2/3 Effect/Receipt/Outbox 不变 | ✅ |

## 结论

**P0=0 P1=0 P2=0 P3=0。DCF-1.3B INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING。**

- 87/554 clean+build+vitest 独立重跑 ✅
- write()===false 唯一触发 backpressure ✅
- 30s fixed deadline / 测试 DI 可缩短 ✅
- drain 恢复 cursor 不提前 ✅
- ephemeral drop + heartbeat skip 不排队 ✅
- durable Event 只留 SQLite / 无内存队列 ✅
- dedupeSet 2048 有界 + abort 清理 ✅
- 100 SSE 断连资源回零 / 25 restart + 20 start-stop SQLite 不丢 ✅
- Kernel 隔离 + 无超前 DCF-1.3C/CGF-1.3 ✅

**DCF-1.3C / CGF-1.3 GATED，等待用户明确接受后进入。**
