# 0.0.0-pdt.2 — Claude Code 独立 QA 报告

## 基本信息

| 属性 | 内容 |
| --- | --- |
| RUN_ID | `2026-08-08-2324-version-0.0.0-pdt.2` |
| 验收对象 | PDT-2：PDF Table Extract Core Registry / Agent Exposure |
| 日期 | 2026-08-08 |
| 验收者 | Claude Code（独立 QA，只读） |
| 触发方式 | 用户显式指令（"PDT-2 已完成实现和自测，IMPLEMENTED / SELF-TEST PASS / INDEPENDENT QA PENDING"） |
| 环境 | Node v24.13.0 / pnpm 11.11.0 / vitest 4.1.10 |
| 开发版本 | Core / Document Worker `0.0.0-pdt.2`；Desktop / Contracts 保持 `0.0.0-mar.1b`/`0.0.0-mar.1.0`；Root `0.0.0-dtp.4` |
| 证据目录 | `qa-reports/2026-08-08-2324-version-0.0.0-pdt.2/evidence/` |

治理说明：PDT-1 已正式 `PASS/CLOSED`（PDT-1 P3 DW 版本未升版已在本批关闭：`0.0.0-dwo.1` → `0.0.0-pdt.2`）。本批是 `extract_tables` 首次作为**正式 Core Tool Registry 注册且默认 Agent 可见**的批次，本报告重点核查 schema 泄漏、五工具 catalog、bounded preview、scripted model 触发逻辑。

---

## 一、门禁复跑结果（全部独立执行，未采信自测）

| # | 门禁 | 结果 | 与自测声明对比 |
| --- | --- | --- | --- |
| 1 | PDT-2 focused（registry + context + backend + scripted model） | **PASS** 4 files / 29 tests | 一致 |
| 2 | Document Worker 全量 | **PASS** 23 files / 181 tests | 一致 |
| 3 | Core 全量测试 | **PASS** 77 files / 590 tests | 一致（本次沙箱内直接通过） |
| 4 | Desktop 全量测试 | **PASS** 23 files / 104 tests | 一致（本次沙箱内直接通过） |
| 5 | 关键 E2E（dtp3b + dwe3 + dcf12b） | **PASS** 3 files / 3 tests | 一致 |
| 6 | lint + architecture boundary | **PASS** | 一致 |
| 7 | `audit:dtp4` | **PASS**（Core/DW `0.0.0-pdt.2` 版本期望） | 一致 |
| 8 | offline frozen install | **PASS**（Already up to date） | 一致 |
| 9 | 完整 `pnpm run check` | **PASS** 151 files / 1033 tests ＋ 三项 smoke | 一致 |

回归基线：PDT-1 为 151 files / 1031 tests；本次 1033 tests（+2）。无既有用例回归。

---

## 二、静态核查（Core Registry / Agent Exposure，全部通过）

| 关注点 | 结论 | 证据 |
| --- | --- | --- |
| **五工具 catalog** | ✅ | `DOCUMENT_CAPABILITIES` = 4 只读（extract_text/extract_tables/xlsx.read/docx.read）+ Core registry xlsx.write = 5 个正式 Document Tool（`document-capability-options.ts` L3-7；`document-tool-registry.ts` CAPABILITY_MATERIALS） |
| **extract_tables schema 无泄漏** | ✅ | 使用新的 `documentToolModelInputSchema`（仅 `relativePath` + `options`），无 `workspaceRoot`/`limits`/绝对路径/digest/parser authority（`document-tool-registry.ts:669-682`）；与 xlsx.write 对齐，优于三个旧 read 工具仍用的 DTP-2A `documentToolInputSchema`（含 workspaceRoot/limits — 既有已知观察） |
| **output schema 结构化** | ✅ | `PDF_TABLE_RESULT_SCHEMA`：format/extraction/page count/tables[]/warnings[]，逐层定义 table（pageNumber/tableIndex/rowCount/columnCount/confidence/locator/rows/bbox/warnings）+ row/rowIndex/cells/bbox + cell/rowIndex/columnIndex/text/bbox/confidence/warnings——完整但无原始解析器输出 |
| **bounded preview** | ✅ | `pdfTablePreview`：≤5 tables、≤3 rows per table、≤6 cells per row、≤8 warnings（`document-tool-context.ts:156-185`）；整体 4KiB UTF-8 truncation；无原始 `tables` JSON 或超大 cell 文本 |
| **scripted model table intent** | ✅ | `isTableIntent`：英文 `table`/`tables`/`tabular` + 中文 `表格`/`表` + `.pdf` → `extract_tables`（mode absent）；普通 PDF 不匹配时走既有 `extract_text`（`desktop-document-scripted-model-provider.ts:126-128,154-155`） |
| **backend 执行真实 Worker** | ✅ | Core backend `DOCUMENT_CAPABILITY_SET` 含 `extract_tables`；`parseDocumentActionPayload` 接受 `extract_tables` options；focused integration 通过子进程执行 PDT-1 已实现的 pdf-extract-tables |
| **PDT-1 P3 已关闭** | ✅ | DW `package.json` 从 `0.0.0-dwo.1` → `0.0.0-pdt.2` |
| **无越界** | ✅ | Core 未导入 `pdfjs-dist`/parser 代码；无 OCR/tesseract/opencv/sharp；Contracts/Desktop/Central 零 `extract_tables` 标识；根配置/lockfile 未改 |

---

## 三、发现与分级

### P0 = 0，P1 = 0，P2 = 0，P3 = 0

无代码级、治理级或越界发现。无环境阻塞（本次所有全量门禁在沙箱内直接通过）。

---

## 四、总体结论

```text
结论：PASS（P0 = 0 / P1 = 0 / P2 = 0 / P3 = 0）
```

- PDT-2 的 Core Registry/Agent Exposure 正确：extract_tables 进入正式五工具 catalog、模型可见 schema 用新 `documentToolModelInputSchema`（仅 relativePath+options，无 workspaceRoot/limits）、bounded preview（≤5 tables/3 rows/6 cells/8 warnings + 4KiB truncation）、scripted model 仅 table 意图触发（普通 PDF 走 extract_text）、Core backend 执行真实 Worker。
- PDT-1 P3（DW 版本未升版）已关闭。无 OCR/扫描件/XLSX 导出越界，Core 未导入 parser 代码。
- 全部门禁独立复跑通过，与自测声明一致。
- `tool.document.pdf.extract_tables` 现在是正式注册、默认 Agent 可见的只读 Document Tool。
- **PDT-2 可进入用户接受流程。**

## 五、建议的下一步

1. 用户接受 PDT-2 QA 后正式关闭 PDT-2。
2. 单独授权 PDT-3（Desktop Product E2E：交互式表格提取闭环、scripted model 意图、assistant 最终回复）。
3. PDT-4、OCR/扫描件、XLSX 导出、人工校正 UI、bulk extraction 保持 GATED。

— Claude Code（独立 QA，只读，未修改任何生产代码）
