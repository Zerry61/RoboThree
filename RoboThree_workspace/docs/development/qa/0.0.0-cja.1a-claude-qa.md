# Alignment-1A Independent QA Report

| 项目 | 值 |
| --- | --- |
| RUN_ID | `2026-07-28-cja.1a-qa` |
| 版本 | `0.0.0-cja.1a-SNAPSHOT` |
| 结论 | **INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING** |
| Central online | 66 tests / 0 failures / 0 skipped |
| Central offline | 66 tests / 0 failures / 0 skipped |
| Testcontainers + Embedded PostgreSQL | 实际执行，0 skipped |
| 执行时间 | 2026-07-28 14:28 |
| P0/P1/P2/P3 | 0 |

## 执行环境

```text
Java：OpenJDK 21.0.12 / Maven Wrapper：3.9.16
Node.js：v24.13.0 / macOS：Darwin 24.4.0
```

## 执行结果

| 命令 | 结果 |
| --- | --- |
| `pnpm run check:central` | **66 tests / 0 failures / 0 errors / 0 skipped** |
| `pnpm run check:central:offline` | **66 tests / 0 failures / 0 errors / 0 skipped** |
| Testcontainers PostgreSQL 16 | 实际执行 ✅ |
| Embedded PostgreSQL 16 | 实际执行 ✅ |
| fat jar Lombok | BOOT-INF/lib 不含 lombok ✅ |

## 12 项独立 QA 逐条验证

### 1. check:central + check:central:offline 实际执行 ✅

66/0/0 x2。Testcontainers + Embedded PostgreSQL 均实际执行。

### 2. canonical Contract/Schema/Fixture + V1-V5 零修改 ✅

| 资源 | MD5 | 状态 |
| --- | --- | --- |
| `openapi.yaml` | `5c2449dd...` | 未变 ✅ |
| V1 | `c32d2f2a...` | 未变 ✅ |
| V2 | `10784542...` | 未变 ✅ |
| V3 | `3f6df8f1...` | 未变 ✅ |
| V4 | `ac96006b...` | 未变 ✅ |
| V5 | `61902dd0...` | 未变 ✅ |

### 3. 所有业务 Controller 只声明 GET/POST / 无 Persistence/事务/Provider ✅

`@PutMapping/@PatchMapping/@DeleteMapping` → 0 命中。Controller HTTP 包无 `JdbcTemplate/@Transactional/TransactionTemplate/Flyway` import ✅

### 4. Envelope/Command/ETag/304/Cache-Control 逻辑已迁出 Controller ✅

Controller 方法只保留：接收 DTO → 调用 Facade/Mapper → 调用 ResponseAssembler → 返回 ResponseEntity。校验/Mapper/Assembler 已按计划拆入独立 Adapter ✅

### 5. Bearer Filter 仅保护精确配置路径 / readiness/compatibility/Fixture 不受影响 ✅

`EnterpriseBearerTokenFilter` 只匹配精确 Configuration GET 路径。readiness/compatibility/Foundation Fixture 路径不受影响 ✅

### 6. 缺失/空/错误前缀/whitespace/过长/多值 Bearer → access_token_invalid ✅

`EnterpriseBearerTokenFilter`：精确接受 `Bearer `，拒绝缺失、空 Token、错误大小写、whitespace、>8192 字符、多个 Authorization Header。统一返回 `access_token_invalid`，不泄漏判断细节 ✅

### 7. Filter order 自动化断言 + Application Token Validator 仍执行 ✅

Filter order 由自动化读取实际 `@Order` 验证（非注释）。Application Token Validator 继续执行 `configuration.read` 权限校验。无路径或顺序绕过 ✅

### 8. 64 路并发 Bearer/correlationId 隔离 + 安全响应无 Token/SQL ✅

并发 64 请求验证单请求属性隔离、请求结束清理 + correlationId 唯一。安全响应不含 Authorization/Bearer/SQL/内部消息 ✅

### 9. Error Envelope 仍为 strict v1alpha1 六字段 ✅

`GlobalExceptionHandler` 输出保持 `contractVersion/errorCode/category/retryable/safeSummary/correlationId`，无新增字段。未知异常不返回 message/class/SQL/Token ✅

### 10. Lombok 仅触碰的 HTTP Adapter / 危险注解被 Guard 拒绝 / fat jar 不含 ✅

`lombok.config` 禁止 `@SneakyThrows`(error) + `@Data`(warning)。`BOOT-INF/lib` 不含 `lombok` ✅

### 11. Alignment-1B/2A/2B/CAS/Model/CGF-2 未超前 ✅

| 检查项 | 结果 |
| --- | --- |
| MyBatis-Plus | 不存在 ✅ |
| Flyway 移除 | 不存在 ✅ |
| Micrometer/OTel Tracing | 不存在 ✅ |
| 真实 CAS/Model | 不存在 ✅ |
| CGF-2 | 不存在 ✅ |

### 12. P0=0 P1=0 P2=0 P3=0。Alignment-1B GATED ✅

## 结论

**P0=0 P1=0 P2=0 P3=0。Alignment-1A INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING。**

- Central 66/0/0 x2 + Testcontainers + Embedded PostgreSQL 全部独立重跑 ✅
- Contract/Schema/Fixture/V1-V5 零修改 ✅
- Thin Controller + Bearer Filter + Global Exception 收敛完成 ✅
- 64 路并发隔离 + Filter order 自动验证 ✅
- Lombok 受控 + fat jar 不含 ✅
- 无超前 Alignment-1B/2A/2B/CGF-2 ✅

**Alignment-1B GATED，等待用户明确接受后进入。**
