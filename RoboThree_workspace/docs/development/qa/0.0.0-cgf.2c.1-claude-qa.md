# CGF-2C.1 Independent QA Report

| 项目 | 值 |
| --- | --- |
| RUN_ID | `2026-08-03-cgf.2c.1-qa` |
| 版本 | `0.0.0-cgf.2c.1` (Root/Core/Contracts) / `0.0.0-cgf.2c.1-SNAPSHOT` (Central) |
| 结论 | **PASS/CLOSED — USER ACCEPTED** |
| `harness:cgf2c1` | **11 Node files / 79 tests PASS + 8 Java evidence + 30/30 matrix items + leak 0** |
| `pnpm run check` | Architecture PASS + **116 files / 757 tests PASS** + 3 smoke |
| `check:central` | **214 / 0 / 0 / 0** BUILD SUCCESS |
| `check:central:offline` | **214 / 0 / 0 / 0** BUILD SUCCESS |
| P0/P1/P2/P3 | **P0=0 P1=0 P2=0 P3=0** |
| 执行时间 | 2026-08-03 13:07–13:18 CST |

## 执行环境

```text
Java：OpenJDK 21.0.12 / Spring Boot 3.5.16
Node.js：v24.13.0 / pnpm 11.11.0
Docker Engine 29.6.2 / PostgreSQL 16-alpine
Electron: 43.2.0 (Mac ARM)
```

## 1. 独立门禁

| 命令 | 独立结果 | 开发者声明 |
| --- | --- | --- |
| `harness:cgf2c1` | **11/79 Node + 8 Java + 30/30 + leak 0** | 一致 ✅ |
| `CI=true pnpm run check` | Architecture PASS + **116/757** + 3 smoke | 一致 ✅ |
| `CI=true check:central` | **214/0/0/0** | 214 ✅ |
| `CI=true check:central:offline` | **214/0/0/0** | 214 ✅ |

独立执行，未复用开发者结果。

> **注**：首次将 Central online 与 offline 并行后台运行时，online 出现 MojoFailureException——原因是两个 Maven 进程并发写同一 `target/surefire-reports/` 目录导致报告文件互相覆盖。**单独重跑 online 后 214/0/0/0 BUILD SUCCESS**，确认非产品缺陷，是 QA 执行方式竞态。offline 并行通过不受影响。

## 2. Harness 完整性

```
status: PASS
matrix: CGF-2C.1-section-14
matrixItemCount: 30
nodeEvidenceFileCount: 11
javaEvidenceClassCount: 8
evidenceDigest: sha256:6471bec7fef6265c4d72ebab61c17e72bb348f2fbec62c8b5e2b85f0939cc9d1
sensitiveContentMatchCount: 0
durationMs: 4053
```

## 3. 核心机制验收

### 3.1 task_model_external_scope ✅

[confirmation-scope.ts:45](packages/contracts/src/authorization/confirmation-scope.ts#L45) 新增 `type: z.literal("task_model_external_scope")` 分支。`user-confirmation.ts:22` 同步处理。七类 Model Context provenance 与 additive scope 落盘。

### 3.2 per-Task Provider 解析 ✅

`AgentLoopCoordinator` 支持每 Task 注入精确 `ModelProvider`；`DurableAgentLoopStarter` 从锁定 AdapterDescriptor 解析 Handle，传递不可变 `ModelProviderInvocation`。Tool Schema 只来自锁定 Tool 集合。

### 3.3 SQLite migration 14 + L1/L2/L3 ✅

[migrations.ts:655](services/core/src/adapters/sqlite/migrations.ts#L655) 新增 `cgf_2c_1_model_invocation_links` 表 + recovery index。L1（accept intent）、L2（Central identity/progress）、L3（Assistant Message commit）三提交点实现，迁移 1～13 未改。

### 3.4 HttpEnterpriseModelProvider ✅

[http-enterprise-model-gateway-client.ts](services/core/src/adapters/http/http-enterprise-model-gateway-client.ts) 实现：HTTPS、manual redirect、Bearer header-only、request/response/SSE 上限、sequence/digest/identity 校验、inactivity timeout、**Token 最多续签一次**（line 344 `let renewed = false` + line 354 拒绝二次续签）、status-first 恢复、同一 Invocation cancel。

### 3.5 Central HTTP/SSE Surface ✅

- [ModelInvocationController.java](services/central-service/src/main/java/com/robothree/central/modelgateway/adapter/http/ModelInvocationController.java) Thin GET/POST
- `ModelInvocationHttpMapper` strict DTO
- [ModelInvocationAdmissionPolicy.java](services/central-service/src/main/java/com/robothree/central/modelgateway/application/ModelInvocationAdmissionPolicy.java) production `user_confirmed` admission
- subscriber-before-execute + local owner 抑制 + passive/recovery subscriber lease/fencing
- canonical `started`/`text_delta`/`tool_call` ephemeral buffer

### 3.6 output_unrecoverable 复用 ADR-015 ✅

[durable-enterprise-model-provider.ts:35](services/core/src/application/durable-enterprise-model-provider.ts#L35) 与 Controller:134 统一使用 `model_stream_resume_unavailable`（非初稿错误名）。输出连续性不可证明时进入人工处理，不创建第二 Invocation。

## 4. 边界验收

| 边界 | 状态 |
| --- | --- |
| Enterprise canonical `model-invocation.schema.json` | MD5 `d35f9363...` 未变 ✅ |
| Central v0007 B0007 | MD5 `9b8cf002...` 未变 ✅ |
| SQLite migration 1～13 | 未改（14 新增）✅ |
| Kernel reducer | `task_model_external_scope`/`model_stream_resume` 0 命中 ✅ |
| Desktop | `0.0.0-dcf.2.3-demo.2` 保持，无 C.2 超前 ✅ |
| CGF-2C.2/C.3 | 无超前实现 ✅ |

## 5. 结论

**P0=0 P1=0 P2=0 P3=0。CGF-2C.1 PASS/CLOSED — USER ACCEPTED。**

- harness 11/79 + 8 Java + 30/30 + leak 0 ✅
- check 116/757 + 3 smoke ✅
- Central online/offline 214/0/0/0 ×2（首次 online 失败为 QA 并行竞态，重跑通过） ✅
- task_model_external_scope + 七类 provenance ✅
- per-Task Provider 解析 + migration 14 L1/L2/L3 ✅
- HttpEnterpriseModelProvider token-once + status-first ✅
- Central Thin Controller + user_confirmed admission + SSE owner ✅
- `model_stream_resume_unavailable` 复用 ADR-015 ✅
- Contract/v0007/Kernel/migration 1-13 未变 ✅
- C.2/C.3/Enterprise Integration 无超前 ✅

用户已正式接受独立 QA 并关闭 CGF-2C.1。CGF-2C.2 继续 GATED，等待用户提供
需求、确认 Model Experience PRD/UX 并单独授权。
