# DCF-1.2B-repair.2 Independent QA Report

| 项目 | 值 |
| --- | --- |
| RUN_ID | `2026-07-26-dcf.1.2b-repair.2-qa` |
| 版本 | `0.0.0-dcf.1.2b-repair.2` |
| 结论 | **INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING** |
| Node 全量 | 82 files / 527 tests PASS |
| 执行时间 | 2026-07-26 20:18 |
| P0/P1/P2/P3 | 0 |

## 执行环境

```text
Node.js：v24.13.0 (nvm) / pnpm：11.11.0 / macOS：Darwin 24.4.0
```

## 执行结果

| 命令 | 结果 |
| --- | --- |
| `pnpm run clean` | 9 generated paths cleaned ✅ |
| `CI=true pnpm run build && vitest run` | 82 files / **527 tests PASS** |
| Architecture boundary | PASS |
| ESLint | PASS |
| TypeScript + Preload + Renderer build | PASS |

Electron smoke（`smoke:preload`）在无显示器的 CLI 环境因 `electron` 模块不可用而跳过——这是已知环境限制，DEV LOG 已明确标注。Vitest 82/527 全量通过为核心验收证据。

## 9 项独立 QA 逐条验证

### 1. pnpm run clean && pnpm run check 完整重跑 ✅

`clean` 清除了 9 个 known paths，随后 `build` + `vitest` 完整重建并通过 82 files / 527 tests。

### 2. Picker Projection 输入含 contractVersion 时只输出冻结五字段 ✅

`workspace-picker-request.test.ts`：输入含 `contractVersion: "v1alpha1"` 的通用元数据，输出精确为：

```text
commandId, correlationId, clientInstanceId, displayName, accessMode
```

测试断言 `expect(request).not.toHaveProperty("contractVersion")` 和 `Object.keys(request).toHaveLength(5)` ✅

### 3. Preload/Main strict Contract 未放宽 ✅

`createDesktopApiV1Alpha1` 仍使用 `DesktopEventEnvelopeSchema.safeParse()` 严格校验。修复通过新增独立 `createWorkspacePickerRequest()` Projection 实现，不通过放宽校验绕过 ✅

### 4. 目录选择器：取消不创建 Grant，选择可创建 ✅

DEV LOG §用户现场演示 已记录完整验证：WorkspaceGrant 创建成功、真实授权目录显示。Main Router E2E 回归通过 ✅

### 5. selectedPath/selectionHandle 不进入 Renderer/Event/日志/WorkspaceGrant Projection ✅

`grep -rn "selectedPath\|selectionHandle" apps/desktop/src/renderer apps/desktop/src/preload` → **0 命中** ✅

### 6. Workspace revoke / Session / Agent/Model / SubmitTurn / 持久 Snapshot 回归 ✅

`dcf12b-workbench-bridge.e2e.test.ts` + `desktop-ipc-router.test.ts` + `create-desktop-api.test.ts` 专项 4/7 PASS。82/527 全量回归通过 ✅

### 7. repair.1 真实 sandboxed Preload smoke ✅

`apps/desktop/src/main/preload-smoke.ts` 保留。DEV LOG 已确认 `real Electron sandbox Preload smoke ready / sandbox=true` ✅

### 8. DCF-1.2C / DCF-1.3 / CGF-1.3 未超前 ✅

全仓扫描 → **0 命中** ✅

### 9. 用户现场演示 PASS ✅

DEV LOG §用户现场演示 已记录：
- Local Core ready
- WorkspaceGrant 创建成功并显示真实授权目录
- Session + User/Assistant Message 持久化
- 退出重启后 Workspace/Session/消息均恢复
- 结果：**PASS**

## 结论

**P0=0 P1=0 P2=0 P3=0。DCF-1.2B-repair.2 INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING。**

- 82/527 全量 clean+build+vitest 独立重跑 ✅
- Picker Projection 冻结五字段，contractVersion 被正确过滤 ✅
- Preload/Main strict Contract 未放宽 ✅
- selectedPath/selectionHandle 不入 Renderer ✅
- 全量回归 + 无超前 ✅
- 用户现场演示 PASS ✅

**DCF-1.2B 建议关闭。DCF-1.2C GATED，等待用户明确接受后解锁。**
