# 0.0.0-arh.2.2 — Claude Code 独立 QA 报告

## 基本信息

| 属性 | 内容 |
|---|---|
| RUN_ID | `2026-08-12-2331-version-0.0.0-arh.2.2` |
| 验收对象 | ARH-2.2：Production Automatic Compaction Orchestration |
| 日期 | 2026-08-12 |
| 验收者 | Claude Code（独立 QA） |
| 环境 | Node v24.13.0 / pnpm 11.11.0 / Java 21 (Central) |
| 开发版本 | Core `0.0.0-arh.2.2`；Contracts/Desktop/DW 保持原版本 |
| 证据目录 | `qa-reports/2026-08-12-2331-version-0.0.0-arh.2.2/evidence/` |

---

## 一、门禁复跑结果（全部串行独立执行）

| # | 门禁 | 结果 |
|---|---|---|
| 1 | `harness:arh2.2` | **PASS** 9 files / 47 tests |
| 2 | `pnpm run check` | **PASS** 156 files / 1067 tests + 3 smoke |
| 3 | `check:central` (online) | **PASS** BUILD SUCCESS (3:06) |
| 4 | `check:central:offline` | **PASS** BUILD SUCCESS (13:27) |

---

## 二、14 项重点核查（全部通过）

| # | 核查项 | 结论 | 证据 |
|---|---|---|---|
| 1 | ContextPreparationCoordinator 唯一入口 | ✅ | `prepare()` 为 buildRequest 唯一 Context 准备入口；`#loadView` → assess → plan → compact → reload → final rerun |
| 2 | 80% 触发线与硬上限分离 | ✅ | L144 `initial ≤ threshold → not_required`；L161-174 `exceedsAvailableInput` 才 typed fail；压缩后仍 > available 才 `available_input_exceeded` |
| 3 | Tool Call/Result、waiting confirmation 不跨压缩边界 | ✅ | `CompactionSourceRangePlanner` 仅取闭组；`ConversationAtomicGroupPlanner` 非终态 disposition → blocked → open group 留 raw tail |
| 4 | compaction_summary 独立 purpose-bound admission | ✅ | `authorizeAndCompact` 回调委托；`dataScopeDigest` 含 `purpose=compaction_summary`（§7.2） |
| 5 | admission pending/rejected 稳定 Receipt + 确认前零 Job | ✅ | `ContextPreparationAdmissionInterruption`（`model.user_confirmation_required`/`authorization.user_rejected`）；rejected 映射 `failed` + `admission_rejected`；确认前无 `authorizeAndCompact` Provider 调用 |
| 6 | 历史 Assistant provenance 精确 revision | ✅ | `CompactionProvenanceResolver` 从 immutable source range 重建 + source digest 校验；跨 Model/Relay 或 drift → `model.external_scope_unclassifiable` |
| 7 | Binding 重启后精确恢复 | ✅ | `CompactionSummarizerResolver` 接口（§8.1）：load Binding → 校验 digest/revision → 重建 Handle → drift 失败关闭 |
| 8 | migration 19 forward-only + InMemory/SQLite 一致 | ✅ | `arh_2_2_compaction_model_invocation_links` forward-only；InMemory 同语义 conformance；`ON DELETE CASCADE` + recovery index |
| 9 | 摘要 invocation link 不伪造 Assistant Message | ✅ | 表 17 列，无 `assistant_message_id`/`message_committed_at`；主 Link 的 `(task,run,round)` 唯一键不被触碰 |
| 10 | 部分/不可恢复输出不提交 Summary | ✅ | §10.1：output-started-unrecoverable → `recovery_exhausted`，不提交部分文本、不自动新建调用 |
| 11 | 第二事务原子提交 | ✅ | `summaryCommittedAt` + Record + Job + Receipt + Event + Outbox 原子提交（§9.2 + coordinator 第二事务路径） |
| 12 | 公共 Contracts/Kernel/Desktop/Central 无范围漂移 | ✅ | Contracts(08-06)/Desktop(08-08) 未改；kernel reducer(07-20~07-23) 未改；Central 未含 compaction 组件 |
| 13 | ARH-2.3/ARH-3 未提前实现 | ✅ | 无 restart harness/50-round/token accounting/prompt cache 生产实现 |
| 14 | 47 项 QA 范围核验 | ✅ | harness 47 tests 覆盖 budget/atomic/provenance/admission/link/recovery/architecture 七类 |

---

## 三、发现

### P0 = 0，P1 = 0，P2 = 0，P3 = 0

---

## 四、结论

```text
结论：PASS（P0=0 / P1=0 / P2=0 / P3=0）
```

ARH-2.2 的生产自动压缩编排正确接入 `DurableAgentLoopStarter` 唯一 `buildRequest` 路径，14 项重点核查全部通过。**ARH-2.2 建议 PASS/CLOSED。** ARH-2.3、ARH-3 继续 GATED。

— Claude Code（独立 QA，只读）
