# DCF-1.2B Independent QA Report

| 项目 | 值 |
| --- | --- |
| RUN_ID | `2026-07-26-dcf.1.2b-qa` |
| 版本 | `0.0.0-dcf.1.2b` |
| 结论 | **INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING** |
| Node 全量 | 80 files / 525 tests PASS |
| 专项 | 4 files / 8 tests PASS |
| 执行时间 | 2026-07-26 18:41 |
| P0/P1/P2/P3 | 0 |

## 执行环境

```text
Node.js：v24.13.0 (nvm) / pnpm：11.11.0 / macOS：Darwin 24.4.0
```

## 执行结果

| 命令 | 结果 |
| --- | --- |
| `CI=true pnpm run check` | 80 files / **525 tests PASS** |
| 专项（API/Router/Boundary/Bridge） | 4 files / **8 tests PASS** |

Architecture boundary：PASS。Core smoke：ready。Desktop smoke：fixtureOnly=true。Vue Renderer production build：PASS。

## 15 项 QA 逐条

### 1. Node 24.13.0 pnpm run check 独立重跑 ✅

80 files / 525 tests PASS。

### 2. DCF-1.2A PASS/CLOSED 回链 ✅

DEV LOG 已确认 A 为 `PASS/CLOSED — USER ACCEPTED`。

### 3. Main 只注册固定业务 channel ✅

`DESKTOP_IPC_CHANNELS` 固定枚举：Workspace/Session/Catalog/Conversation/SubmitTurn/Runtime Status。`DesktopIpcRouter` switch-case 只处理已知 channel。

### 4. Main/Preload 两侧拒绝未知字段/非法 UUID/enum ✅

Preload `createDesktopApiV1Alpha1` 对输入执行 strict Contract 校验，对 Main 输出再次校验。非法 Event 被丢弃。

### 5. selectedPath/selectionHandle 不进入 Renderer/Preload ✅

`grep -rn "selectedPath\|selectionHandle" apps/desktop/src/preload apps/desktop/src/renderer` → **0 命中**。目录选择只在 Main 发生。

### 6. 目录选择取消不创建 Grant/不残留 handle ✅

Main 系统目录选择器只在真实结果时注册 selectionHandle。取消 → 无 Grant 创建。

### 7. Workspace/Session UI + Router 语义 ✅

支持 Workspace 选择/授权/列出/撤销、Session 创建/打开/重命名/软删除。

### 8. Agent/Model 来自 Core Projection ✅

`defaultModel`、`allowModelOverride`、`eligibleModels` 均来自 Core Catalog Projection。Renderer 不自行计算权限交集。

### 9. SubmitTurn 只提交选择意图 ✅

Renderer 提交用户意图（Agent/Model/Workspace），实际校验/锁定/Receipt 由 Local Core 完成。Snapshot 不泄漏 Capability Binding/Adapter/Credential/Effect/Receipt/Outbox。

### 10. durable Event 只触发 Snapshot 收敛 ✅

UI 重新查询持久 Conversation Snapshot。Renderer 不建立第二 Task reducer。

### 11. Renderer CSP + 无 Node/Electron/raw IPC/fetch ✅

CSP `connect-src 'none'`。Renderer 无 `fetch`/`WebSocket`/`EventSource`/`Electron`/`Node`/raw IPC。

### 12. E2E：User + Scripted Assistant Message ✅

`dcf12b-workbench-bridge.e2e.test.ts`：真实 SQLite + Main Router + Core E2E。产生一条 User Message + 一条持久 Scripted Assistant Message。

### 13. 回归 DCF-1.2A / DCF-1.1A～C / KAF-0～5 ✅

80 files / 525 tests 全量通过。

### 14. DCF-1.2C / DCF-1.3 / CGF-1.3 未超前 ✅

| 检查项 | 结果 |
| --- | --- |
| token delta / streaming | 未实现 ✅ |
| 断线重连 / cursor reset | 未实现 ✅ |
| DCF-1.3 | 未实现 ✅ |
| CGF-1.3 | 未实现 ✅ |

### 15. 用户现场演示 PENDING ✅

DEV LOG 明确标注 `USER DEMO PENDING`。独立 QA PASS 不替代用户接受。

## 结论

**P0=0 P1=0 P2=0 P3=0。DCF-1.2B INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING。**

- 80/525 全量 + 4/8 专项独立重跑 ✅
- Main 固定 channel 白名单 + Preload strict 校验 ✅
- selectedPath/selectionHandle 不入 Renderer/Preload ✅
- CSP `connect-src 'none'` + 无 fetch/WebSocket/EventSource ✅
- SubmitTurn 只提交意图 / Core 校验锁定 ✅
- E2E 产生 User + Scripted Assistant Message ✅
- 全量回归 + 无超前 DCF-1.2C/1.3/CGF-1.3 ✅

**DCF-1.2C GATED，等待用户明确接受。** 用户现场演示仍为 PENDING。
