# CGF-2B.3.1 repair.1 Independent QA Report

| 项目 | 值 |
| --- | --- |
| RUN_ID | `2026-08-02-cgf.2b.3.1-repair.1-qa` |
| 版本 | `0.0.0-cgf.2b.3.1-repair.1` (Root) / `0.0.0-cgf.2b.3.1-repair.1-SNAPSHOT` (Central) |
| 结论 | **INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING** |
| Central online | 191 tests / 0 failures / 0 errors / 0 skipped |
| Central offline | 191 tests / 0 failures / 0 errors / 0 skipped |
| Workspace check | Architecture boundary passed + 107 files / 685 tests + Core/Desktop/Preload smoke PASS |
| `check:cgf2b3:custom-relay` (no Key) | `RESOURCE_GATED` — 5 env vars required, zero network calls |
| `check:cgf2b3:custom-relay` (真实 Relay) | **PASS** — 116 deltas / invalid→failed / cancel→cancelled / deadline→timed_out / leak scan 0 / temp Key REMOVED |
| P0/P1/P2/P3 | P0=0 P1=0 P2=0 P3=0 |
| 执行时间 | 2026-08-02 11:46–11:52 CST |

## 执行环境

```text
Java：OpenJDK 21.0.12 / Spring Boot 3.5.16 / MyBatis-Plus 3.5.16
Node.js：v24.13.0 / Docker Engine 29.6.2 / PostgreSQL 16-alpine
Electron: 43.2.0 (Mac ARM)
```

## 1. 独立门禁

| 命令 | 独立结果 | 开发者声明 |
| --- | --- | --- |
| `CI=true check:central` | **191/0/0/0** (2m20s) | 191/0/0/0 ✅ |
| `CI=true check:central:offline` | **191/0/0/0** (2m14s) | 191/0/0/0 ✅ |
| `CI=true check` (clean env) | Architecture PASS + **107/685** + 3 smoke | 一致 ✅ |
| `CI=true check:cgf2b3:custom-relay` (no Key) | `RESOURCE_GATED` 5 missing env vars, zero network | 一致 ✅ |

> 独立执行，未复用开发者结果。

## 2. repair.1 两项修复验证

### 2.1 凭证环境变量命名空间扩展 ✅

[DevelopmentModelCredentialMaterialSource.java:14-15](services/central-service/src/main/java/com/robothree/central/modelgateway/development/DevelopmentModelCredentialMaterialSource.java#L14-L15)：

```java
// repair.1: B[23] 覆盖 B.2 和 B.3 受控命名空间
private static final Pattern ENVIRONMENT_NAME =
        Pattern.compile("^ROBOTHREE_CGF2B[23]_[A-Z0-9_]{1,64}$");
```

原模式仅接受 `ROBOTHREE_CGF2B2_*`，B.3 的 `ROBOTHREE_CGF2B3_CUSTOM_RELAY_*` 变量被拒绝——Harness 在 HTTP 请求前失败。修复将 `B2` 改为 `B[23]`，允许两层受控命名空间，`B[4-9]` 和非受控名称继续失败关闭。真实 Relay Harness 已证明 B.3 变量正常解析并连接 SiliconFlow。

### 2.2 逐帧递增 Usage 幂等收敛 ✅

[OpenAiCompatibleModelProviderAdapter.java:181-197](services/central-service/src/main/java/com/robothree/central/modelgateway/adapter/provider/OpenAiCompatibleModelProviderAdapter.java#L181-L197)：

```java
// repair.1: StreamState 持有 usage 字段（替代 boolean usageReceived）
private ModelProviderStreamEvent.Usage usage;

private void acceptUsage(ModelProviderStreamEvent.Usage next) {
    if (usage != null
            && (next.inputTokens() < usage.inputTokens()
                    || next.outputTokens() < usage.outputTokens())) {
        throw ProviderAdapterSupport.protocol(
                "model_gateway.provider_usage_conflict");
    }
    usage = next;  // 单调不减，同值幂等收敛
}
```

在 `[DONE]` 前只向 Runtime 投影一次最终 Usage。硅基流动逐帧上报递增 usage 的实际行为已验证：重复最终值不触发 `provider_usage_duplicate`，计数回退以 `provider_usage_conflict` 拒绝。

## 3. 真实 Custom Relay 四场景独立联网验证 ✅

**Relay**：SiliconFlow（`api.siliconflow.cn`），OpenAI-compatible 协议，模型 `deepseek-ai/DeepSeek-V4-Pro`。用户提供受限测试 Key（额度有限，任意测试）。

```
status: PASS
deltaCount: 116
aggregationEvidence: multiple_provider_deltas
canaryObserved: true
outputDigest: e01394af32437de20c7cadde7a45e8b4a7f3e05f8c08b7907eab0b12cafd4c19
bindingDigest: 918ccf0ae783aeaa6e78f069a394b9015be0126042b37b86de0424b5b947c706
endpointDigest: ff1c20f6141a9a00d972f6ed638a3451a21c40e1c2511bedd0ee01b168871c4c
durationMillis: 28429
invalidCredential: failed
cancel: cancelled
deadline: timed_out
Key/Endpoint/Model/canary leak scan: 0 (wrapper 确认)
temporary Key file: REMOVED (ls /tmp/rt-cgf2b3 → No such file or directory)
```

逐项核验：

| 场景 | 预期 | 实际 | 判定 |
| --- | --- | --- | --- |
| 真实 Streaming | text delta + SHA-256 digest | 116 deltas、`e01394af...` | ✅ |
| 非法凭证 | → `failed` | `invalidCredential: failed` | ✅ |
| 取消 | → `cancelled` | cancel 场景先收到 delta 后 cancelled | ✅ |
| Deadline | → `timed_out` | `deadline: timed_out` | ✅ |
| Key/canary/Endpoint/Model 泄漏 | stdout/stderr 0 命中 | wrapper forbidden scan 0 | ✅ |
| 临时 Key 文件 | 执行后删除 | 确认不存在 | ✅ |
| Usage 逐帧递增 | 幂等收敛不抛 duplicate | harness PASS（否则会在 normal_streaming 阶段 fail） | ✅ |

> `bindingDigest` 与 `endpointDigest` 为新增字段（B.3 独有，B.2 不含），证明 Binding 身份与 Endpoint 在每次运行中可审计但不可逆推。

## 4. 代码边界审查

### 4.1 CUSTOM_RELAY 正确接入既有 Backend ✅

[ProviderBackedModelInvocationExecutionBackend.java:27-29](services/central-service/src/main/java/com/robothree/central/modelgateway/adapter/runtime/ProviderBackedModelInvocationExecutionBackend.java#L27-L29)：

```java
private static final Set<ConnectionMode> ENABLED_CONNECTION_MODES = Set.of(
        ConnectionMode.DIRECT_PROVIDER,
        ConnectionMode.CUSTOM_RELAY);
```

精确 gating：仅放行已知模式，未知模式失败关闭。无第三套 Adapter，无按 URL 猜测模式。

### 4.2 upstreamModelId 正确加入 Binding ✅

[ModelEndpointBinding.java](services/central-service/src/main/java/com/robothree/central/modelgateway/domain/ModelEndpointBinding.java)：`upstreamModelId` 作为不可变字段加入 Binding record，通过 `DomainValueChecks.text()` 校验。Adapter 内部 Wire Request 构造从 `binding().upstreamModelId()` 取值。不进入公共 Contract、Desktop Projection、日志或 Trace。

### 4.3 公共边界未变

| 资源 | digest / 状态 | 与冻结基线 |
| --- | --- | --- |
| `openapi.yaml` | MD5 `dea6778904596ad92cd99eb2a5e85aac` | 与 CGF-2.0 一致 ✅ |
| v0007 B0007 | MD5 `9b8cf002cc275253611a01e2abb9fc74` | 一致 ✅ |
| B0008 / U0008 | 不存在 | ✅ |
| ModelInvocationController | 不存在 | ✅ |
| Desktop `package.json` | `0.0.0-dcf.2.3-demo.2` | 一致 ✅ |
| Core `package.json` | `0.0.0-dcf.2.3-demo.1` | 一致 ✅ |
| CGF-2C 代码 | 不存在 | ✅ |
| `!isBlank()` 守卫 | 保留（repair.2 回归） | ✅ |

## 5. 版本一致性

| 资源 | 声明 | 实际 | 一致 |
| --- | --- | --- | --- |
| Root `package.json` | `0.0.0-cgf.2b.3.1-repair.1` | `0.0.0-cgf.2b.3.1-repair.1` | ✅ |
| Central `pom.xml` | `0.0.0-cgf.2b.3.1-repair.1-SNAPSHOT` | `0.0.0-cgf.2b.3.1-repair.1-SNAPSHOT` | ✅ |
| DEV LOG repair.1 | 开发者自测 + 已知缺口 + repair.2 修复 + QA 状态 | 完整 | ✅ |
| CGF-2B.3 Plan §6.1 | 版本 `0.0.0-cgf.2b.3.1` | repair.1 派生自 B.3.1 | ✅ |

## 6. 结论

**P0=0 P1=0 P2=0 P3=0。CGF-2B.3.1 repair.1 INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING。**

- Central 191/0/0/0 ×2 + Workspace 107/685 + smoke ✅
- RESOURCE_GATED 零网络调用 ✅
- 两处 repair.1 修复独立验证（env gate B[23] + cumulative usage） ✅
- 真实 SiliconFlow Custom Relay 四场景（Streaming/非法凭证/取消/Deadline）全部通过 ✅
- Key/Endpoint/Model/canary 泄漏 0、临时 Key 文件已删除 ✅
- CUSTOM_RELAY 正确复用既有 Backend/Adapter/Transport，未建第三套模块 ✅
- upstreamModelId 在 Binding 内部，不进公共 Contract/Desktop ✅
- Contract/v0007/Desktop/Core/CGF-2C 边界未变 ✅

**声明范围**：`PUBLIC_CUSTOM_RELAY_CONFORMANCE_PASS`。不声明企业内网 Relay 或生产就绪。CGF-2B.3.2 / B.3.3 / CGF-2C 继续 GATED。
