# CGF-0 Independent QA Report

| 项目 | 值 |
| --- | --- |
| 版本 | `0.0.0-cgf.0.1-SNAPSHOT` |
| 结论 | **PASS** |
| JDK | OpenJDK 21.0.11 (Temurin) |
| 两轮 | Round 1 `mvnw verify` PASS / Round 2 `mvnw -o verify` PASS |
| P0 | 0 |
| P1 | 0 |
| P2 | 0 |
| P3 | 0 |

## 执行结果

| 轮次 | 命令 | 结果 |
| --- | --- | --- |
| Round 1 | `JAVA_HOME=<JDK21> ./mvnw verify` | BUILD SUCCESS, 5 tests / 0 failures |
| Round 2 | `JAVA_HOME=<JDK21> ./mvnw -o verify` | BUILD SUCCESS, 5 tests / 0 failures |

## 12 项 QA 范围

| # | 检查项 | 结果 | 证据 |
| --- | --- | --- | --- |
| 1 | Maven Enforcer Java 21 / Maven 3.9.x | ✅ | `RequireJavaVersion passed`, `RequireMavenVersion passed` |
| 2 | Maven Wrapper 可复跑 | ✅ | Round 1 online + Round 2 offline both BUILD SUCCESS |
| 3 | 模块化单体包边界 | ✅ | pom 模块 + 目录骨架：bootstrap/authentication/compatibility/configuration/credentials/model-gateway/tool-gateway/audit/persistence |
| 4 | readiness/compatibility fixtureOnly | ✅ | `FoundationFixtureControllerTest` 验证 fixtureOnly + `X-RoboThree-Fixture: true` |
| 5 | Cache-Control: no-store | ✅ | Fixture controller 返回 `no-store` |
| 6 | 默认 127.0.0.1 随机端口 | ✅ | `FoundationApplicationSmokeTest` 真实 Spring Boot HTTP server 随机端口 |
| 7 | 真实 HTTP server 启动 | ✅ | Spring Boot 启动成功，随机端口绑定 |
| 8 | Java/TS 共享 Fixture，不共享源码 DTO | ✅ | TS 在 `packages/contracts`，Java 独立 `src/test` |
| 9 | Fake Secret Store/Model/Tool 不含真实凭证 | ✅ | `FoundationFakesTest` 确认 |
| 10 | 无正式业务 DTO/Migration/Activation | ✅ | 无 JPA/JDBC 依赖，无 migration |
| 11 | 无 JPA/JDBC/Security/MCP/Provider SDK | ✅ | pom.xml 依赖门禁确认 |
| 12 | 可执行 Jar | ✅ | `spring-boot:repackage` 生成 `central-service-0.0.0-cgf.0.1-SNAPSHOT.jar` |

## Java 测试明细

| 测试类 | 测试数 | 覆盖 |
| --- | --- | --- |
| FoundationApplicationSmokeTest | 1 | 真实 Spring Boot HTTP server 随机端口启动 |
| FoundationFixtureControllerTest | 2 | readiness + compatibility endpoint |
| FoundationFixtureConformanceTest | 1 | TS/Java fixture 一致性 |
| FoundationFakesTest | 1 | Fake 组件不含真实凭证 |
| **合计** | **5** | **0 failures** |

## 已知非阻断提示

- Mockito inline-mock-maker 未来 JDK 兼容警告（非 CGF-0 范围）
- JDK 来自 `/private/tmp`，工具链工程化在 Java Toolchain 收口批次解决

## 证据路径勘误

原表第 8 项“TS 在 `packages/contracts`”为路径描述错误。实际 TypeScript
Conformance 测试是 `tests/e2e/cgf-foundation-fixture.test.ts`，共享 JSON
Fixture 位于
`services/central-service/src/test/resources/conformance/cgf-foundation.fixture.json`。
公共 `packages/contracts` 没有吸收该非正式 Fixture。此勘误不改变测试事实和
PASS 结论。
