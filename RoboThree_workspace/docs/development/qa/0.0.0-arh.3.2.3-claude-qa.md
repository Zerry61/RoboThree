# 0.0.0-arh.3.2.3 — Claude Code 独立 QA 报告

## 基本信息

| 属性 | 内容 |
|---|---|
| RUN_ID | `2026-08-14-2350-version-0.0.0-arh.3.2.3` |
| 验收对象 | ARH-3.2.3：Provider Cache Projection Closure（双协议 wire projection） |
| 日期 | 2026-08-14 |
| 验收者 | Claude Code（独立 QA） |
| 环境 | Node v24.13.0 / pnpm / Java 21 (Central) |

---

## 一、门禁复跑结果

| # | 门禁 | 结果 |
|---|---|---|
| 1 | `harness:arh3.2.3` | **PASS** 10 classes / 93 tests，C8-C10 + 双协议，providerProjectionEnabled=true，default=false，realProviderProductionReadyClaimed=false |
| 2 | `pnpm run check` | **PASS** 163 files / 1132 tests + 3 smoke |
| 3 | `check:central` (online) | 首次 2 个偶发时序失败（见 P3-1），**单独复跑均 PASS** |
| 4 | `check:central:offline` | **PASS** 297/0/0/0 |

---

## 二、重点核查

| # | 核查项 | 结论 |
|---|---|---|
| 1 | immutable Plan → typed Projection 闭环 | ✅ |
| 2 | Anthropic cache_control marker 仅最后 static block | ✅ `SYSTEM_LAST_STATIC` 目标，`sortedTools()` canonical 同源 |
| 3 | OpenAI automatic-observed / explicit key 三态分离 | ✅ explicitKey 才发 `prompt_cache_key`，automatic 零字段 |
| 4 | static digest 与 wire body 共用 canonical material | ✅ |
| 5 | Provider Usage 是缓存命中唯一事实 | ✅ harness `providerProjectionEnabled=true` 但 `default=false`、`realProviderProductionReadyClaimed=false` |
| 6 | C8～C10 + deterministic reject + cancel/deadline | ✅ harness 输出 `namedCrashWindows: ["C8","C9","C10a","C10b","C10c"]` |
| 7 | 敏感输出 0 | ✅ `sensitiveOutputMatchCount: 0` |
| 8 | 公共 Contract/v0009/migration/Kernel/Desktop/依赖未改 | ✅ |

---

## 三、发现

### P3-1：Central online 完整套件偶发 2 个时序失败（非 ARH-3.2.3 回归）

**现象**：完整 `check:central` online 首次运行，2 个老测试偶发失败：
- `Cgf2a3DualNodeModelRecoveryIntegrationTest` — "backend did not enter the blocking execution"（AssertionFailedError）
- `PostgreSqlMyBatisPersistenceIntegrationTest` — `ck_model_invocation_lease_time` 约束违反

**判断**：均为**既有老测试**（CGF-2A.3 双节点恢复 / 持久化），非 ARH-3.2.3 新增。单独复跑 Cgf2a3 2/2 PASS、PostgreSqlMyBatis 11/11 PASS；Central offline 完整套件 297/0/0/0 PASS——确认为 Testcontainers PostgreSQL + 双节点 lease 时序竞争导致的**偶发**，非稳定回归。

**建议**：后续为这两个测试增加重试/隔离（非本批阻塞项）。

### P0 = 0，P1 = 0，P2 = 0，P3 = 1

---

## 四、结论

```text
INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING
```

ARH-3.2.3 的 41 项边界、双协议 wire projection、C8-C10 恢复、Usage durable winner、敏感扫描 0、资源归零全部通过。Central online 首次 2 个偶发时序失败经单独复跑确认非回归。

**ARH-3.2.3 可进入用户接受流程，ARH-3.2 系列整体可收口。ARH-3.3 继续 GATED。**

— Claude Code（独立 QA，只读）
