# 0.0.0-arh.3.3.2-preflight-repair.2 — Claude Code 独立 QA 报告

## 基本信息

| 属性 | 内容 |
|---|---|
| RUN_ID | `2026-08-15-1400-version-0.0.0-arh.3.3.2-preflight-repair.2` |
| 验收对象 | ARH-3.3.2 前置修复：Durable Usage Reconciliation（status-first terminal） |
| 日期 | 2026-08-15 |
| 验收者 | Claude Code（独立 QA） |
| 环境 | Node v24.13.0 / pnpm / Java 21 (Central) |
| 开发版本 | Root/Core `0.0.0-arh.3.3.2-preflight-repair.2`；Central 保持 `0.0.0-arh.3.3.1-SNAPSHOT` |

---

## 一、门禁复跑结果（串行独立执行）

| # | 门禁 | 结果 |
|---|---|---|
| 1 | 专项（durable-enterprise-model-provider） | **PASS** 27 tests |
| 2 | `pnpm run check` | **PASS** 164 files / 1155 tests + 3 smoke |
| 3 | `check:central` (online) | **PASS** 299/0/0/0 |
| 4 | `check:central:offline` | **PASS** 299/0/0/0 |

---

## 二、重点核查

| # | 核查项 | 结论 |
|---|---|---|
| 1 | Assistant/Compaction status-first terminal reconciliation | ✅ L175-190 先 `assertStatusIdentity` 再 `#reconcileAssistantTerminalFacts` |
| 2 | 从持久 cursor 补偿 durable Usage/terminal facts | ✅ reconcile 补齐 cursor 之后错过的 durable facts |
| 3 | usage_recorded 保持 Projection-before-cursor | ✅ L234-256 先 `#recordUsageProjection` 再 `#recordProgress` |
| 4 | 不重放 ephemeral output | ✅ ephemeral 事件（started/text_delta/tool_call）只在 live stream 处理，不在 reconcile 重放 |
| 5 | completed 返回 model_stream_resume_unavailable，不伪造 Assistant Message/Summary | ✅ L184-185 抛 `ModelStreamResumeUnavailableError(outputStartedAt !== undefined)` |
| 6 | 非终态且已开始输出 fail-closed，不抢占活跃执行 | ✅ L191 `if (link.outputStartedAt !== undefined) throw` |
| 7 | 禁止范围未改 | ✅ 仅 `durable-enterprise-model-provider.ts` 生产代码；Contract/Schema/migration/Kernel/Desktop/Central/依赖/lockfile 未改 |

---

## 三、发现

### P0 = 0，P1 = 0，P2 = 0，P3 = 0

---

## 四、结论

```text
INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING
```

本修复正确实现 status-first terminal reconciliation：从持久 cursor 补偿 durable Usage/terminal facts、不重放 ephemeral output、completed 返回 resume_unavailable 不伪造消息、非终态已输出 fail-closed 不抢占活跃执行。禁止范围未改。四项门禁独立复跑通过。

**本修复可进入用户接受流程；ARH-3.3.2 主开发保持 PAUSED，ARH-3.3.3 继续 GATED。**

— Claude Code（独立 QA，只读）
