# DCF-1.2A Independent QA Report

| 项目 | 值 |
| --- | --- |
| RUN_ID | `2026-07-26-dcf.1.2a-qa` |
| 版本 | `0.0.0-dcf.1.2a` |
| 结论 | **INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING** |
| Node 全量 | 77 files / 518 tests PASS |
| 执行时间 | 2026-07-26 18:07 |
| P0/P1/P2/P3 | 0 |

## 执行环境

```text
Node.js：v24.13.0 (nvm)
pnpm：11.11.0
macOS：Darwin 24.4.0
```

## 执行结果

| 命令 | 结果 |
| --- | --- |
| `CI=true pnpm run check` | 77 files / **518 tests PASS** |

Architecture boundary checks：PASS。Core smoke：ready。Desktop smoke：fixtureOnly=true。

## 主要变更

- `DesktopApplicationFacade`：统一业务入口
- `CorePrivateHttpServer`：仅绑定 `127.0.0.1` 随机端口
- `CorePrivateSupervisor`：正式 Electron Main 启动路径（替换 DCF-0 Fixture）
- `CorePrivateClient`：Main 侧类型化 HTTP/SSE Client
- `DurableAgentLoopStarter`：绑定现有 Agent Loop
- `EphemeralWorkspaceSelectionStore`：TTL ≤ 30s selectionHandle

## 20 项独立 QA 逐条验证

### 1. Node 24.13.0 pnpm run check 独立重跑 ✅

77 files / 518 tests PASS。

### 2. Desktop Local additive Contract .strict() + Fixture 失败关闭 ✅

`packages/contracts/src/desktop-local/v1alpha1/` 所有新增 Query/Event/Error Schema 均使用 `.strict()`。非法 Fixture 失败关闭。

### 3. Headless 与 HTTP/SSE 均委托同一 DesktopApplicationFacade ✅

`HeadlessDesktopRuntime` 仅委托 `DesktopApplicationFacade`，不持有第二套业务编排。相同 Agent Query 产生相同 Projection。

### 4. 正式 Electron 启动路径不引用 DCF-0 Fixture ✅

`CorePrivateSupervisor`（`fixtureOnly: false`）为生产路径。DCF-0 `CoreHarnessSupervisor`（`fixtureOnly: true`）保留为 smoke/test。

### 5. Core 只绑定 127.0.0.1 随机端口，Host/Origin/Bearer 缺失或错误失败关闭 ✅

`server.listen(0, "127.0.0.1")`。Host 校验 `127.0.0.1:<port>`。Origin/Bearer 缺失 → typed error。

### 6. Token/端口/realpath/selectionHandle 不进入 Renderer/Preload API ✅

`RoboThreeDesktopApiV1Alpha1` 只冻结 Renderer-safe 类型签名。端口、Token、realpath、selectionHandle 均未暴露。

### 7. selectionHandle TTL ≤ 30s、单次消费、client/correlation 绑定 ✅

`MAX_SELECTION_TTL_MS = 30_000`。`EphemeralWorkspaceSelectionStore` 单次使用后立即失效。重启即失效。

### 8. SQLite/WAL 扫描，selectionHandle 未持久化 ✅

`grep -rn "selectionHandle" services/core/src/adapters/sqlite` → 0 命中。handle 不进入 SQLite 或 WAL。

### 9. 请求/响应/SSE frame 上限 + redirect/manual + 错误脱敏 ✅

HTTP body 1 MiB、response 2 MiB、SSE frame 有界。Redirect fail-closed。错误 typed 无内部细节。

### 10. 同一 runtime 只允许一个认证 SSE ✅

第二个并发 SSE 连接被拒绝。收到 `submit_turn_status_changed` durable event。

### 11. DurableAgentLoopStarter 校验 Task/Selection/Agent/Model/userMessageId ✅

`bundle.runtimeSelection.runtimeSelectionId !== input.runtimeSelectionId`、`bundle.binding.userMessageId !== input.userMessageId` → 不匹配则拒绝启动。

### 12. 同 SubmitTurn 顺序/并发/close/reopen 不重复写 Message ✅

重放相同 SubmitTurn → 幂等。冲突 digest → 失败关闭。SQLite close/reopen 后 Receipt + Assistant Message 可恢复。

### 13. 冲突 command/clientTurn/digest 或缺失 locked Registry 失败关闭 ✅

SubmitTurnCoordinator 已有完整 typed conflict + Registry 缺失 retryable 判断。

### 14. 最终 Assistant Message 持久关联 taskId，重启后由 Snapshot 收敛 ✅

`message.envelope.messageId === input.userMessageId` 校验。Snapshot 收敛复用 KAF-5/DCF-1.1 已有语义。

### 15. Scripted Model 不执行 Tool，真实 Model/Credential 未超前 ✅

只用 Scripted/Fake Model。真实 Model/Credential 未实现。

### 16. Facade/Event/Projection 不泄漏内部对象 ✅

不暴露 Effect、Receipt、Outbox、Checkpoint、CapabilityBinding、AdapterDescriptor、Runtime Handle、Credential。

### 17. Phoenix/WebSocket/Socket.IO/SockJS 不存在 ✅

全仓扫描 → 0 命中。

### 18. 正式 Core 子进程 compatibility/业务 Query/有限重启/优雅停止 ✅

`CorePrivateSupervisor` 完成 compatibility handshake、业务 Query、优雅停止。

### 19. DCF-1.1A～1.1C + KAF-0～KAF-5 全量回归 ✅

77 files / 518 tests 全量通过。无回归。

### 20. DCF-1.2B/C、DCF-1.3、CGF-1.3 未超前 ✅

| 检查项 | 结果 |
| --- | --- |
| DCF-1.2B Preload 业务实现 | 仅冻结类型签名，未实现 ✅ |
| DCF-1.2C streaming/重连 | 未实现 ✅ |
| DCF-1.3 | 未实现 ✅ |
| CGF-1.3 | 未实现 ✅ |

## 结论

**P0=0 P1=0 P2=0 P3=0。DCF-1.2A INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING。**

- 77/518 全量独立重跑 ✅
- Core 仅 127.0.0.1 + 随机端口 + Bearer Token ✅
- Headless/HTTP 同一 Facade ✅
- 正式路径不引用 Fixture ✅
- selectionHandle TTL ≤ 30s + 不持久化 + 不入 API ✅
- DurableAgentLoopStarter 校验 bundle 身份 ✅
- 无 Phoenix/WebSocket ✅
- 无超前 DCF-1.2B/C/1.3/CGF-1.3 ✅

**DCF-1.2B GATED，等待用户明确接受后解锁。**
