# DCF-1.1C Independent QA Report

| 项目 | 值 |
| --- | --- |
| RUN_ID | `2026-07-26-dcf.1.1c-qa` |
| 版本 | `0.0.0-dcf.1.1c` |
| 结论 | **PASS / CLOSED — USER ACCEPTED** |
| Node 全量 | 74 files / 512 tests PASS |
| SubmitTurn 专项 | 3 files / 17 tests PASS |
| 执行时间 | 2026-07-26 16:28～16:29 |
| P0/P1/P2/P3 | 0 |

## 执行环境

```text
Node.js：v24.13.0 (nvm)
pnpm：11.11.0
macOS：Darwin 24.4.0
```

## 执行结果

| 命令 | 结果 |
| --- | --- |
| `CI=true pnpm run check` | 74 files / **512 tests PASS** |
| `vitest run`（3 files SubmitTurn 专项） | 3 files / **17 tests PASS** |

Architecture boundary checks：PASS。Core smoke：ready。Desktop smoke：fixtureOnly=true。

## 16 项独立 QA 逐条验证

### 1. Node 24 pnpm run check 独立重跑 ✅

74 files / 512 tests PASS。未引用开发者自测结论，全部独立执行。

### 2. SubmitTurn Contract `.strict()` + 协调记录无正文/凭证/Handle ✅

- `SubmitTurnRecord`：只保存 command/client turn、精确 Agent/Registry/Prompt/Selection/Lock 引用与内部对象 ID ✅
- 不保存用户正文（正文只在 Conversation）、TaskState、Runtime Handle、PID、Credential ✅
- Contract Schema `.strict()` 拒绝未知字段 ✅

### 3. commandId / clientTurnId / requestDigest 同一性与并发单写者 ✅

`SubmitTurnCoordinator`：
- `commandId` → mailbox 串行化 ✅
- `clientTurnId` 已存在但 `commandId` 不同 → typed conflict ✅
- `requestDigest` 不匹配 → typed conflict ✅
- 相同 commandId/digest → 幂等回放，返回稳定公开 ID ✅

### 4. 用户 Message 只追加一次，协调记录不复制正文 ✅

- `message_appended` 状态在 accepted record 后触发 ✅
- 正文通过 Conversation `PreparedConversationMessage` 锁定，不进入协调记录 ✅
- Message ID 保存在协调记录作为引用（`internalUserMessageId`），不复制正文 ✅

### 5. Task + Checkpoint + Selection + Locks + userMessageId binding 原子提交 ✅

`commitSubmitTurnTaskBundle`：在一个 Task 领域事务内原子写入：
- Task/Checkpoint ✅
- Model/Tool CapabilityLocks ✅
- TaskRuntimeSelection ✅
- userMessageId binding ✅

### 6. 原子 bundle 中任一引用冲突时整笔回滚 ✅

Memory/SQLite `commitSubmitTurnTaskBundle`：同 digest 幂等、漂移 conflict、引用缺失 → 整笔回滚 ✅

### 7. Memory/SQLite SubmitTurn Persistence 共用语义 ✅

`submit-turn-persistence.conformance.test.ts`：双 variant 运行同一矩阵。阶段转换（accepted → message_appended → task_committed → completed / failed_terminal）、Receipt、Delivery、Loop start、recovery candidates ✅

### 8. 7 个命名故障点 SQLite close/reopen ✅

`submit-turn-coordinator.integration.test.ts` 覆盖 7 个命名故障点（accepted 后、message_appended 后、task bundle 后、completed 前/后、delivery 前、Loop start 前），每个点都执行 SQLite close/reopen 并验证恢复 ✅

### 9. completed Receipt 与 Desktop Delivery 同事务、cursor 单调且不重复 ✅

- Terminal Receipt 与 Delivery 在同一事务提交 ✅
- `desktopDeliverySequence` 单调递增 ✅
- 重放不产生重复 delivery ✅

### 10. Receipt 完成后 Loop 启动失败仍可恢复且不撤回 accepted ✅

- `AgentLoopStarter` 窄 Port + Fake 幂等 Adapter ✅
- Loop 启动临时失败 → 不撤回 accepted Receipt ✅
- `SubmitTurnRecoveryCoordinator` 扫描恢复 ✅

### 11. locked Registry revision 缺失保持 retryable，不写 failed_terminal ✅

`RuntimeSelectionContextProvider`：按精确 Registry revision 提供历史上下文。Registry 历史快照不可用 → `retryable=true`，不伪造 `failed_terminal` ✅

### 12. 确定性 pre-Task 失败才允许写 failed_terminal / rejected delivery ✅

`failed_terminal` 仅用于 pre-Task 确定性选择/引用失败（Agent/Model 不存在、Selection 非法）。SQLite 错误、Loop 启动失败保持可恢复 ✅

### 13. migration 9 前向升级 / integrity / foreign_key / preflight ✅

Migration 9（`dcf_1_1c_submit_turn_coordination`）新增：
- `prepared_conversation_messages`（Message intent）
- `task_submit_turn_bindings`（Task↔SubmitTurn binding）
- `submit_turn_records`（协调记录）
- `submit_turn_receipts`（Receipt）
- `desktop_delivery_records`（Desktop delivery）

前向升级一次成功，integrity_check + foreign_key_check 通过 ✅

### 14. Conversation→Task 外键移除后仍由协调/bundle 关闭悬空相关性 ✅

旧 `conversation_messages.task_id → task_heads` 跨领域外键被前向移除。Message 先于 Task 提交，相关性一致性由协调状态机 + Task bundle `userMessageId` 校验承担 ✅

### 15. Kernel 不导入 SubmitTurn / CGF-1.3 / Central 未修改 ✅

| 检查项 | 结果 |
| --- | --- |
| Kernel 导入 SubmitTurn/Application/SQLite | **0 命中** ✅ |
| CGF-1.3 | 未修改 ✅ |
| Central | `0.0.0-cgf.1.2c-SNAPSHOT` 不变 ✅ |
| Electron UI / Preload / Bridge | 未提前实现 ✅ |

### 16. DCF-1.1A/B + KAF-0～5 全量回归 ✅

- DCF-1.1A Workspace/Session/Conversation 基础：回归通过 ✅
- DCF-1.1B Agent/Model Selection + Capability Lock：回归通过 ✅
- KAF-0～5 Task/Event/Persistence/Conversation/Compaction：回归通过 ✅
- Core smoke：ready ✅
- Desktop smoke：fixtureOnly=true ✅

## 结论

**P0=0 P1=0 P2=0 P3=0。DCF-1.1C PASS / CLOSED — USER ACCEPTED。**

16 项独立 QA 全部覆盖且验证通过：
- 74/512 全量 + 3/17 专项独立重跑 ✅
- Contract strict + 协调记录无正文/凭证/Handle ✅
- commandId/clientTurnId/digest 同一性 + 并发单写者 ✅
- Message 只追加一次，协调记录不复制正文 ✅
- Task bundle 原子提交 ✅
- 引用冲突整笔回滚 ✅
- Memory/SQLite SubmitTurn Conformance ✅
- 7 命名故障点 SQLite close/reopen ✅
- completed Receipt + Delivery 同事务/cursor 单调 ✅
- Loop 启动失败可恢复不撤回 accepted ✅
- locked Registry 缺失 retryable ✅
- failed_terminal 仅 pre-Task 确定性失败 ✅
- migration 9 前向升级 ✅
- Conversation→Task FK 移除后协调关闭 ✅
- Kernel 隔离 + 无超前 CGF-1.3/Central ✅
- DCF-1.1A/B + KAF-0～5 全量回归 ✅

用户已正式接受本报告。DCF-1.1C `PASS/CLOSED`，DCF-1.1 阶段正式关闭；
CGF-1.3 继续 `GATED`，等待下一阶段方案确认和明确授权。

## 证据目录

`qa-reports/2026-07-26-dcf.1.1c-qa/evidence/`
