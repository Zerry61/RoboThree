# 0.0.0-arh.3.1 — Claude Code 独立 QA 报告

## 基本信息

| 属性 | 内容 |
|---|---|
| RUN_ID | `2026-08-13-2049-version-0.0.0-arh.3.1` |
| 验收对象 | ARH-3.1：Provider Usage Fact Accounting + Retry Dedupe |
| 日期 | 2026-08-13 |
| 验收者 | Claude Code（独立 QA） |
| 环境 | Node v24.13.0 / pnpm 11.11.0 / Java 21 (Central) |
| 开发版本 | Core `0.0.0-arh.3.1`；Central schema v0008 |

---

## 一、门禁复跑结果（串行独立执行）

| # | 门禁 | 结果 |
|---|---|---|
| 1 | `harness:arh3.1` | **PASS** 4 files / 24 tests，EXIT=0 |
| 2 | `pnpm run check` | **PASS** 162 files / 1099 tests + smoke |
| 3 | `check:central` (online) | **PASS** BUILD SUCCESS |
| 4 | `check:central:offline` | **PASS** BUILD SUCCESS |

---

## 二、13 项重点核查

| # | 核查项 | 结论 |
|---|---|---|
| 1 | v0008 Fresh / v0006 / v0007 / Legacy Bridge 升级路径 | ✅ `SchemaManifestLoader` 引 `postgresql-v0008.json`，`CentralSchemaPreflight` 验证 fresh + v0007_upgrade + legacy bridge |
| 2 | v0001~v0007 SQL/manifest/digest 未改写 | ✅ 旧 schema 文件未变，preflight 校验 v0007 升级历史完整 |
| 3 | ProviderUsageFact authority/attempt/fencing/digest 自校验 | ✅ record 含 `authorityInvocationId` + `providerAttemptKey`(digest) + `fencingEpoch>=1` + `usageDigest`(digest) + `reportingSemanticsRevision`(digest)，构造函数全量校验 |
| 4 | 同 attempt 同 digest 幂等，不同 digest conflict | ✅ `ProviderUsageFacts` 按 `(invocation_id, provider_attempt_key)` 唯一约束 |
| 5 | terminal winner Fact/Event/Outbox/terminal 原子提交及回滚 | ✅ 同 Central 事务提交，conformance 覆盖 commit 前回滚 |
| 6 | stale owner 只能 superseded_confirmed 不覆盖 winner | ✅ `AttemptDisposition` 枚举 `terminal_winner | superseded_confirmed`，不追加公共 Event |
| 7 | Anthropic/OpenAI breakdown 语义正确，缺字段不伪造 0 | ✅ `reportingSemanticsRevision` 按协议分公式，cache/reasoning 字段 optional（null 保留 unknown） |
| 8 | 主调用 + Compaction 调用 Core Projection 持久化/恢复/去重 | ✅ `in-memory/sqlite-provider-usage-projection-persistence` 双实现，eventId/digest 幂等 |
| 9 | Session Usage 只从 invocation projection 派生，无第二可改累计事实 | ✅ 派生读模型，不建独立累计事实 |
| 10 | local_personal 仅 Port/Fake，无真实 Provider/凭据/权威表 | ✅ `InMemoryLocalPersonalUsageAuthority` 仅 InMemory，无凭据/真实 Provider |
| 11 | Usage Fact/Projection/日志不包含 Prompt/正文/Endpoint/Credential/Token/路径 | ✅ 泄漏扫描零命中（`Long.toString` 仅用于 digest 计算 canonical string，非日志） |
| 12 | 公共 Contracts / Kernel reducer / Desktop 未修改 | ✅ |
| 13 | Prompt Cache / v1alpha2 cacheContext / ARH-3.2/3.3 未提前实现 | ✅ 生产代码零 `cacheContext`/`prompt_cache_key`/`cache_control` |

---

## 三、发现

### P0 = 0，P1 = 0，P2 = 0，P3 = 0

---

## 四、结论

```text
INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING
```

ARH-3.1 的 ProviderUsageFact 持久事实、attempt identity、fencing、digest 自校验、retry dedupe、terminal 原子提交、双协议 breakdown 语义、local_personal 边界全部正确。四项门禁独立复跑通过，无 P0/P1/P2/P3。

**ARH-3.1 可进入用户接受流程。ARH-3.2/3.3 继续 GATED。**

— Claude Code（独立 QA，只读）
