# 0.0.0-pdt.1 — Claude Code 独立 QA 报告

## 基本信息

| 属性 | 内容 |
| --- | --- |
| RUN_ID | `2026-08-08-2256-version-0.0.0-pdt.1` |
| 验收对象 | PDT-1：PDF Table Extract Document Worker Private Foundation |
| 日期 | 2026-08-08 |
| 验收者 | Claude Code（独立 QA，只读） |
| 触发方式 | 用户显式指令（"PDT-1 已完成，IMPLEMENTED / SELF-TEST PASS / INDEPENDENT QA PENDING"） |
| 环境 | Node v24.13.0 / pnpm 11.11.0 / vitest 4.1.10 |
| 开发版本 | Document Worker `0.0.0-dwo.1`（未升版 — 见 P3 观察）；Core/Desktop/Contracts 保持原版本 |
| 证据目录 | `qa-reports/2026-08-08-2256-version-0.0.0-pdt.1/evidence/` |

治理说明：PDT-0 `PASS/CLOSED`（含 Revision 1）。本批是 PDT-0 复核意见的代码落地：typed error 严格对齐、no-text-layer fail-closed、Router private-protocol-only、`DOCUMENT_CAPABILITIES` 不包含 `extract_tables`（使用独立 `DOCUMENT_WORKER_PRIVATE_CAPABILITIES`）。

---

## 一、门禁复跑结果（全部独立执行，未采信自测）

| # | 门禁 | 结果 | 与自测声明对比 |
| --- | --- | --- | --- |
| 1 | PDT-1 focused（pdf-extract-tables + document-capability-router） | **PASS** 2 files / 11 tests | 一致 |
| 2 | Document Worker 全量 | **PASS** 23 files / 181 tests | 一致 |
| 3 | Core document-tool 回归（backend + context + registry） | **PASS** 3 files / 22 tests | 一致 |
| 4 | lint + architecture boundary | **PASS** | 一致 |
| 5 | `audit:dtp4` | **PASS** | 一致 |
| 6 | offline frozen install | **PASS**（Already up to date） | 一致 |
| 7 | 完整 `pnpm run check` | **PASS** 151 files / 1031 tests ＋ 三项 smoke | 一致 |

回归基线：MAR-1B 为 150 files / 1024 tests；本次 1031 tests（+7，全部为 PDT-1 表格提取用例）。无既有用例回归。

---

## 二、静态核查（PDT-1 Worker-private 基础，全部通过）

| 关注点 | 结论 | 证据 |
| --- | --- | --- |
| **未注册 Core Tool** | ✅ | `DOCUMENT_CAPABILITIES` 仍仅 3 个只读工具；`extract_tables` 在 `DOCUMENT_WORKER_PRIVATE_CAPABILITIES`（独立数组，仅 Worker 内部可见） |
| **private-protocol-only** | ✅ | Router L70-77：`extract_tables` 仅接受 v1alpha2 private protocol，public 调用 → `unsupported_feature` + `private_protocol_required` |
| **仅用 pdfjs getTextContent** | ✅ | `page.getTextContent({ disableNormalization: false })`，不引入 canvas/sharp/opencv/任何图像处理依赖 |
| **no-text-layer fail-closed** | ✅ | `totalTextItems === 0` → `unsupported_feature` + `pdf_table_no_text_layer`（L268-274） |
| **typed error 严格对齐 v1alpha2** | ✅ | `typedError` 函数仅接受 `invalid_format`/`encrypted`/`corrupt`/`limit_exceeded`/`unsupported_feature`/`internal_failure`——与 PDT-0 Revision 1 对齐要求一致（L757-768） |
| **算法正确拒绝误检** | ✅ | 聚类：y-tolerance 4pt 行分组 + x-tolerance 12pt 列分组 + 32pt vertical gap 段落分割；置信度评分 `coverage*0.45 + density*0.35 + consistency*0.2`；`minConfidence` 阈值（默认 0.6）拒绝低信心候选（L303-425） |
| **预算逐表强制** | ✅ | `enforceTableBudgets`：rows/cells/output bytes 超限抛出 `limit_exceeded`（L518-550）；文本项数上限 10000/page 与 50000/total |
| **依赖无新增** | ✅ | 仅既有 `pdfjs-dist@6.2.108` + `xlsx@0.20.3`；`pnpm-lock.yaml` 未改；`pdf_extract_text`/`xlsx_read`/`xlsx_write`/`docx_read` 全量回归通过 |
| **无越界** | ✅ | Core/Contracts/Desktop/Central 零 `extract_tables`/`pdfTable` 标识；`DW_DIAGNOSTIC`/`_dw` 零命中；根配置/lockfile 未改 |

---

## 三、发现与分级

### P0 = 0，P1 = 0，P2 = 0，P3 = 1

| # | 等级 | 问题 | 建议 |
| --- | --- | --- | --- |
| P3-1 | P3 | Document Worker `package.json` 版本仍为 `0.0.0-dwo.1`，未升版反映本批生产代码变更 | PDT-2 编码前同步升版 |

无环境阻塞（本次所有全量门禁在沙箱内直接通过）。

---

## 四、总体结论

```text
结论：PASS（P0 = 0 / P1 = 0 / P2 = 0 / P3 = 1）
```

- PDT-1 的 Worker-private 表格提取基础正确实现了 PDT-0 冻结的全部设计：仅用 pdfjs text layer 几何聚类、confidence 评分优先拒绝误检（默认 minConfidence=0.6）、no-text-layer fail-closed、typed error 严格对齐 v1alpha2、Router private-protocol-only、`extract_tables` 未注册 Core Tool 且未写入 `DOCUMENT_CAPABILITIES`。
- 全部门禁独立复跑通过，无依赖越界，无环境阻塞。
- 1 项 P3 为版本号未升版，不阻塞。
- 模型/用户仍不可调用 `extract_tables`（PDT-2 Core Registry 前保持 disabled）。
- **PDT-1 可进入用户接受流程。**

## 五、建议的下一步

1. 用户接受 PDT-1 QA 后正式关闭 PDT-1。
2. 单独授权 PDT-2（Core Registry / Agent Exposure / model-visible schema）。
3. PDT-3、PDT-4、OCR/扫描件、XLSX 导出、人工校正 UI、bulk extraction 保持 GATED。

— Claude Code（独立 QA，只读，未修改任何生产代码）
