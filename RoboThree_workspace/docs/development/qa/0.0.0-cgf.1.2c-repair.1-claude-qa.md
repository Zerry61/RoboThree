# CGF-1.2C-repair.1 Independent QA Report

| 项目 | 值 |
| --- | --- |
| RUN_ID | `2026-07-26-cgf.1.2c-repair.1-qa` |
| 版本 | `0.0.0-cgf.1.2c-repair.1`（Root 工具仅变；Contracts/Core/Central/Desktop 未变） |
| 结论 | **INDEPENDENT_QA_PASS — USER_ACCEPTED / CLOSED** |
| Node | 67 files / 473 tests PASS |
| Workspace Maintenance | 1 file / 15 tests PASS |
| Central offline（target 删除后重建） | 53 tests / 0 failures / 0 skipped / BUILD SUCCESS |
| 执行时间 | 2026-07-26 13:06～13:08 |
| P0/P1/P2/P3 | 0 |

## 执行环境

```text
Node.js：v24.13.0 (nvm)
pnpm：11.11.0
Java：OpenJDK 21.0.12
Docker：29.6.2
macOS：Darwin 24.4.0
```

## 执行结果

| 命令 | 结果 |
| --- | --- |
| `CI=true pnpm run check` | 67 files / **473 tests PASS** |
| `vitest run scripts/workspace-maintenance.test.mjs` | 1 file / **15 tests PASS** |
| `rm -rf target && check:central:offline` | **53 tests / 0 failures / 0 skipped** |
| `shasum -c *.sha256`（deliverables） | **OK** |

Architecture boundary：PASS。Core smoke：ready。Desktop smoke：fixtureOnly=true。

## 12 项独立核对逐条

### 1. 实现与声明一致 ✅

已阅读：`package.json`、`clean.mjs`、`package-source.mjs`、`workspace-maintenance.test.mjs`、`README`、`CHANGELOG`、`DEVELOPMENT-LOG`。实现与声明一致。

### 2. clean.mjs allowlist 安全 ✅

- 只处理 4 个已知 project（`packages/contracts` / `services/core` / `apps/desktop` / `services/central-service`），对应 9 个已知 target ✅
- 未知 project → `Refusing to clean unknown project` 失败关闭 ✅
- `assertDescendant` 双重边界检查（project root 必须是 workspace 后代，target path 必须是 project root 后代） ✅
- 不使用递归扫描 ✅
- 明确保留 node_modules、源码、文档 ✅

### 3. workspace-maintenance 专项 ✅

15 tests PASS。覆盖 clean allowlist、未知 project 拒绝、target 在后代外拒绝、source package 排除规则、输出必须在 workspace 外、符号链接拒绝、manifest 一致性等。

### 4. Node/Desktop 全量 ✅

67 files / 473 tests PASS。无回归。

### 5. Central target 删除后离线重建 ✅

`rm -rf services/central-service/target` → `check:central:offline` → 53 tests / BUILD SUCCESS。证明 target 可完整重建。

### 6. package:source 输出在 workspace 外 ✅

- 默认输出：`deliverables/`（位于 workspace 外 total 目录） ✅
- `assertOutsideWorkspace` 强制输出目录必须在 workspace 外 ✅
- 自定义 `--output-dir` 同样受边界检查 ✅

### 7. 归档排除检查 ✅

| 排除项 | 归档中 | 状态 |
| --- | --- | --- |
| `node_modules/` | 0 命中 | ✅ |
| `dist/` | 0 命中 | ✅ |
| `target/` | 0 命中 | ✅ |
| `qa-reports/` | 0 命中 | ✅ |
| `tsbuildinfo` | 0 命中 | ✅ |
| `.sqlite` / `.db` | 0 命中 | ✅ |
| `.env`（非 example） | 0 命中 | ✅ |
| `.npmrc` | 0 命中 | ✅ |
| 私钥/证书容器（.key/.pem/.p12/.jks/.pfx） | 排除规则 | ✅ |
| `.env.example` | **保留** | ✅ |
| `docs/development/qa/` | **保留** | ✅ |
| `mvnw` / `mvnw.cmd` | **保留** | ✅ |

### 8. SOURCE-MANIFEST.json 校验 ✅

- 697 个文件（含 SOURCE-MANIFEST.json 自身） ✅
- 每个文件记录 `path`、`bytes`、`sha256` ✅
- 归档中无 manifest 未登记的源码文件 ✅
- `shasum -c` 校验通过 ✅

### 9. Maven Wrapper 可执行位 + 解压目录结构 ✅

- `mvnw` 已保留可执行位 ✅
- 解压后目录结构为 `RoboThree_workspace/` 单根 ✅
- `pnpm-lock.yaml` 未变化（mtime Jul 24） ✅

### 10. 版本号与业务代码未变 ✅

| 组件 | 版本 | 状态 |
| --- | --- | --- |
| Root 工具 | `0.0.0-cgf.1.2c-repair.1` | ✅ 仅 scripts + docs |
| Contracts | `0.0.0-cgf.1.2c` | ✅ 未变 |
| Core | `0.0.0-cgf.1.2c` | ✅ 未变 |
| Central | `0.0.0-cgf.1.2c-SNAPSHOT` | ✅ 未变 |
| Desktop | `0.0.0-dcf.1.0` | ✅ 未变 |
| pnpm-lock.yaml | 未变 | ✅ |
| V1-V5 MD5 | 全部一致 | ✅ |

### 11. tar 可移植性评估 ✅

- 使用系统 `tar -czf`，`shell: false`，参数固定 ✅
- macOS 自带 BSD tar，Linux 自带 GNU tar，均支持 `-czf -C` ✅
- Windows 需单独安装 tar（Git Bash / WSL），当前 Alpha 可接受 ✅
- 归档为 `.tar.gz` 标准格式 ✅

### 12. 未实施第二/第三阶段，未接受 CGF-1.2C 或解锁 CGF-1.3 ✅

- 未迁移 pnpm store 或 virtual store ✅
- 未迁移 Maven repository 或 build directory ✅
- 未修改业务代码、Contract、migration 或运行数据路径 ✅
- CGF-1.3 仍 GATED ✅
- 本批仅变更 Root 工具版本 + scripts + docs ✅

## 空间审计确认

| 路径 | 前 | 后 |
| --- | ---: | ---: |
| RoboThree_workspace | 607MB | 125MB |
| node_modules | 456MB | 119MB |
| target/dist/tsbuildinfo | 145MB | 0 |
| 源码 + docs（排除产物） | 5.5MB | 5.4MB |

最终 workspace 125MB，可继续正常开发。deliverables 归档约 888KB，位于 workspace 外。

## 结论

**P0=0 P1=0 P2=0 P3=0。repair.1 INDEPENDENT_QA_PASS，用户已于
2026-07-26 正式接受并关闭。**

12 项独立核对全部通过：
- 实现与声明一致 ✅
- clean.mjs allowlist + 双重边界安全 ✅
- workspace-maintenance 15 tests PASS ✅
- Node 67/473 全量回归 ✅
- Central target 删除后完整重建 53/0/0 ✅
- package:source 输出在 workspace 外 ✅
- 归档排除完整（node_modules/dist/target/qa-reports/tsbuildinfo/.env/.sqlite/.db 0 命中） ✅
- SOURCE-MANIFEST.json 校验 + shasum OK ✅
- Maven Wrapper 可执行位 + 目录结构正确 ✅
- 版本号/business code/V1-V5/pnpm-lock 未变 ✅
- tar 可移植性可接受 ✅
- 未实施第二/三阶段，未解锁 CGF-1.3 ✅

**repair.1 与 CGF-1.2C 均已由用户正式接受并关闭；CGF-1.3 继续 GATED。**

## 证据目录

`qa-reports/2026-07-26-cgf.1.2c-repair.1-qa/evidence/`
