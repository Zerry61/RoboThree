# 0.0.0-apv.1a — Claude Code 独立 QA 报告

## 基本信息

| 属性 | 内容 |
| --- | --- |
| RUN_ID | `2026-08-05-1602-version-0.0.0-apv.1a` |
| 验收对象 | APV-1A：Desktop Artifact Panel（metadata-only） |
| 日期 | 2026-08-05 |
| 验收者 | Claude Code（独立 QA，只读） |
| 触发方式 | 用户显式指令（"APV-1A 已完成实现和自测，IMPLEMENTED / SELF-TEST PASS / INDEPENDENT QA PENDING"） |
| 环境 | Node v24.13.0 / pnpm 11.11.0 / vitest 4.1.10 |
| 开发版本 | Contracts / Core / Desktop `0.0.0-apv.1a`；Root 保持 `0.0.0-dtp.4`；Document Worker `0.0.0-dwe.2` |
| 证据目录 | `qa-reports/2026-08-05-1602-version-0.0.0-apv.1a/evidence/` |

治理说明：APV-1.0 已正式 `PASS/CLOSED`。APV-1A 是本系列**首个修改公共 Contracts 的批次**（additive），按 APV-0 计划 §9 硬阻塞判据（Desktop typed `loadTaskDetail` 必须承载 artifact metadata）执行。本报告重点核查该 additive 改动的边界。

---

## 一、门禁复跑结果（全部独立执行，未采信自测）

| # | 门禁 | 结果 | 与自测声明对比 |
| --- | --- | --- | --- |
| 1 | APV-1A focused（contracts + projection + task-projection + presentation + audit） | **PASS** 5 files / 29 tests | 一致 |
| 2 | Core 全量测试 | **PASS** 77 files / 572 tests | 一致（本次沙箱内直接通过） |
| 3 | Document Worker 全量 | **PASS** 22 files / 168 tests | 一致 |
| 4 | Desktop 全量测试 | **PASS** 20 files / 81 tests | 一致（本次沙箱内直接通过） |
| 5 | Contracts 全量（补充） | **PASS** 14 files / 79 tests | 额外验证 |
| 6 | lint + architecture boundary | **PASS** | 一致 |
| 7 | `audit:dtp4` | **PASS**（Contracts/Core/Desktop `0.0.0-apv.1a` 版本期望） | 一致 |
| 8 | offline frozen install | **PASS**（Already up to date） | 一致（本机 store 完整，未复现缺 tarball） |
| 9 | 完整 `pnpm run check` | **PASS** 147 files / 969 tests ＋ 三项 smoke | 一致 |

回归基线：APV-1.0 为 146 files / 963 tests；本次 969 tests（+6）。无既有用例回归（Contracts/Core/Desktop/DW 全量均过）。

---

## 二、静态核查（核心关注点全部通过）

| 关注点 | 结论 | 证据 |
| --- | --- | --- |
| **Contracts additive 且 metadata-only** | ✅ | `ArtifactProjectionSchema` 仅含 artifactId/taskId/sourceKind/sourceId/sourceDigest/displayName/kind/mediaType/relativePath/byteSize/createdAt/previewState/metadata；**无 `schemaVersion`、无 `sessionId` 字段**；metadata 4096 B 有界、relativePath 守卫（`packages/contracts/src/desktop-local/v1alpha1/task.ts:178-238`） |
| **TaskDetailProjection.artifacts additive + 默认 []** | ✅ | `artifacts: z.array(ArtifactProjectionSchema).max(256).default([])`；`.strict()` 下新增字段声明即可；superRefine 校验 artifact.taskId 与 summary.taskId 一致（L240-276） |
| **Contracts 层泄漏边界负向证明** | ✅ | `dcf-2-0-contracts.test.ts` 新增负向测试：`ArtifactProjectionSchema` 对含 `workspaceRoot`/`workbook`/`sessionId`/`schemaVersion` 的 payload `safeParse=false`（strict 拒绝） |
| **Core 剥离内部字段** | ✅ | `loadTaskDetail` 复用 `projectArtifactIndexForTask`；`projectArtifactForDesktop` 白名单映射 13 字段，明确剥离内部 `schemaVersion` 与 `sessionId`（`desktop-task-projection-service.ts:522-540`） |
| **sourceDigest 格式一致** | ✅ | Contracts `Sha256DigestSchema = ^sha256:[0-9a-f]{64}$` 与 Core 内部 digest 前缀一致，无转换缺口 |
| **Desktop 面板只读 metadata** | ✅ | `artifact-presentation.ts` 纯展示 helper（title/kindLabel/stateLabel/tone/summary/meta）；`main.ts:805` 只读卡片渲染，无文件打开 / preview server / Markdown-HTML 渲染 / fetch / window.open |
| **无越界** | ✅ | 无 `artifact.preview` / `tool.artifact` Tool 注册或 model-visible schema；无 IPC route 新增；Document Worker 生产 src / Central / `pnpm-lock.yaml` / 根 `package.json` / 根 `tsconfig.json` 未改 |
| **修改范围清单完整** | ✅ | DEVELOPMENT-LOG APV-1A 修改范围含全部 17 个文件（含 `dcf-2-0-contracts.test.ts`），无遗漏 |

---

## 三、发现与分级

### P0 = 0，P1 = 0，P2 = 0，P3 = 0

无代码级、治理级或越界发现。无环境阻塞（本次所有全量门禁在沙箱内直接通过）。

---

## 四、总体结论

```text
结论：PASS（P0 = 0 / P1 = 0 / P2 = 0 / P3 = 0）
```

- APV-1A 的公共 Contracts additive 改动符合 APV-0 硬阻塞判据，且为**最小 additive**：新增 `ArtifactProjectionSchema`（metadata-only）+ `TaskDetailProjection.artifacts`（默认 `[]`），既有字段与语义不变。
- Contracts 层有负向测试证明 `workspaceRoot`/`workbook`/`sessionId`/`schemaVersion` 均被拒绝；Core 投影白名单剥离内部字段；Desktop 面板只读 metadata，无任何文件打开/preview server/渲染能力。
- 全部门禁独立复跑通过，与自测声明一致。
- **APV-1A 可进入用户接受流程。**

## 五、建议的下一步

1. 用户接受 APV-1A QA 后正式关闭 APV-1A。
2. 单独授权 APV-1B（Markdown/Text preview，含 sanitized 渲染安全边界）后进入渲染相关批次。
3. APV-1C（HTML Sandbox）、APV-2 保持 GATED。

— Claude Code（独立 QA，只读，未修改任何生产代码）
