# 0.0.0-dwe.1 — Claude Code 独立 QA 报告

> **⚠️ 版本标注说明**：DWE-1 生产代码已实现，但 `DEVELOPMENT-LOG.md` **尚无任何 DWE 版本条目**，Document Worker 版本号仍为 `0.0.0-dtp.2.0`，DWE 方案文档头部状态仍为 `DWE-1: GATED`。本报告文件名 `0.0.0-dwe.1` 为**临时版本标注**，用于独立记录本次验收，正式开发版本条目需由 Codex 补齐后回链本报告。

## 基本信息

| 属性 | 内容 |
| --- | --- |
| RUN_ID | `2026-08-05-0933-version-dwe.1` |
| 验收对象 | DWE-1：XLSX Write 私有 Worker 能力（`tool.document.xlsx.write`） |
| 日期 | 2026-08-05 |
| 验收者 | Claude Code（独立 QA，只读） |
| 触发方式 | 用户显式指令（"Claude Code 只读 QA DWE-1，先不要进入 DWE-2"） |
| 环境 | Node v24.13.0 / pnpm 11.11.0 / vitest 4.1.10 |
| 改动面 | `services/document-worker/src/xlsx/xlsx-write.ts`（新增）、`xlsx/index.ts`、`document-capability-router.ts`、`document-capability-handler.ts`、`document-worker-runtime.ts`、`protocol/document-worker-protocol.ts`（v1alpha2）＋ 2 个测试文件 |
| 证据目录 | `qa-reports/2026-08-05-0933-version-dwe.1/evidence/` |

> 治理说明：因 `DEVELOPMENT-LOG.md` 无 DWE-1 版本条目（无开发者自测记录、无已知缺口声明、无 `READY_FOR_INDEPENDENT_QA` 状态），本次验收基于用户明确提供的验收范围（用户消息中的 QA 顺序清单）执行，并以当前代码为验收基线。治理缺口作为独立发现记录于本报告 §4。

---

## 一、门禁复跑结果（全部独立执行，未采信开发者自测）

| # | 门禁 | 命令 | 结果 | 证据 |
| --- | --- | --- | --- | --- |
| A1 | Document Worker build | `pnpm --config.verify-deps-before-run=false --filter @robothree/document-worker run build` | **PASS**（tsc -b, EXIT=0） | gates-and-matrix.txt |
| A2 | DW xlsx-write 专项 | `vitest run services/document-worker/tests/xlsx/xlsx-write.test.ts` | **PASS**（9/9） | 同上 |
| A3 | DW 全量测试 | `vitest run services/document-worker/tests` | **PASS**（22 files / 168 tests） | 同上 |
| A4 | Core document-worker 回归 | 5 个 focused 测试文件 | **PASS**（5 files / 29 tests） | 同上 |
| A5 | lint + architecture boundary | `pnpm run lint`（eslint + check-boundaries.mjs） | **PASS** | 同上 |
| A6 | offline frozen install | `CI=true pnpm install --frozen-lockfile --offline` | **PASS**（Already up to date） | 同上 |
| A7 | 完整 `pnpm run check` | `pnpm run check` | **PASS**（144 files / 950 tests）＋ Core smoke / foundation smoke / Desktop preload smoke（sandbox:true）全部通过 | 同上 |

**回归基线说明**：DTP-4 基线为 DW 21 files / 156 tests、全仓 143 files / 938 tests。本次 DW 22 files / 168 tests（+xlsx-write 9 项 + protocol detailCode 用例）、全仓 144 files / 950 tests，新增用例全部为 DWE-1 范围，既有用例无回归。

---

## 二、禁止范围核查（通过）

DWE-1 改动窗口（2026-08-04 20:00 后）仅改动以下文件：

```text
services/document-worker/src/handlers/document-capability-router.ts
services/document-worker/src/protocol/document-worker-protocol.ts
services/document-worker/src/runtime/document-capability-handler.ts
services/document-worker/src/runtime/document-worker-runtime.ts
services/document-worker/src/xlsx/index.ts
services/document-worker/src/xlsx/xlsx-write.ts
services/document-worker/tests/protocol/document-worker-protocol.test.ts
services/document-worker/tests/xlsx/xlsx-write.test.ts
```

| 禁止范围 | 状态 |
| --- | --- |
| `packages/contracts/**`（src） | ✅ 未改 |
| `services/core/**`（src） | ✅ 未改 |
| `apps/desktop/**`（src） | ✅ 未改 |
| `services/central-service/**` | ✅ 未改 |
| `pnpm-lock.yaml` | ✅ 未改（mtime 08-04 13:19，窗口前） |
| 根 `package.json` | ✅ 未改（mtime 08-04 18:06，窗口前） |
| 根 `tsconfig.json` / `pnpm-workspace.yaml` | ✅ 未改 |

**`tool.document.xlsx.write` 未进入正式 Registry / default Agent**（通过）：

- `DOCUMENT_CAPABILITIES`（`document-capability-options.ts`）仍只含 3 个只读工具，测试显式断言 `DOCUMENT_CAPABILITIES` 不含 `xlsx.write`；
- Core `document-tool-registry.ts` 仍只注册 3 个只读工具；
- `services/core`、`apps/desktop`、`packages/contracts` 全仓 grep `xlsx.write` **0 命中**。

---

## 三、DWE-1 安全矩阵核查（通过）

| 矩阵项 | 代码证据 | 测试证据 | 状态 |
| --- | --- | --- | --- |
| fs.link no-clobber 原子发布 | `link(temp, target)`；EEXIST → `target_exists` | 测试验证 target 原文件内容保留 | ✅ |
| 并发 target 出现（validation 与 publish 之间） | link EEXIST 分支 | 注入 racing link（先写 target 再抛 EEXIST）→ `target_exists`，racing 文件保留 | ✅ |
| link 不可用（跨设备/权限）失败关闭，不回退 rename/copy | EXDEV 等 → `publish_failed`，无 rename/copy 回退 | "fails closed when hard-link publishing is unavailable"：target 不存在 + temp 清理 | ✅ |
| 九个崩溃窗口 | 9 个 `XlsxWriteFaultPoint` fault 注入点贯穿 temp create→write→fsync→link→parent fsync→verify→unlink | 9 个 fault point 全部迭代，断言无 temp 残留、target 若存在则无可执行内容 | ✅ |
| formula-like text 以纯文本写出 | 字符串 cell 始终 `t:"s"`，不构造 formula | `=SUM(A1:A2)`/`+cmd`/`-cmd`/`@cmd` 读回均 `t:"s"`；`assertNoExecutableSpreadsheetContent` 断言无 `cell.f` | ✅ |
| readback digest 证据 | 发布前 `logicalDigestFromBytes`（读回规范化比对）+ 发布后 `verifyPublishedFile`（binary sha256 + logical digest） | digest 跨 row/cell 输入顺序稳定测试 | ✅ |
| temp cleanup | `finally` 清理 + handle 闭包 + `removeTemp`（ENOENT 容忍） | 全场景 `tempEntries` 为空 | ✅ |
| 资源收口 | handle 在 finally close；directory fsync handle close | 100 次重复执行无 temp 残留、无文件数增长 | ✅ |
| 输出预算先于发布 | `generateWorkbookBytes` 内 byteLength 检查 → `output_too_large` | maxOutputBytes:64 场景 | ✅ |
| 无网络 / 无 shell / 无嵌套 worker / 无诊断后门 | 静态扫描：无 child_process/net/http/fetch/exec/spawn | canary-scan / production-safety-scan 通过 | ✅ |
| 无新依赖 | document-worker `package.json` 未改，xlsx@0.20.3 为既有依赖 | — | ✅ |
| OOXML readback 安全（macro/ActiveX/OLE/外部关系） | `validateXlsxOoxmlPreflight`：ZIP magic、EOCD bounds、加密拒绝、解压比预算、必需条目、vbaProject/.bin/embeddings/activex/ctrlprops/external/customxml 拒绝、XML 内容类型与外部 Target 检查 | `validateXlsxOoxmlPreflight` 在生成/读回/发布后三段均调用 | ✅ |
| 泄漏边界 | 错误消息无绝对路径/temp 路径；输出仅 bounded metadata（relativePath/digests/计数）；`workspaceRoot` 仅存在于 Worker-private 输入 | — | ✅ |
| 协议兼容（v1alpha2 增量） | 私有多协议常量新增 `v1alpha2`，error 增 `detailCode`（可选），读工具顶层语义不变 | DW 全量 168 tests（含 PDF/XLSX/DOCX read 回归）全过 | ✅ |
| timed_out / cancel 归属 runtime | runtime deadline timer → `timed_out`；signal abort → write 各阶段 `throwIfAborted` → `cancelled`；worker_busy 在并发 invoke | cancel-timeout-race 等 harness 通过 | ✅ |

---

## 四、发现与分级

### 代码正确性 / 安全：P0 = 0，P1 = 0

门禁全过、安全矩阵 16/16 项通过、静态扫描干净。未发现代码级 P0/P1。

### 治理 / 交付流程缺口（阻塞正式关闭）

| # | 等级 | 问题 | 依据 | 建议 |
| --- | --- | --- | --- | --- |
| G-1 | **P1** | `DEVELOPMENT-LOG.md` **无 DWE-1 版本条目**，本次改动 6 个生产源码文件 + 2 个测试文件未升级开发版本 | 工作区规则：修改生产代码必须升级开发版本并在 `DEVELOPMENT-LOG.md` 追加本批范围、自测命令、结果、已知缺口和 QA 状态（`docs/development/README.md`）；独立验收基线要求版本状态为 `READY_FOR_INDEPENDENT_QA` | 由 Codex 补齐 `0.0.0-dwe.1`（或按实际规则命名）版本条目，记录自测命令/结果/已知缺口，并回链本报告 |
| G-2 | **P1** | `CHANGELOG.md` `Unreleased` **无 DWE 条目** | 工作区规则：协议/配置/架构/安全有效改动后必须按触发规则更新 `Unreleased`（本次含私有多协议 v1alpha2、新 capability、error 类 detailCode） | 由 Codex 在 `Unreleased` 补 DWE-1 逻辑变更摘要 |
| G-3 | **P2** | DWE 方案文档头部状态未更新（仍 `DWE-0 IMPLEMENTED / DOCUMENT REVIEW PENDING`，`DWE-1: GATED`），与实际已实现状态不符 | `docs/development/dwe/DWE-XLSX-WRITE-DEVELOPMENT-PLAN.md` L4 | Codex 更新状态并追加 Revision 2 |
| G-4 | **P2** | Document Worker `package.json` 版本仍为 `0.0.0-dtp.2.0` | 版本规则要求生产代码变更升级开发版本 | 随 G-1 一并升级 |

---

## 五、总体结论

```text
代码质量结论：PASS（P0 = 0，P1(code) = 0，安全矩阵 16/16 通过，全部门禁独立复跑通过）

正式交付结论：不能立即 PASS/CLOSED —— 存在 2 项 P1 治理缺口（G-1 缺 DEVELOPMENT-LOG 版本条目、G-2 缺 CHANGELOG 条目）
```

- DWE-1 的**代码实现质量与安全性已达到可验收水平**：no-clobber 原子发布、崩溃窗口收口、formula 注入防护、readback digest、泄漏边界、协议增量兼容均经独立复跑与静态核查确认。
- 按用户既定规则"QA 有 P0/P1 先修不关闭"及工作区强制记录规则，**需 Codex 先补齐 G-1/G-2（并建议一并处理 G-3/G-4）**，再由用户接受后正式关闭 DWE-1。
- `tool.document.xlsx.write` 仍**未进入正式 Registry / default Agent**，DWE-2 保持 GATED，本次 QA 不涉及 DWE-2 任何授权。

## 六、建议的下一步

1. Codex 补齐 DWE-1 开发版本条目 + CHANGELOG 条目 + 方案状态更新（G-1~G-4）。
2. 用户接受后正式关闭 DWE-1（`DWE-1 PASS/CLOSED`）。
3. 再单独授权 DWE-2（Core 接入：WorkspaceGrant、ResourceAccess create、Effect、恢复、Observation、正式 Tool Registry）。

— Claude Code（独立 QA，只读，第一轮未修改任何生产代码）
