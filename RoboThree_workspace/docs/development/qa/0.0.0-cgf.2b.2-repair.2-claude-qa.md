# CGF-2B.2 repair.2 Independent QA Report

| 项目 | 值 |
| --- | --- |
| RUN_ID | `2026-07-31-cgf.2b.2-repair.2-qa` |
| 版本 | `0.0.0-cgf.2b.2-repair.2` (Root) / `0.0.0-cgf.2b.2-repair.2-SNAPSHOT` (Central) |
| 结论 | **INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING** |
| 前置 | repair.1 QA `FAIL — P1`；P1 已由 `!isBlank()` 守卫 + 回归测试关闭 |
| Central online | 182 tests / 0 failures / 0 errors / 0 skipped |
| Central offline | 182 tests / 0 failures / 0 errors / 0 skipped |
| Workspace check | Architecture boundary passed + 107 files / 685 tests + Core/Desktop/Preload smoke PASS |
| `check:cgf2b2:direct-provider` | **PASS** — Streaming 293 deltas / invalid→failed / cancel→cancelled / deadline→timed_out / leak scan 0 / temp Key REMOVED |
| P0/P1/P2/P3 | P0=0 P1=0 P2=0 P3=3（repair.1 遗留 P3 均非本批回归） |
| 执行时间 | 2026-07-31 23:03–23:08 CST |

## 执行环境

```text
Java：OpenJDK 21.0.12 / Spring Boot 3.5.16 / MyBatis-Plus 3.5.16
Node.js：v24.13.0 / Docker Engine 29.6.2 / PostgreSQL 16-alpine
Electron: 43.2.0 (Mac ARM)
```

## 1. 独立门禁

| 命令 | 独立结果 | 开发者声明 |
| --- | --- | --- |
| `CI=true check:central` | **182/0/0/0** (2m14s) | 182/0/0/0 ✅ |
| `CI=true check:central:offline` | **182/0/0/0** (1m58s) | 182/0/0/0 ✅ |
| `CI=true check` (clean env) | Architecture PASS + **107 files / 685 tests** + 3 smoke | 一致 ✅ |
| `CI=true check:cgf2b2:direct-provider` (no Key) | `{"status":"RESOURCE_GATED",...}` 零网络调用 | 一致 ✅ |

> 独立执行，未复用开发者结果。Workspace check 在 `unset ELECTRON_RUN_AS_NODE` 后通过（repair.1 已知 P3 环境问题）。

## 2. P1 修复验证

### 2.1 修复内容

[OpenAiCompatibleModelProviderAdapter.java:151](services/central-service/src/main/java/com/robothree/central/modelgateway/adapter/provider/OpenAiCompatibleModelProviderAdapter.java#L151)：

```java
// repair.1: 仅 isEmpty() — 漏掉纯空白 content
// repair.2: isBlank() — 覆盖 null、空字符串、纯空白
if (text != null && !text.isBlank()) {
    sink.accept(new ModelProviderStreamEvent.TextDelta(text));
}
```

### 2.2 回归测试

[ModelProviderAdapterConformanceTest.java:128](services/central-service/src/test/java/com/robothree/central/modelgateway/provider/ModelProviderAdapterConformanceTest.java#L128) 新增 `ignoresOpenAiEmptyAndBlankContentDeltas()`：

- 注入空字符串 content → 不产生 TextDelta
- 注入三个空格 content → 不产生 TextDelta
- 注入真实文本 "最终答案" + usage + finish → 产生恰好一个 TextDelta
- 断言 `containsExactly(TextDelta("最终答案"), Usage(8,6), Terminal("stop"))`

Conformance 测试从 9 个增长到 **10 个**，全部通过。

### 2.3 repair.1 P1 关闭证据

repair.1 独立 QA 诊断：DeepSeek 首个 SSE chunk `"content":""` → `TextDelta("")` → `DomainValueChecks.text()` reject → `IllegalArgumentException`。

repair.2 以 `!isBlank()` 替换原 `isEmpty()` 守卫：空字符串、`null`、纯空白三态均安全跳过。Central online/offline 均 182/0/0/0（+1 回归测试），Conformance 覆盖仍完整。**P1 代码层已关闭。**

## 3. 真实 Provider 四场景独立联网验证 ✅

**Provider**：DeepSeek（`deepseek-chat`），OpenAI-compatible 协议，临时受限 Key（用户提供，执行后立即重置）。

```
status: PASS
deltaCount: 293
aggregationEvidence: multiple_provider_deltas
canaryObserved: true
outputDigest: ed4a7482e9ec7430a4b5bbe294f665ee49f6fe9de7b82867c212ab09494516b3
durationMillis: 4840
invalidCredential: failed
cancel: cancelled
deadline: timed_out
Key / canary leakage: 0 (wrapper 扫描确认)
temporary Key file: REMOVED (确认不存在)
```

逐项核验：

| 场景 | 预期 | 实际 | 判定 |
| --- | --- | --- | --- |
| 真实 Streaming | text delta 合法接收 + output digest SHA-256 | 293 deltas、`ed4a7482...`、4.8s | ✅ |
| 非法凭证 | 收敛为 `failed` | `invalidCredential: failed` | ✅ |
| 取消 | 收敛为 `cancelled`，且有 delta 后取消 | cancel 场景先收到 delta 再 cancelled | ✅ |
| Deadline | 收敛为 `timed_out` | `deadline: timed_out` | ✅ |
| Key 泄漏 | 不进 stdout/stderr/report | wrapper 扫描 forbidden 值 0 命中 | ✅ |
| canary 泄漏 | 不进 stdout/stderr/report | wrapper 扫描 0 命中 | ✅ |
| 临时 Key 文件 | 执行后删除 | `ls /tmp/rt-cgf2b2` → No such file | ✅ |

> `canaryObserved: true`：DeepSeek 模型原样复述了 synthetic canary，与开发者 Anthropic 结果（`canaryObserved: false`）不同，属于模型行为差异，不影响传输 Conformance。两种结果均合法。

## 4. 代码边界回归确认

repair.2 仅修改 `OpenAiCompatibleModelProviderAdapter` 一行的 `isEmpty()` → `isBlank()` 守卫 + 新增一条 Conformance 测试。其余不变：

| 边界 | repair.2 状态 |
| --- | --- |
| thinking_delta / signature_delta 不投影 | 未改 ✅ |
| Runtime 唯一 durable 提交者 | 未改 ✅ |
| Backend 不直改 Repository | 未改 ✅ |
| ephemeral 不进 durable Result | 未改 ✅ |
| provider_response_invalid 确定性 failed | 未改 ✅ |
| Contract / v0007 / Controller | 未改 ✅ |
| 生产代码 `System::getenv` | 仍仅 Development Source 1 处 ✅ |
| B0008 / U0008 | 不存在 ✅ |
| CGF-2B.3 / CGF-2C | 不存在 ✅ |

## 5. 版本一致性

| 资源 | 声明 | 实际 | 一致 |
| --- | --- | --- | --- |
| Root `package.json` | `0.0.0-cgf.2b.2-repair.2` | `0.0.0-cgf.2b.2-repair.2` | ✅ |
| Central `pom.xml` | `0.0.0-cgf.2b.2-repair.2-SNAPSHOT` | `0.0.0-cgf.2b.2-repair.2-SNAPSHOT` | ✅ |
| README | repair.2 版本 + 状态 | 一致 | ✅ |
| CHANGELOG Unreleased | repair.2 条目 | 已有 | ✅ |
| KEY-NODES KN-116/117 | repair.1 QA FAIL / repair.2 P1 FIXED | 一致 | ✅ |
| CGF-2 Plan | `REPAIR.2 P1 FIXED + FULL REGRESSION PASS / REAL PROVIDER RE-QA PENDING / NOT CLOSED` | 一致 | ✅ |
| CGF-2B.2 Plan §20 | repair.1 增补完整 | 一致 | ✅ |
| CGF-2B.3 / CGF-2C | GATED | GATED | ✅ |

## 6. P3 遗留

| P3 编号 | 项 | 来源 | 状态 |
| --- | --- | --- | --- |
| P3-01 | B.2 计划文档头部状态陈旧 | repair.1 | DEV LOG 确认已同步 |
| P3-02 | `ELECTRON_RUN_AS_NODE=1` 环境污染 | repair.1 | 非产品代码，Workspace check 已规避 |
| P3-03 | 本地 SOCKS 代理污染 Maven Surefire JVM | repair.1 | 非产品代码，`_JAVA_OPTIONS=""` 已规避 |
| P3-04 | 偶发异步测试时序抖动 | repair.1 | repair.1 聚焦复跑两轮 4/0/0/0 未复现 |

均不阻塞 release。

## 7. 结论

**P0=0 P1=0 P2=0 P3=4。CGF-2B.2 repair.2 INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING。**

- Central 182/0/0/0 ×2（在线 + 离线）✅
- Workspace 107/685 + 三 smoke ✅
- repair.1 P1 已由 `!isBlank()` 守卫 + 回归测试关闭 ✅
- 真实 OpenAI-compatible Provider 四场景全部通过 ✅
- 非法凭证→failed、取消→cancelled、Deadline→timed_out ✅
- Key/canary 泄漏 0、临时 Key 文件已删除 ✅
- 代码边界六项全部确认未变 ✅
- 版本一致性全部对齐 ✅

**建议关闭 CGF-2B.2。CGF-2B.3 / CGF-2C 继续 GATED 等待用户解锁。**
