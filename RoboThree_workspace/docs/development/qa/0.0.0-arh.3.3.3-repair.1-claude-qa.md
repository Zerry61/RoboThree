# 0.0.0-arh.3.3.3-repair.1 — Claude Code 独立 QA 报告

## 基本信息

| 属性 | 内容 |
|---|---|
| RUN_ID | `2026-08-16-1718-version-0.0.0-arh.3.3.3-repair.1` |
| 验收对象 | ARH-3.3.3 repair.1：确定性 failpoint handshake 与四通道泄漏扫描 |
| 日期 | 2026-08-16 |
| 验收者 | Claude Code（独立 QA） |
| 环境 | Node v24.13.0（nvm）/ pnpm 11.11.0 / Java 21.0.12 (Central) / Docker 29.6.2 (Testcontainers) |
| 开发版本 | Root/Core `0.0.0-arh.3.3.3-repair.1`；Central `0.0.0-arh.3.3.3-repair.1-SNAPSHOT`；Contracts/Desktop/Document Worker 不变 |
| 前置 | ARH-3.3.3 独立 QA FAIL（P1：长稳第 26 cycle `awaitFailpointBlocked` 时序） |

---

## 一、门禁复跑结果（串行独立执行，Node 24.13.0）

| # | 门禁 | 结果 |
|---|---|---|
| 1 | `CI=true pnpm run harness:arh3.3.3`（formal，从零） | **PASS**：3 轮 semantic replay + **92 个轻量长稳 cycle**，总时长 2198839 ms（36.6 min），`formalStabilitySatisfied=true` |
| 2 | fail-fast 精确 takeover × 10 | **PASS** 10/10（`Cgf2b32…#executesArh333LightweightTakeoverAndResourceClosure`） |
| 3 | `CI=true pnpm run check` | **PASS** 166 files / 1176 tests + 3 smoke |
| 4 | `CI=true pnpm run check:central` | **PASS** 302/0/0/0（BUILD SUCCESS） |
| 5 | `CI=true pnpm run check:central:offline` | **PASS** 302/0/0/0（BUILD SUCCESS） |

关键 Evidence：`semanticResultDigest=sha256:c78fc2cab…` 三轮一致；`stabilityResultDigest=sha256:f93304087…`；
92 个 cycle 的 `perStabilityCycleResourceMetrics` 八类全 0、`perStabilityCycleLeakageChannelMatchCounts`
四通道全 0；`sensitiveOutputMatchCount=0`。历史 digest 仅用于对比，实际执行已从零重跑。

---

## 二、重点核查项（用户指定）

| # | 核查项 | 结论 |
|---|---|---|
| 1 | exact-session latch handshake | ✅ `Cgf2b32FailpointBackend` 每次 `configure` 生成不可复用 `UUID sessionId` + 两个 `CountDownLatch`（entered/release）；`awaitBlocked` 用一次阻塞 `entered().await(15s)` 完成 `configure→await-blocked→release` 精确会话握手，非轮询 |
| 2 | stale/mismatch 拒绝 | ✅ `requireSession(sessionId)` 对过期/不匹配会话抛 `session identity mismatch`；`awaitBlocked` 再校验 session 未变更、未提前 release，均 fail-closed |
| 3 | 四通道五类 marker | ✅ `FourChannelLeakageScanner` + `LEAKAGE_CHANNEL_KEYS`（processOutput/childLogAndTrace/testAndMachineEvidence/safeJsonAndDiagnostics）+ `LEAKAGE_MARKER_KEYS`（canary/credential/providerEndpoint/contentBody/absolutePath），严格 schema 拒绝缺通道/缺 marker |
| 4 | raw/Base64/URL-encoded 泄漏扫描 | ✅ `markerVariants` 生成 raw + `Buffer.toString("base64")` + `encodeURIComponent` 三形态；负向测试覆盖编码泄漏检测且不回显 marker 值 |
| 5 | 无延长轮询 | ✅ failpoint 的 `awaitCondition(() => get(...,"/failpoint"))` 轮询已移除，边界测试 `not.toMatch` 锁定 |
| 6 | 无自动重试 | ✅ 失败即 fail-fast 抛错并落盘 `failure.json`，无 retry/sleep 循环（仅长稳 `while` 是正常 cycle 循环） |
| 7 | 无删除 takeover | ✅ `executesArh333LightweightTakeoverAndResourceClosure` 原样保留，takeover/lease/fencing/Provider 语义未变 |
| 8 | 无生产边界漂移 | ✅ repair.1 改动仅限 `scripts/` 与 `services/central-service/src/test/**`，生产源码/Contract/Schema/migration/Kernel/Desktop/依赖/lockfile 零改动 |

---

## 三、发现

### P0 = 0，P1 = 0，P2 = 0，P3 = 0

**过程记录（非缺陷）**：首次并行执行"formal harness + 10 次 takeover"时，formal harness 第 1 轮
replay 的 B33 测试因与我并行的 Central takeover 复跑争抢 PostgreSQL/Testcontainers 资源而失败
（`executesB33SecurityProtocolAndResourceClosureAcrossFiveLifecycles`）。这属于我独立 QA 引入的并行
干扰，非 codex 缺陷。改为严格串行后，formal harness 从零重跑 92 个长稳 cycle 全过。本结论以串行
结果为准，并明确：**正式 Harness 与任何 Central 测试必须串行，不得并行争抢 Docker**。

上一轮 P1（长稳第 26 cycle `awaitFailpointBlocked` 偶发失败）在 repair.1 后消失：本轮长稳 92 个
cycle（远超上一轮失败的 26 cycle）零失败，精确 session latch handshake 已消除 failpoint 阻塞检测
的时序窗口。

---

## 四、结论

```text
INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING
P0 = 0，P1 = 0，P2 = 0，P3 = 0
```

repair.1 正确修复了 ARH-3.3.3 的 P1：确定性 failpoint session handshake 替代 blocked-state 轮询，
stale/mismatch 失败关闭，四通道五类 marker 及 raw/Base64/URL-encoded 泄漏扫描落地，且无延长轮询、
无自动重试、未删除 takeover、无生产边界漂移。五项门禁独立串行复跑全部通过，正式长稳 92 个轻量
cycle 稳定通过。

**ARH-3.3.3-repair.1 可进入用户接受流程。ARH-3.3.3 / ARH-3.3 / ARH-3 仍需用户接受本结论后方可
关闭。**

— Claude Code（独立 QA，只读）
