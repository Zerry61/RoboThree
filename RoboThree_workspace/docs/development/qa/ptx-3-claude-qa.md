# PTX-3 — Claude Code 独立 QA 报告

## 基本信息

| 属性 | 内容 |
|---|---|
| RUN_ID | `2026-08-25-1600-version-ptx.3` |
| 验收对象 | PTX-3：Desktop Product E2E（scripted provider PPTX 意图 + 端到端产品化链路） |
| 日期 | 2026-08-25 |
| 验收者 | Claude Code（独立 QA，只读） |
| 环境 | Node v24.13.0（nvm，`.node-version`）/ pnpm 11.11.0 |
| 开发版本 | Root `0.0.0-ptx.3`；Contracts/Core 并发 DFI `0.0.0-dfi.5.2.1`；Document Worker `0.0.0-ptx.1` |
| 上游 | PTX-1、PTX-2（含 P1 修复复测）均 `PASS/CLOSED` |

---

## 一、门禁复跑结果（串行独立执行，Node 24.13.0）

| # | 门禁 | 结果 |
|---|---|---|
| 1 | PTX-3 focused（scripted provider + ptx3 E2E） | **PASS 2 files / 9 tests** |
| 2 | `CI=true pnpm run check`（root） | **PASS 254 files / 1699 tests + 3 smoke + Architecture boundary** |
| 3 | `pnpm run audit:dtp4` | **PASS** |
| 4 | 边界：lockfile / 零命中 | 仍 `c47641ac…` 未变；Contracts/Desktop/Central/Admin 对 `tool.document.pptx.write`/`PresentationSpecV1`/`pptxgenjs` 零命中 |

注：PTX-3 为 Desktop E2E 批次，未改 Central；Central 在最近 DFI-5.2.1 QA 时已由我的环境补跑 404/404
全绿，本批不涉及 Central 路径，无需重跑。

---

## 二、重点核查项

| # | 核查项 | 结论 |
|---|---|---|
| 1 | scripted provider PPTX 意图 | ✅ [desktop-document-scripted-model-provider.ts](services/core/src/adapters/fake/desktop-document-scripted-model-provider.ts) `isWriteIntent` + `.pptx` 匹配 → `tool.document.pptx.write`；scripted arguments 只含 relativePath + 受控 presentation + options，不含 workspaceRoot/limits/dataBase64/URL |
| 2 | 端到端链路 | ✅ E2E 覆盖 `submitTurn → AgentBridge → Core Registry → Document Worker → PPTX 生成 → Artifact projection → assistant final` |
| 3 | conversation 安全展示 | ✅ 断言 `Using 1 Tool`、`Document tool: tool.document.pptx.write`、`Created: reports/deck.pptx`、`Slides: 2`、`Presentation digest:`；**`not.toContain(workspacePath)`/`"\slides"`/`dataBase64`/`Generated by RoboThree`**——不泄漏绝对路径/raw slides/base64 |
| 4 | .pptx 真实落盘 | ✅ `readdir(workspace/reports)` 只有 `deck.pptx`（真实文件生成） |
| 5 | OOXML 结构验证 | ✅ `readZipEntries` 断言 `[Content_Types].xml` + `ppt/presentation.xml` + `ppt/slides/slide1.xml` + `slide2.xml`，且 slide XML 可 inflate 读取——结构化 golden（非二进制 sha256） |
| 6 | Task Detail artifact projection | ✅ kind `document` + PPTX mediaType + `slideCount: 2` + capabilityId + Presentation digest；**not.toContain workspacePath/slides** |
| 7 | 边界 | ✅ 未改 Desktop Renderer/Main/Preload、Admin、Central、Document Worker 生产实现、migration、依赖、lockfile |

---

## 三、发现

### P0 = 0，P1 = 0，P2 = 0，P3 = 0

---

## 四、结论

```text
INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING
P0 = 0，P1 = 0，P2 = 0，P3 = 0
```

PTX-3 正确完成 Desktop Product E2E：scripted model provider 新增 `Create … .pptx` 意图（仅产生
relativePath + 受控 presentation + options）；真实 Desktop `submitTurn → AgentBridge → Core Registry →
Document Worker` 链路生成 `.pptx` 并落盘；conversation 与 Task Detail 均以安全摘要展示（不泄漏
workspace 绝对路径、raw slides、dataBase64、演示文案）；OOXML ZIP 结构结构化验证（presentation.xml +
两页 slide XML）通过。门禁独立复跑全绿（focused 2/9、完整 check 254/1699 + 3 smoke、audit:dtp4）。
边界零漂移：lockfile 未变、Contracts/Desktop/Central/Admin 零命中。

**PTX-3 可进入用户接受流程；接受后 `tool.document.pptx.write` 的 PTX 线（PTX-0~PTX-3）完整收口，但
PPTX visual preview（PTX-4）仍 GATED 需单独授权。DFI-5.2.2~5.2.3、DFI-5.3~5.4、AAPI-0.3~0.4、TGM、
Knowledge Provider 继续 GATED。**

— Claude Code（独立 QA，只读）
