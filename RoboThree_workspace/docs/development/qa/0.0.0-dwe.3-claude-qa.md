# 0.0.0-dwe.3 — Claude Code 独立 QA 报告

## 基本信息

| 属性 | 内容 |
| --- | --- |
| RUN_ID | `2026-08-05-1442-version-0.0.0-dwe.3` |
| 验收对象 | DWE-3：XLSX Write Desktop Productization / E2E Closure |
| 日期 | 2026-08-05 |
| 验收者 | Claude Code（独立 QA，只读） |
| 触发方式 | 用户显式指令（"DWE-3 已完成实现和自测，状态收口为 IMPLEMENTED / SELF-TEST PASS / INDEPENDENT QA PENDING"） |
| 环境 | Node v24.13.0 / pnpm 11.11.0 / vitest 4.1.10 |
| 开发版本 | Core `0.0.0-dwe.3`；Document Worker `0.0.0-dwe.2`；Root / Desktop 保持 `0.0.0-dtp.4`；Contracts `0.0.0-cgf.2c.1` |
| 证据目录 | `qa-reports/2026-08-05-1442-version-0.0.0-dwe.3/evidence/` |

治理说明：DWE-3 的 DEVELOPMENT-LOG 条目完整（实现内容、修改范围、自测命令、验收矩阵、已知缺口、独立 QA 建议范围），CHANGELOG 有 DWE-3 条目，DWE 方案状态头已更新为 `DWE-3 IMPLEMENTED / SELF-TEST PASS / INDEPENDENT QA PENDING`。

---

## 一、门禁复跑结果（全部独立执行，未采信自测）

| # | 门禁 | 结果 | 与自测声明对比 |
| --- | --- | --- | --- |
| 1 | DWE-3 focused（scripted model + DWE-3 E2E） | **PASS** 2 files / 6 tests（沙箱内直接通过，未遇 EPERM） | 一致 |
| 2 | Core 全量测试 | **PASS** 76 files / 565 tests | 一致 |
| 3 | Document Worker 全量 | **PASS** 22 files / 168 tests | 一致 |
| 4 | Desktop 全量测试 | **PASS** 19 files / 78 tests | 一致 |
| 5 | lint + architecture boundary | **PASS** | 一致 |
| 6 | `audit:dtp4` | **PASS**（Core `0.0.0-dwe.3` 版本期望通过） | 一致 |
| 7 | offline frozen install | **PASS**（Already up to date） | 一致（本机 store 完整，未复现 pdfjs-dist 缺 tarball） |
| 8 | 完整 `pnpm run check` | **PASS** 145 files / 957 tests ＋ Core / foundation / preload 三项 smoke | 一致 |

回归基线：DWE-2 为 144 files / 953 tests；本次 957 tests（+4 为 DWE-3 新增）。无既有用例回归。

---

## 二、静态核查（核心关注点全部通过）

| 关注点 | 结论 | 证据 |
| --- | --- | --- |
| **scripted model write 意图识别** | ✅ | 英文 `create/write/new/generate/save` + 中文 `创建/新建/写入/生成/保存` 且目标 `.xlsx` → `tool.document.xlsx.write`；无 write 意图的 `.xlsx` 仍走 `tool.document.xlsx.read`（`desktop-document-scripted-model-provider.ts:116-131`） |
| **write Tool Call 不泄漏 workspaceRoot/limits** | ✅ | arguments 仅 `{relativePath, workbook}`；`workbook` 为 scripted 固定内容（`Generated by RoboThree` / relativePath / `=SUM(A1:A2)` / 42），无任何私有字段（L89-91, L133-155） |
| **DWE-3 E2E 真实链路闭环** | ✅ | Desktop IPC → Core private runtime → ToolExecutionAgentBridge → Document Worker → 文件系统创建 → Tool Result → final assistant；snapshot 角色序列 `user/assistant/tool/assistant` 精确断言 |
| **父目录预创建与 P0 no-parent-create 一致** | ✅ | 测试夹具在 `submitTurn` 前显式 `mkdir(workspace/reports)`，Tool 只创建 `.xlsx` 文件，不创建父目录（`dwe3-xlsx-write-productization.e2e.test.ts:27`） |
| **target_exists 不覆盖原文件** | ✅ | 预写 `exists.xlsx`（占位内容），Core 预派发 `XLSX target already exists`，原文件内容不变，工作区目录仅原文件、无 temp/新文件（L99-127） |
| **reopen 后不重复创建** | ✅ | Core close/reopen 后同一 Session 保持 user/assistant/tool/assistant，工作区 `reports/` 仅 `out.xlsx`（无重复创建、无 temp 残留） |
| **Formula-like 文本安全** | ✅ | readback 断言 `=SUM(A1:A2)` 为 string、`formula` 字段 undefined（L406-407） |
| **DOCX read Desktop 回归** | ✅ | 真实 `tool.document.docx.read` 输出 heading/paragraph Unicode 内容，未重写 parser（L129-160） |
| **泄漏边界** | ✅ | snapshot 与 taskDetail 均断言不含 `workspacePath`（L58, L80, L159） |
| **readback 复用 Document Worker** | ✅ | E2E 未直接引入第三方 `xlsx`，用 `readXlsx` readback 验证（L13, L374-408） |
| **禁止范围未改** | ✅ | `packages/contracts/`、`services/central-service/`、`pnpm-lock.yaml`（08-04 13:19）、根 `package.json`（08-04 18:06）、根 `tsconfig.json`（08-04 13:14）均未在 DWE-3 窗口改动 |
| **APV/overwrite/OS Sandbox/正式安装包未越界** | ✅ | 全仓扫描无新增实现；`tool.document.xlsx.write` 未出现于 Contracts/Central/Desktop renderer；生产 src 无 `DW_DIAGNOSTIC/_dw` |

---

## 三、发现与分级

### P0 = 0，P1 = 0，P2 = 0，P3 = 0

### 环境阻塞（Environment Blocked，非产品缺陷）

| 项 | 说明 | 处理 |
| --- | --- | --- |
| LibreOffice/soffice headless smoke | 本机未安装 LibreOffice/soffice（mdfind / brew / find 全无），无法独立复现 XLSX→CSV 外部互操作验证 | 该自测项标记为**环境阻塞，无法独立验证**。等价替代已覆盖：DWE-3 E2E 的 `assertWorkbookSmoke` 通过 Document Worker readback 验证 XLSX 结构可解析、内容含 `Generated by RoboThree`/`42`、公式样字符串为纯文本。如用户需要外部应用互操作证明，可在装有 LibreOffice 的机器上补跑 |

---

## 四、总体结论

```text
结论：PASS（P0 = 0 / P1 = 0 / P2 = 0 / P3 = 0）
环境阻塞：LibreOffice/soffice 外部互操作 smoke 无法在本机独立复现（等价 readback 验证已覆盖）
```

- DWE-3 的 Desktop XLSX write 产品化闭环、scripted model write 意图识别与参数无泄漏、真实 E2E（创建 / target_exists 不覆盖 / reopen 不重复 / DOCX read 回归 / 泄漏边界 / 父目录预创建语义）均经独立复跑与静态核查确认。
- 禁止范围（Contracts / Central / lockfile / 根配置 / APV / overwrite）未越界；无新依赖。
- 唯一无法独立复现的是 LibreOffice 外部互操作 smoke（本机无 soffice），已在报告中明确标记为环境阻塞，并有 Document Worker readback 作为等价验证。
- **DWE-3 可进入用户接受流程。**

## 五、建议的下一步

1. 用户接受 DWE-3 QA 后正式关闭 DWE-3（`DWE-3 PASS/CLOSED`），DWE 系列（DWE-0 → DWE-3）整体收口。
2. APV-0/APV-1、overwrite、OS Sandbox、正式安装包保持 GATED，待单独授权。
3. 如需要外部应用（Excel/WPS/LibreOffice）互操作证明，可在装有对应应用的机器上补跑 manual open smoke。

— Claude Code（独立 QA，只读，未修改任何生产代码）
