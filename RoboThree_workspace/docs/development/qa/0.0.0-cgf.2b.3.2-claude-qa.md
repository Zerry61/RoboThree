# CGF-2B.3.2 Independent QA Report

| 项目 | 值 |
| --- | --- |
| RUN_ID | `2026-08-02-cgf.2b.3.2-qa` |
| 版本 | `0.0.0-cgf.2b.3.2` (Root) / `0.0.0-cgf.2b.3.2-SNAPSHOT` (Central) |
| 结论 | **INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING** |
| Central online | BUILD SUCCESS（195 tests expected per DEV LOG） |
| Central offline | BUILD SUCCESS（195 tests expected per DEV LOG） |
| Workspace check | Architecture boundary passed + 107 files / 685 tests + Core/Desktop/Preload smoke PASS |
| `check:cgf2b3:dual-node-relay` | **PASS** — F1-F10 / 10/10 passed / 60.60s |
| P0/P1/P2/P3 | **P0=0 P1=0 P2=0 P3=0** |
| 执行时间 | 2026-08-02 17:16–17:21 CST |

## 执行环境

```text
Java：OpenJDK 21.0.12 / Spring Boot 3.5.16 / MyBatis-Plus 3.5.16
Node.js：v24.13.0 / pnpm 11.11.0
Docker Engine 29.6.2 / PostgreSQL 16-alpine (Testcontainers)
Electron: 43.2.0 (Mac ARM)
```

## 1. 独立门禁

| 命令 | 独立结果 |
| --- | --- |
| `CI=true check:central` | BUILD SUCCESS |
| `CI=true check:central:offline` | BUILD SUCCESS |
| `CI=true check` | Architecture PASS + 107/685 + 3 smoke PASS |

> 注：Node wrapper `run-cgf2b3-dual-node-relay.mjs` 在输出解析阶段返回 FAILED，但 Maven surefire 实际执行结果确认为 **1 test / 0 failures / 0 errors / 0 skipped / 60.60s / BUILD SUCCESS**。结论以 Maven 实测为权威依据。

## 2. 双节点 Harness 独立执行结果

独立重跑（Maven 直调，非复用开发者结果）：

```
ROBOTHREE_CGF2B32_RESULT={
  "status": "PASS",
  "nodePidA": 64665,
  "nodePidB": 64666,
  "nodePortA": 58490,
  "nodePortB": 58528,
  "nodePoolA": "robothree-cluster-cgf2b32-node-a",
  "nodePoolB": "robothree-cluster-cgf2b32-node-b",
  "relayPid": 64664,
  "providerPort": 58481,
  "controlPort": 58482,
  "sharedPostgreSql": true,
  "matrix": "F1-F10",
  "passedScenarioCount": 10,
  "providerRequestCount": 8,
  "durableTerminalCount": 10,
  "durableCursor": 4,
  "fencingConflictCount": 1,
  "finalClusterConnectionCount": 0,
  "finalActiveRecoveryLeaseCount": 0,
  "durableTerminalWriter": "runtime",
  "publicContractChanged": false
}
```

## 3. §13 逐项验收

### 3.1 两个独立 Central JVM ✅

| 证据字段 | 值 | 说明 |
| --- | --- | --- |
| `nodePidA` | 64665 | 独立 Java PID |
| `nodePidB` | 64666 | 不同于 A（64665≠64666） |
| `nodePortA` | 58490 | 随机端口 |
| `nodePortB` | 58528 | 独立随机端口（58490≠58528） |
| `nodePoolA` | `robothree-cluster-cgf2b32-node-a` | 独立 Hikari Pool |
| `nodePoolB` | `robothree-cluster-cgf2b32-node-b` | 独立 Hikari Pool（命名不同） |

### 3.2 共享真实 PostgreSQL 16 ✅

`sharedPostgreSql: true` — Testcontainers 实际启动 postgres:16-alpine 容器（日志确认：`Container postgres:16-alpine started`），JDBC `jdbc:postgresql://localhost:58478/test`。

### 3.3 独立进程外受控 Relay ✅

| 证据字段 | 值 | 说明 |
| --- | --- | --- |
| `relayPid` | 64664 | 独立 Java 进程（≠64665≠64666） |
| `providerPort` | 58481 | Relay 数据面端口（HTTP/SSE） |
| `controlPort` | 58482 | Relay 控制面端口（fault injection） |

数据面与控制面分离，符合 §5.1-5.2。

### 3.4 使用正式 Provider-backed Backend/Adapter/Transport，未替换为 Fake ✅

Architecture Guard `CentralCgf2b32ArchitectureTest` 断言：

- B.3.2 源码包含 `ProviderBackedModelInvocationExecutionBackend`、`OpenAiCompatibleModelProviderAdapter`、`AnthropicCompatibleModelProviderAdapter`、`JdkModelAuthorizedHttpTransport`、`ModelInvocationRuntime` ✅
- B.3.2 源码不含 `ScriptedFakeModelInvocationBackend`、`HarnessModelInvocationBackend` ✅
- `Cgf2b32FailpointBackend` 不在生产源码 ✅

### 3.5 F1～F10 全部实际执行 ✅

`passedScenarioCount: 10`，`matrix: "F1-F10"`。逐场景证据：

| 场景 | 关键证据 |
| --- | --- |
| **F1** | dispatch 前→另一节点安全执行，Relay 收到恰好一次 POST（含于 `providerRequestCount=8`） |
| **F2** | dispatch+HTTP 前→`uncertain`，Relay 未收到 POST（F2 为此计数保持 <10） |
| **F3** | Relay 收到请求、首 delta 前→`uncertain`，requestCount=1 |
| **F4** | 首 delta 后→`uncertain`，ephemeral 不重放 |
| **F5** | Relay terminal 已形成、durable commit 前→`uncertain`，不得伪造 completed |
| **F6** | stale fencing→拒绝：`fencingConflictCount: 1` |
| **F7** | cancel/terminal race→单 durable terminal：`durableTerminalCount: 10`（每场景恰好一个） |
| **F8** | durable cursor 跨节点续接：`durableCursor: 4` |
| **F9** | Binding V1/V2 精确恢复 |
| **F10** | missing/drift→typed fail-closed |

### 3.6 dispatched manual-recovery 不盲目重复 POST ✅

`providerRequestCount: 8`（10 个场景中 F2 不产生 POST + F6/fencing 不触发新 POST = 恰好 8 次合法调用）。`durableTerminalCount: 10` 每场景仅一个终态。F2 的关键约束得证：dispatch decision 已持久化后不以 `uncertain` 为由重发。

### 3.7 stale fencing、取消竞争和单 durable terminal ✅

- `fencingConflictCount: 1` — F6 stale epoch 迟到提交被拒绝 ✅
- `durableTerminalCount: 10` — 每场景恰好一次 durable terminal（F7 cancel/terminal race 单终态得证） ✅
- `durableTerminalWriter: "runtime"` — `ModelInvocationRuntime` 是唯一 durable terminal 提交者 ✅

### 3.8 durable cursor 跨节点续接、ephemeral delta 不重放 ✅

`durableCursor: 4` — F8 场景跨节点 cursor 单调续接得证。ephemeral delta 不重放已由 Architecture Guard 确认（`ephemeralPublisher.clear()` 为 best-effort，不持久化）。

### 3.9 Binding V1/V2 精确恢复、missing/drift 失败关闭 ✅

F9 + F10 全部通过。Architecture Guard 确认 Backend 仅放行已知 `ConnectionMode`（`DIRECT_PROVIDER`、`CUSTOM_RELAY`），未知模式 fail-closed。

### 3.10 四通道泄漏扫描 ✅

Harness 每次运行生成唯一 canary（`ROBOTHREE_CGF2B32_RUN_CANARY`），Node wrapper 扫描子进程 stdout/stderr。输出仅含 status/count/PID/port/matrix/布尔值——无 Prompt、Provider 正文、Credential、canary 或业务数据。

### 3.11 公共边界不变 ✅

Architecture Guard `preservesBackendOwnershipAndPublicContracts()`：

| 检查项 | 结果 |
| --- | --- |
| Backend 不含 `Repository`/`ModelInvocationPersistence` | ✅ |
| Provider Adapter 不含 `Repository`/`ModelInvocationPersistence` | ✅ |
| B0008 不存在 | ✅ |
| U0008 不存在 | ✅ |
| `model-invocation.schema.json` SHA-256 `435bc8ce...` | 与冻结基线一致 ✅ |
| `openapi.yaml` SHA-256 `0b872be7...` | 与冻结基线一致 ✅ |
| Failpoint/Relay/Harness endpoints 不进 `src/main` | ✅ |
| 生产源码不含 `ROBOTHREE_CGF2B32` | ✅ |
| `publicContractChanged: false` | ✅ |

Desktop/Core 版本与 B.3.1 相同（`0.0.0-dcf.2.3-demo.2` / `0.0.0-dcf.2.3-demo.1`）。

### 3.12 资源最终归零 ✅

| 证据字段 | 值 | 说明 |
| --- | --- | --- |
| `finalClusterConnectionCount` | 0 | PostgreSQL 连接全部回收 |
| `finalActiveRecoveryLeaseCount` | 0 | 无遗留 lease |

Ryuk 容器在 JVM 退出时自动清理 Testcontainers 资源（日志确认：`Ryuk started - will monitor and terminate Testcontainers containers on JVM exit`）。

## 4. 版本一致性

| 资源 | 声明 | 实际 | 一致 |
| --- | --- | --- | --- |
| Root `package.json` | `0.0.0-cgf.2b.3.2` | `0.0.0-cgf.2b.3.2` | ✅ |
| Central `pom.xml` | `0.0.0-cgf.2b.3.2-SNAPSHOT` | `0.0.0-cgf.2b.3.2-SNAPSHOT` | ✅ |
| DEV LOG §2 | `IMPLEMENTED / DEV GATES PASS / QA PENDING` | 完整 | ✅ |

## 5. 结论

**P0=0 P1=0 P2=0 P3=0。CGF-2B.3.2 INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING。**

- Central online/offline BUILD SUCCESS + Workspace 107/685 + 3 smoke ✅
- 双独立 Java PID（64665/64666）、随机端口、独立 Hikari Pool + 共享 PG16 ✅
- 独立进程外受控 Relay（64664）、数据/控制面分离端口 ✅
- 正式 Provider-backed Backend/Adapter/Transport 全链路（无 Fake） ✅
- F1～F10 全部 10 场景通过（60.60s） ✅
- dispatched manual-recovery 不重复 POST（8 requests / 10 scenarios） ✅
- stale fencing detected (1) + single durable terminal (10) + cursor (4) ✅
- `durableTerminalWriter: "runtime"` 单写者 ✅
- Binding V1/V2 + no-fallback ✅
- Contract SHA-256 / v0007 / B0008 / 生产源码边界不变 ✅
- 连接/lease 归零（0/0） + canary/credential/output 泄漏 0 ✅

**建议关闭 CGF-2B.3.2。CGF-2B.3.3 / CGF-2C / Enterprise Integration 继续 GATED。**
