# DCF-2.0 Independent QA Report

| 项目 | 值 |
| --- | --- |
| RUN_ID | `2026-07-27-dcf.2.0-qa` |
| 版本 | `0.0.0-dcf.2.0` |
| 结论 | **INDEPENDENT_QA_PASS — USER_ACCEPTED / CLOSED** |
| Node 全量 | 94 files / 614 tests PASS |
| Contracts 专项 | 1 file / 14 tests PASS |
| 执行时间 | 2026-07-27 18:17 |
| P0/P1/P2/P3 | 0 |

## 执行环境

```text
Node.js：v24.13.0 / pnpm：11.11.0 / macOS：Darwin 24.4.0
```

## 执行结果

| 命令 | 结果 |
| --- | --- |
| `CI=true pnpm run lint` | Architecture boundary + ESLint PASS ✅ |
| `CI=true pnpm exec vitest run` | 94 files / **614 tests PASS** ✅ |
| Contracts 专项（verbose） | 1 file / **14 tests PASS** ✅ |
| TypeScript/Preload/Renderer build | PASS ✅ |

Electron smoke 在 headless CLI 跳过——已知环境限制。Vitest 94/614 + Contracts 14/14 为核心验收证据。

## 14 项独立 QA 逐条验证

### 1. clean build + pnpm run check ✅

94/614 + Architecture boundary + ESLint + TypeScript/Preload/Renderer build。

### 2. 四类 Projection + 五类 Command 全部 strict/JSON-safe ✅

"accepts all five high-level Task commands" + "rejects internal or sensitive fields from Task Detail" + "keeps Task Summary strict" ✅

### 3. Task display status 顺序与语义未改变 / additive-only ✅

"freezes the initial Task display status order while allowing future additive versions" ✅

### 4. 所有 Task Command 必须带 expectedTaskRevision ✅

"requires Task revision on all control commands" ✅

### 5. confirmation decision 必须带 confirmationId + requestDigest ✅

"binds a confirmation decision to confirmationId and requestDigest" ✅

### 6. 决策 Contract 拒绝 decidedByUserId/ActionIntent/Credential/Tool 参数 ✅

"does not accept identity claims or mutable ActionIntent in confirmation decisions" ✅

### 7. pending/confirmed/rejected 时间字段一致性 ✅

"rejects impossible User Confirmation status metadata" ✅

### 8. Tool Activity terminal 状态必须有 endedAt ✅

"keeps Tool Activity terminal timing consistent" ✅

### 9. Task Detail 中 Activity/Confirmation 必须属于相同 Task ✅

"requires all nested projections to belong to the same Task" ✅

### 10. Projection 不暴露 Effect/Receipt/Outbox/Checkpoint/CapabilityLock ✅

`grep` 全仓扫描 → 仅 `TaskControlReceiptSchema`（用户可见的 command receipt，非内部对象）。无 Effect/Outbox/Checkpoint/CapabilityLock 泄漏 ✅

### 11. durable changed Event 只有 ID + queryRef ✅

"emits only query references for Tool Activity and Confirmation changes" + 5 个 Event 均只有 `queryRef` 字段 ✅

### 12. 新增 typed error + bounded Query ✅

"defines bounded Task and pending-confirmation queries" + Task not found/invalid state/stale revision/permission denied + Confirmation not found/expired/duplicate/digest conflict ✅

### 13. Kernel reducer / Confirmation Persistence / Task SQLite 未修改 ✅

`grep -rn "TaskProjection\|ConfirmationProjection\|ToolActivity" services/core/src/kernel` → **0 命中** ✅

### 14. DCF-1.1～1.3 + KAF-0～5 全量回归 ✅

94/614 全量通过 ✅

## 结论

**P0=0 P1=0 P2=0 P3=0。DCF-2.0 INDEPENDENT_QA_PASS — USER_ACCEPTED / CLOSED。**

- 94/614 + Contracts 14/14 全部独立重跑 ✅
- Projection strict/JSON-safe / 五类 Command expectedTaskRevision ✅
- confirmationId + requestDigest 绑定 ✅
- 拒绝 ActionIntent/Credential/identity claims ✅
- Event 只有 ID + queryRef ✅
- Kernel 隔离 + 全量回归 ✅

用户已于 2026-07-27 正式接受本报告并关闭 DCF-2.0；DCF-2A 已明确授权进入开发。
