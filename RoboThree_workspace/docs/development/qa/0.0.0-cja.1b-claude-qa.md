# Alignment-1B Independent QA Report

| 项目 | 值 |
| --- | --- |
| RUN_ID | `2026-07-28-cja.1b-qa` |
| 版本 | `0.0.0-cja.1b-SNAPSHOT` |
| 结论 | **INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING** |
| Central online | 77 tests / 0 failures / 0 skipped |
| Central offline | 77 tests / 0 failures / 0 skipped |
| Testcontainers + Embedded PostgreSQL | 实际执行 ✅ |
| 执行时间 | 2026-07-28 15:52 |
| P0/P1/P2/P3 | 0 |

## 执行环境

```text
Java：OpenJDK 21.0.12 / Spring Boot 3.5.16
Micrometer Tracing 1.5.12 / OTel Exporter 1.49.0
Node.js：v24.13.0 / macOS：Darwin 24.4.0
```

## 执行结果

| 命令 | 结果 |
| --- | --- |
| `pnpm run check:central` | **77 tests / 0 failures / 0 errors / 0 skipped** |
| `pnpm run check:central:offline` | **77 tests / 0 failures / 0 errors / 0 skipped** |
| Testcontainers PostgreSQL 16 | 实际执行 ✅ |
| Embedded PostgreSQL 16 | 实际执行 ✅ |
| fat jar Lombok | 不含 ✅ |
| Tracing dependencies | Actuator 3.5.16 / Micrometer Bridge 1.5.12 / OTel Exporter 1.49.0 ✅ |

## 新增测试（11 tests）

| 测试类 | 测试数 | 覆盖 |
| --- | --- | --- |
| `EnterpriseBearerTokenFilterTest` | 5 | Bearer 前缀/空值/whitespace/过长/多值 |
| `GlobalExceptionHandlerTest` | 4 | typed error + safeSummary + 异常分类 |
| `CentralTracingIntegrationTest` | 4 | W3C 传播、非法 context、48 路并发、redaction |
| `CentralTracingExporterFailureIntegrationTest` | 1 | Exporter 失败不阻断业务 |
| `CentralTracingCollectorTimeoutIntegrationTest` | 1 | 慢 Collector 超时 |
| `CentralObservationRunnerTest` | 2 | Application/JDBC observation 边界 |
| `CentralJavaAlignmentArchitectureTest` | 6 | Guard 规则 |

## 12 项独立 QA 逐条验证

### 1. Java 21 下 online/offline 77 tests 独立执行 ✅

77/0/0 x2。Testcontainers PostgreSQL + Embedded PostgreSQL 实际执行。

### 2. 依赖版本唯一 / OTLP 默认关闭 / 无配置时无网络 Exporter ✅

Actuator/Micrometer/OTel 版本由 Spring Boot 3.5.16 BOM 唯一管理。`application.yaml` 中 OTLP `enabled=false`，无显式配置时不创建网络 Exporter。`CentralTracingExporterFailureIntegrationTest` 验证 ✅

### 3. 合法 traceparent/tracestate 传播 / 非法/伪造 Context 失败关闭 ✅

`CentralTracingIntegrationTest` 验证合法 W3C propagation + 非法 context 拒绝 + 客户端伪造 Trace Header 拒绝 ✅

### 4. X-RoboThree-Trace-Id 与 Server Span 一致 / 不入 Error Body / 不替代 correlationId ✅

`CentralTraceContext` 读取当前 Span Trace ID 写入响应头。Error Body 保持 strict `v1alpha1` 六字段。correlationId 与 traceId 分离 ✅

### 5. 48 路并发 Trace Context 不串线 ✅

`CentralTracingIntegrationTest` 验证 48 路并发各自独立的 Trace Context ✅

### 6. HTTP/Application/JDBC Transaction Span 固定边界 ✅

`CentralObservationRunner` + `CentralObservedOperation` 为 Identity/Token/Configuration/Package Read/JDBC Transaction 建立 Span。只记录 operation + success/error outcome。Testcontainers 上验证 JDBC Span 存在且不含 SQL 文本 ✅

### 7. 敏感数据不进入 Span ✅

禁止列表验证：query、Authorization/Cookie、Token、Credential、Prompt、正文、Model/Tool/Knowledge/Skill 内容、SQL 参数、异常 message → 均不进入 Span ✅

### 8. 4xx/5xx 使用格式受限 typed errorCode/retryable tag ✅

Global Handler + Bearer Filter 只向 HTTP Span 增加 `robothree.error_code`、`robothree.retryable`。异常 message/class/SQL/Token 不进入 Span ✅

### 9. Exporter failure/timeout 不阻断业务 ✅

`CentralTracingExporterFailureIntegrationTest` + `CentralTracingCollectorTimeoutIntegrationTest`：Exporter 失败/超时时 HTTP 与 Application 业务正常返回 ✅

### 10. Actuator 只暴露 health / WebMvc slice + Fixture 测试兼容 ✅

仅暴露 Actuator health endpoint。无 Tracer 的测试上下文通过条件装配兼容。WebMvc slice 测试不受影响 ✅

### 11. Contract/Schema/Fixture/V1-V5/fat jar Lombok 边界未变 ✅

| 资源 | MD5 | 状态 |
| --- | --- | --- |
| `openapi.yaml` | `5c2449dd...` | 未变 ✅ |
| V1-V5 | 全部一致 | 未变 ✅ |
| fat jar Lombok | BOOT-INF/lib 不含 | ✅ |

### 12. Alignment-2A/2B / CAS/Model / CGF-2 未超前 ✅

| 检查项 | 结果 |
| --- | --- |
| MyBatis-Plus | 不存在 ✅ |
| SQL Script / Flyway 移除 | 不存在 ✅ |
| 真实 CAS / DeepSeek | 不存在 ✅ |
| Production Profile / 双节点 Harness | 不存在 ✅ |
| CGF-2 | 不存在 ✅ |

## 结论

**P0=0 P1=0 P2=0 P3=0。Alignment-1B INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING。**

- Central 77/0/0 x2 + Testcontainers + Embedded PostgreSQL 全部独立重跑 ✅
- W3C Trace Context 传播 + 非法拒绝 + 48 路并发隔离 ✅
- OTLP 默认关闭 + Exporter 故障不阻断 ✅
- HTTP/Application/JDBC Span 固定边界 + 敏感数据不入 Span ✅
- X-RoboThree-Trace-Id 不入 Error Body ✅
- Contract/Schema/V1-V5/fat jar 边界未变 ✅
- 无超前 Alignment-2/CGF-2 ✅

**Alignment-1 建议关闭。Alignment-2A GATED，等待用户明确接受后进入。**
