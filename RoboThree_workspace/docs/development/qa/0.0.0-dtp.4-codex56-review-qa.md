# DTP-4 / Document Tool Pack Codex 5.6 复核验收报告

| 项目 | 结果 |
| --- | --- |
| RUN_ID | `2026-08-04-1847-version-0.0.0-dtp.4-codex56-review` |
| 验收模式 | 验收模式；只读检查与非破坏性测试 |
| 验收对象 | `0.0.0-dtp.4`，并复核 DTP-1～DTP-3 形成的三个只读 Document Tool |
| 测试环境 | macOS arm64；Node `v24.13.0`；pnpm `11.11.0` |
| Git 状态 | 主工作区未初始化 Git；按文件系统范围和全量门禁验收 |
| 证据目录 | `qa-reports/2026-08-04-1847-version-0.0.0-dtp.4-codex56-review/evidence/` |
| 最终结论 | **PASS — USER ACCEPTANCE PENDING** |

## 1. 验收范围

本轮独立复跑以下三个正式工具及共享运行链：

- `tool.document.pdf.extract_text`；
- `tool.document.xlsx.read`；
- `tool.document.docx.read`。

同时复核：Document Worker 进程边界、Core Tool Registry、Capability Lock、授权与
digest 漂移失败关闭、路径保护、零网络、取消/超时、资源清理、DTP-4 packaging audit
和完整工作区回归。

不包含：新增 parser 能力、Office 文件写入、OCR、旧版 `.doc/.xls`、PPTX、OS 级
sandbox、electron-builder/notarization 和真实安装包验收。

## 2. 实际执行结果

| 验收项 | 实际结果 |
| --- | --- |
| Workspace build | PASS |
| Document Worker 专项 | `21 files / 156 tests PASS` |
| Core Document Tool 回归 | `5 files / 29 tests PASS` |
| DTP-4 focused | `3 files / 17 tests PASS` |
| ESLint + Architecture boundary | PASS |
| `audit:dtp4` | PASS |
| Workspace full check（沙箱） | `22 failed / 916 passed`；失败均为 `listen EPERM 127.0.0.1` |
| Workspace full check（允许 loopback 后同命令复跑） | `143 files / 938 tests PASS + 3 smoke PASS` |

第一次完整门禁失败只发生在需要绑定本地回环端口的测试。相同命令在允许
`127.0.0.1` 的环境复跑后全部通过，因此判定为测试沙箱限制，不是产品缺陷。

## 3. 三工具功能复核

### 3.1 PDF 文本抽取

- 单页、多页选择、Unicode、空白页和旋转信息通过；
- 页码 locator、结果 digest 和输出预算通过；
- 错误扩展名、损坏/截断、加密样本和超限均失败关闭；
- 真实 parser worker 与 Core 子进程链通过。

### 3.2 XLSX 读取

- Sheet、used range、单元格、公式、日期及 hidden/veryHidden 可见性通过；
- Sheet/Cell locator、结果预算和行列/单元格限制通过；
- 损坏 ZIP、加密 entry、zip slip、重复 entry、active content 和解压比超限均失败关闭；
- 真实 parser worker 与 Core 子进程链通过。

### 3.3 DOCX 读取

- Heading、Paragraph、List、Table、Row、Cell 和 section locator 通过；
- 合并单元格、Unicode、块/文本/表格预算通过；
- DOCM、Macro、外部 relationship、危险 ZIP 路径、加密、损坏和解压比超限均失败关闭；
- 真实 parser worker 与 Core 子进程链通过。

## 4. 安全与运行时边界

- Workspace 路径穿越、绝对路径、Windows/UNC、null byte 与 symlink 外逃被拒绝；
- 文件读取采用 resolve/stat/open/fstat/realpath 身份校验与有界读取；
- Parser worker 禁止网络、stdio、嵌套 worker 与 child process；
- Document Worker 保持零网络依赖，日志/协议 canary 未泄漏路径或内容；
- 单并发、`worker_busy`、cancel、deadline、late callback 和 1000 次资源有界回归通过；
- Core 只加载精确注册、授权和锁定的三个 Tool；registry/digest 漂移失败关闭；
- crash-after-request 保留 dispatched Effect，不伪造确定失败；
- DTP-4 worker entry `realpath`/JS module 校验、最小 child env、Core shutdown 升级链和
  packaging drift audit 均通过。

## 5. 问题统计

| 等级 | 数量 |
| --- | ---: |
| P0 | 0 |
| P1 | 0 |
| P2 | 0 |
| P3 | 0 |

## 6. 风险与非目标

- 当前是 Node child process + worker thread 的受控隔离，不宣称 OS sandbox；
- 本轮使用结构化测试 Fixture，不等同于覆盖现实世界所有 Office/PDF 变体；
- Desktop 完整 E2E 以 PDF 验证共享链路，XLSX/DOCX 已在真实 Core → Document Worker
  子进程集成中验证；三者共用相同 Registry、Lock、Backend 和 Desktop 执行路径；
- 未重新执行删除/重装 `node_modules` 的 offline frozen install；该项已有 Claude Code
  独立 QA 记录，本轮用 lockfile/installed footprint audit 与实际 build/run 复核当前状态。

上述均为已声明边界，不构成 DTP-4 阻断问题。

## 7. 结论

三个 Document Tool 的解析正确性、失败关闭、安全边界、Core 接入、真实子进程执行、
资源清理和 DTP-4 运行/包装收口均有当前工作区复跑证据。建议：

```text
DTP-4：PASS/CLOSED
Document Tool Pack（DTP-0 → DTP-4）：PASS/CLOSED
```

本报告不代替用户验收，正式状态保持 `USER ACCEPTANCE PENDING`。
