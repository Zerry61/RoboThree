# DCF-1.2C Independent QA Report

| 项目 | 值 |
| --- | --- |
| RUN_ID | `2026-07-26-dcf.1.2c-qa` |
| 版本 | `0.0.0-dcf.1.2c` |
| 结论 | **INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING** |
| Node 全量 | 85 files / 534 tests PASS |
| 专项 | 4 files / 9 tests PASS（含 E2E） |
| 执行时间 | 2026-07-26 20:53 |
| P0/P1/P2/P3 | 0 |

## 执行环境

```text
Node.js：v24.13.0 (nvm) / pnpm：11.11.0 / macOS：Darwin 24.4.0
```

## 执行结果

| 命令 | 结果 |
| --- | --- |
| `pnpm run clean` | 9 paths cleaned ✅ |
| `pnpm run build` | TypeScript/Preload/Renderer PASS ✅ |
| `CI=true pnpm exec vitest run` | 85 files / **534 tests PASS** |
| Architecture boundary | PASS |
| ESLint | PASS |

## 20 项独立 QA 逐条验证

### 1. clean build 后完整重跑 Harness ✅

85 files / 534 tests PASS。Architecture boundary + ESLint + TypeScript/Preload/Renderer build 全部通过。

### 2. ephemeral delta 不入 SQLite/WAL/Snapshot/日志/durable cursor ✅

- `DesktopEphemeralEventBus`：`deliveryKind: "ephemeral"` ✅
- SQLite migration 10 无 delta 相关列 ✅
- ephemeral event 不写入 delivery store ✅
- 断线后主动丢弃 ✅

### 3. Assistant 临时 Message ID 与最终持久 Message ID 一致 ✅

`AgentLoopCoordinator` 预分配稳定 Message ID。`DurableAgentConversationWriter` 使用同一 ID 提交最终消息。

### 4. deltaSequence 重复/跳号/乱序/旧 runtimeInstance 不污染 UI ✅

Renderer 按 Message ID + runtimeInstanceId + deltaSequence 合并临时文本。重复/乱序/迟到/旧运行代 delta 不成为持久事实。收到 durable Message 或 reset 后清理临时投影。

### 5. message_committed 在 Message 持久提交后产生 ✅

`DurableAgentLoopStarter` 在模型流结束后追加 delivery。Message 先持久化 → 再发 `message_committed`。

### 6. Message 已提交但 delivery 未写入的崩溃 → 重启只补一条 ✅

恢复扫描从终态 Assistant Message 幂等补写 delivery。同 deliveryId 幂等。

### 7. 同 deliveryId 同内容幂等 / 不同内容 conflict ✅

`appendDelivery` 同 deliveryId + 同 digest → 幂等。不同 digest → typed conflict。Memory/SQLite 同语义。

### 8. Memory/SQLite 同一 delivery Conformance + close/reopen ✅

`submit-turn-persistence.conformance.test.ts` 覆盖双 variant + close/reopen。

### 9. migration 10 前向升级 + 不改写历史 migration ✅

Migration 10（`dcf_1_2c_durable_message_delivery`）：移除 `submit_turn_command_id` 单投递限制，允许一个 Turn 拥有 accepted + Message delivery。全局单调 sequence。不改写 V1-V9。

### 10. retention 有界 / sequence 不复用 / 窗口外 typed reset ✅

默认保留最近 2048 条 delivery。旧 cursor 明确 `retention_window_exceeded`。

### 11-12. 四种 replay reset + reset 后 Snapshot → replacementCursor ✅

| Reset 类型 | 触发条件 |
| --- | --- |
| `unknown_cursor` | ahead cursor 无对应 delivery |
| `retention_window_exceeded` | cursor 在保留窗口外 |
| `old_projection_generation` | Core 运行代变化 |
| `projection_cleaned` | 连接断开后 Projection 清理 |

reset 后：Main 重新读取 Snapshot → 使用 replacementCursor 建立新 SSE ✅

### 13. 断线退避 250ms→10s / jitter / 重连成功复位 ✅

`DesktopEventReconnectController`：`backoffMs = 250`，指数退避，10s 上限，含 jitter。重连成功后复位为 250ms。

### 14. eventId 去重集合有界 / durable-ephemeral 不混淆 cursor ✅

Memory/SQLite 使用 `Set<string>` 有界去重。ephemeral 不推进 cursor，durable event 独占 cursor。

### 15. 单 SSE / Heartbeat 不推进 cursor / 非法 frame 失败关闭 ✅

Core 私有 SSE 拒绝第二个并发连接。Heartbeat 不持久化、不推进 cursor。非法 frame → typed error。

### 16. SSE/AbortSignal/listener/timer/Core child/loopback port 清理 ✅

`core-private-http-server.ts`：`clearInterval(poll)`、`clearInterval(heartbeat)`、`request.once("close", cleanup)`、`server.close()` + `server.closeAllConnections()`。E2E 验证停止后 Core 子进程和 loopback 服务退出。

### 17. 首 delta 断线 → Snapshot → durable Message E2E 实际重跑 ✅

`dcf12c-real-process.e2e.test.ts`：1 test PASS。首个 delta 后主动断开 SSE → Agent 继续持久完成 → 读取 Snapshot → 从 durable cursor 重连 → 收到 `message_committed`。

### 18. Core unexpected exit 后 Main 新运行代恢复 / 不接受旧 delta ✅

Core 重启后 `runtimeInstanceId` 变化 → 旧 delta 被丢弃（`old_projection_generation`）。Main 以新运行代恢复。

### 19. DCF-1.2A/B / repair.1/2 / DCF-1.1 / KAF-0～5 全量回归 ✅

85 files / 534 tests 全量通过。

### 20. 无超前 DCF-1.3 / CGF-1.3 / 真实 Model/Tool / 第二 UI reducer ✅

| 检查项 | 结果 |
| --- | --- |
| DCF-1.3 | 未实现 ✅ |
| CGF-1.3 | 未实现 ✅ |
| 真实 Model Gateway | 未实现 ✅ |
| Tool/Skill | 未实现 ✅ |
| WebSocket/Phoenix | 不存在 ✅ |

## 结论

**P0=0 P1=0 P2=0 P3=0。DCF-1.2C INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING。**

- 85/534 clean+build+vitest 独立重跑 ✅
- ephemeral delta 不持久化 ✅
- Message ID 一致 + durable 收敛 ✅
- 四种 replay reset + Snapshot→cursor 恢复 ✅
- 断线退避 250ms→10s + jitter ✅
- 单 SSE + Heartbeat 不推进 cursor ✅
- SSE/timer/listener/子进程/端口 全部清理 ✅
- migration 10 前向升级 ✅
- E2E 首 delta 断线 → Snapshot → durable Message ✅
- 全量回归 + 无超前 ✅

**DCF-1.2 建议关闭。DCF-1.3 / CGF-1.3 GATED，等待用户明确接受后解锁。**
