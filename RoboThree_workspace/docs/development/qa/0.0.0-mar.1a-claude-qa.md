# 0.0.0-mar.1a — Claude Code 独立 QA 报告

## 基本信息

| 属性 | 内容 |
| --- | --- |
| RUN_ID | `2026-08-06-1832-version-0.0.0-mar.1a` |
| 验收对象 | MAR-1A：Desktop / Main file picker + Main-owned file guard + Catalog integration |
| 日期 | 2026-08-06 |
| 验收者 | Claude Code（独立 QA，只读） |
| 触发方式 | 用户显式指令（"MAR-1A 已完成实现并收口到 0.0.0-mar.1a，IMPLEMENTED / DEVELOPER_TESTED / INDEPENDENT QA PENDING"） |
| 环境 | Node v24.13.0 / pnpm 11.11.0 / vitest 4.1.10 |
| 开发版本 | Core / Desktop `0.0.0-mar.1a`；Contracts `0.0.0-mar.1.0`；Document Worker `0.0.0-dwo.1`；Root `0.0.0-dtp.4` |
| 证据目录 | `qa-reports/2026-08-06-1832-version-0.0.0-mar.1a/evidence/` |

治理说明：MAR-1.0 已正式 `PASS/CLOSED`。MAR-1A 是首个把 manual registration 接到**真实文件系统 picker** 的批次，本报告重点核查 Main-owned 文件守卫（realpath/lstat/stat/fstat 身份、symlink/hardlink/越界/超限/扩展名拒绝、64KiB streaming digest）、Renderer 零路径、active read_write grant 匹配，以及 catalog 接入 Desktop Artifact Panel。

---

## 一、门禁复跑结果（全部独立执行，未采信自测）

| # | 门禁 | 结果 | 与自测声明对比 |
| --- | --- | --- | --- |
| 1 | MAR-1A focused（ipc-router + api + presentation + workbench + task-projection + audit） | **PASS** 6 files / 40 tests | 一致 |
| 2 | Contracts 全量测试 | **PASS** 14 files / 87 tests | 一致 |
| 3 | Core 全量测试 | **PASS** 77 files / 588 tests | 一致（本次沙箱内直接通过） |
| 4 | Document Worker 全量 | **PASS** 22 files / 174 tests | 一致 |
| 5 | Desktop 全量测试 | **PASS** 23 files / 101 tests | 一致（本次沙箱内直接通过） |
| 6 | lint + architecture boundary | **PASS** | 一致 |
| 7 | `audit:dtp4` | **PASS**（Core/Desktop `0.0.0-mar.1a` 版本期望） | 一致 |
| 8 | offline frozen install | **PASS**（Already up to date） | 一致 |
| 9 | 完整 `pnpm run check` | **PASS** 150 files / 1021 tests ＋ 三项 smoke | 一致 |

回归基线：MAR-1.0 为 150 files / 1019 tests；本次 1021 tests（+2）。无既有用例回归。

---

## 二、静态核查（Desktop/Main file picker 安全边界，全部通过）

| 关注点 | 结论 | 证据 |
| --- | --- | --- |
| **Main 文件守卫** | ✅ | `resolveRegisterableWorkspaceFile`：`hasUnsafeAbsolutePathShape` 拒绝 → lstat 拒 symlink / 非 regular file / `nlink>1` hardlink / 超 `MAX_REGISTER_BYTES`(256MiB) → `realpath` + `stat` 的 `dev/ino` `sameFileIdentity` → `selectContainingAuthority`（active `read_write` grant realpath 包含性）→ 扩展白名单（pdf/xlsx/docx/md/markdown/txt/html/htm）→ safe relativePath（`desktop-ipc-router.ts:610-659`） |
| **64KiB streaming SHA-256** | ✅ | `hashStableFile`：open 后 `handle.stat()` before `sameFileIdentity` + 64KiB buffer 循环 `read` + `hash.update` 分块 + `offset===size` 完整性 + `handle.stat()` after `sameStableFile`（dev/ino/size/mtime 稳定）→ 文件在 hash 期间漂移 → `source_changed` typed failure；`finally` 关 handle，全分支无泄漏（L676-718） |
| **active read_write grant 匹配** | ✅ | picker 流程先 `listWorkspaceGrantAuthorities`，过滤 `status==="active" && accessMode==="read_write"`，无 writable grant → `workspace.selection_invalid`（L300-317） |
| **Main-only picker + Core 私有路由** | ✅ | Renderer 只发 pathless `register_workspace_artifact` command；Main 原生 file picker 仅主进程产生路径；Core 私有路由 `/v1alpha1/workspaces/private-authorities` + `/v1alpha1/artifacts/register` → `registerManualArtifact`（`core-private-http-server.ts:32,44,287-288`） |
| **Renderer/Preload/Shared 零敏感字段** | ✅ | `rootRealPath`/`fileSha256`/`selectedPath`/`selectionHandle`/`workspaceRoot`/`limits` 静态扫描零命中；Renderer 仅 `listArtifacts` 消费 catalog |
| **catalog 接入 Desktop Artifact Panel** | ✅ | Artifact Panel 调 `listArtifacts({ sourceKinds: ["workspace_file"] })` 渲染 metadata-only；manual Artifact 走既有 APV open/export/source delete 私有 authority（`main.ts:430-437`） |
| **无越界** | ✅ | `register_workspace_artifact` 未进入 Document Worker / Tool Registry；无 MAR-1B preview / parser 实现；Contracts / Document Worker / Central / `pnpm-lock.yaml` / 根 `package.json` / 根 `tsconfig.json` 未改 |

---

## 三、发现与分级

### P0 = 0，P1 = 0，P2 = 0，P3 = 0

无代码级、治理级或越界发现。无环境阻塞（本次所有全量门禁在沙箱内直接通过）。

---

## 四、总体结论

```text
结论：PASS（P0 = 0 / P1 = 0 / P2 = 0 / P3 = 0）
```

- MAR-1A 的 Desktop/Main file picker 安全边界达标：Main-owned 文件守卫（realpath/lstat/stat/fstat 身份、symlink/hardlink/越界/超限/扩展名拒绝）、64KiB streaming SHA-256（前后身份稳定校验 + handle 关闭）、active `read_write` grant 匹配、Core 私有注册路由、global workspace_file catalog 接入 Artifact Panel。
- Renderer/Preload/Shared 全程零路径/零文件事实，文件只在 Main 与 Core 私有边界流动。
- 无 MAR-1B preview / bulk / drag-drop / Tool 注册越界，Contracts/DW/Central/lockfile/根配置未改。
- 全部门禁独立复跑通过，与自测声明一致。
- **MAR-1A 可进入用户接受流程。**

## 五、建议的下一步

1. 用户接受 MAR-1A QA 后正式关闭 MAR-1A。
2. 单独授权 MAR-1B（bounded workspace-file text/markdown preview，HTML 走 APV-1C 沙箱）。
3. bulk registration、drag/drop path、overwrite、OS Sandbox、formal installer 保持 GATED。

— Claude Code（独立 QA，只读，未修改任何生产代码）
