# 0.0.0-kaf.2.1 — Claude Code Independent QA Report

| 项目 | 值 |
| --- | --- |
| RUN_ID | `2026-07-20-1500-version-0.0.0-kaf.2.1` |
| 报告路径 | `${CODE_ROOT}/docs/development/qa/0.0.0-kaf.2.1-claude-qa.md` |
| 证据目录 | `${CODE_ROOT}/qa-reports/2026-07-20-1500-version-0.0.0-kaf.2.1/evidence/` |
| 验收模式 | 验收模式 |
| 前序报告 | `docs/development/qa/0.0.0-kaf.1.1-claude-qa.md`（`PASS`） |
| 验收对象 | `0.0.0-kaf.2.1` |
| Contract Version | `v1alpha1`（新增 Persistence 子集） |
| 架构基线 | `KN-006`、`ADR-005 ACCEPTED`、`ADR-007 ACCEPTED` |
| Claude Code 版本 | `2.1.207`（≥ 2.1.196 ✓） |
| Git 状态 | 未初始化 |
| Node 版本 | **v24.13.0**（符合 .node-version ✓；通过 nvm install 临时安装） |
| pnpm 版本 | 11.11.0 / TypeScript 5.9.3 / Vitest 4.1.10 |
| 测试开始 | 2026-07-20 18:21:09 CST |
| 测试结束 | 2026-07-20 18:21:32 CST |

> **P3-ENV-001 关闭**：本次 QA 在 Node 24.13.0 上执行。pnpm 无 engine 警告。DEVELOPMENT-LOG 已知缺口中的 `"Node 22 的 P3-ENV-001 已可能因 Node 24 专用 API 变成实际兼容阻断"` 已解除。

## 1. 执行摘要

- 验收对象：`0.0.0-kaf.2.1`；scope = `version:0.0.0-kaf.2.1`。
- 自测命令独立复跑：`pnpm install --frozen-lockfile && pnpm run clean && pnpm run check` → exit 0。
- 基线结果：**11 files / 73 tests / Architecture boundary checks passed / Core smoke: ready**。
- Node：v24.13.0（首次在 QA 环境达标；无 engine warning）。
- `ExperimentalWarning: SQLite`：DEVELOPMENT-LOG 已声明为已接受风险，不计为问题。
- 15 项"独立 QA 建议范围"：**全部覆盖**。
- KAF-0/KAF-1 回归：45 个已有测试 + boundary + smoke 全部通过。
- **结论**：**PASS**

## 2. 问题统计

| 等级 | 数量 |
| --- | --- |
| P0 | 0 |
| P1 | 0 |
| P2 | 0 |
| P3 | 0 |

> P3-ENV-001（Node 22 vs 24 环境差异）已在此次 QA 中通过使用 Node 24.13.0 关闭。

## 3. 自测命令独立复跑

| 命令 | Exit | 结果 | 证据 |
| --- | --- | --- | --- |
| `pnpm install --frozen-lockfile` | 0 | Already up to date（无 engine warning） | `full-check.log` |
| `pnpm run clean` | 0 | `node scripts/clean.mjs` 成功 | `full-check.log` |
| `pnpm run check`（整体） | 0 | lint + test 11/73 + smoke 串行全通过 | `full-check.log` |
| `pnpm run typecheck` | 0 | 通过 | `A3-typecheck.log` |
| `pnpm run lint` | 0 | Architecture boundary checks passed | `A4-lint.log` |
| `pnpm run test` | 0 | 11 files / 73 tests | `A6-vitest.log` |

**不采信证明**：DEVELOPMENT-LOG 自测结果"11 files / 73 tests / boundaries passed / smoke ready"五项指标均在 Node 24.13.0 上独立复跑通过。

## 4. DEVELOPMENT-LOG 15 项"独立 QA 建议范围"覆盖

| # | 建议项 | 结论 | 关键证据 |
| --- | --- | --- | --- |
| 1 | ADR-007 七项冻结项是否与 KAF-2.1 一致且未进入 2.2/2.3 | **PASS** | `task-persistence.ts` Port 仅含 7 个语义方法（createTask/loadTask/loadCheckpoint/findCommandReceipt/commitAcceptedCommand/commitRejectedCommand/listRecoveryCandidates）；不存在 tail replay/dispatcher/recovery coordinator（与已知缺口一致） |
| 2 | Contracts 拒绝非 JSON / 非法 digest / Event sequence 0 / 不一致 Checkpoint/Receipt/Effect/Outbox | **PASS** | `persistence-contracts.test.ts`：canonicalization 逐键排序但不重排数组 / 拒绝函数/undefined / eventSequence positive / Effect success 必须有 result / Outbox 时间戳校验 |
| 3 | canonical JSON 跨对象键顺序稳定且不改变数组顺序 | **PASS** | `digest.ts` 使用 `canonicalJsonStringify` + SHA-256；`persistence-contracts.test.ts` "canonicalizes object keys recursively without reordering arrays" |
| 4 | InMemory 与 SQLite 运行同一 Conformance Suite | **PASS** | `task-persistence.conformance.test.ts` 使用 `describe.each([InMemory, SQLite])` 运行 **17 个相同测试**（`variant.name` 参数化） |
| 5 | revision 0 Task 同 digest 幂等、different digest 冲突 | **PASS** | conformance test "creates revision zero idempotently and detects conflicting initialization" |
| 6 | accepted Command 原子写入 Event/Checkpoint/head/Receipt/Outbox | **PASS** | `sqlite-task-persistence.ts` `commitAcceptedCommand` 在 `BEGIN IMMEDIATE` 事务内写 5 个表；conformance test "atomically commits an accepted command and replays the same command" |
| 7 | 重复 commandId same digest 不重复写，different digest 冲突 | **PASS** | conformance test "rejects command id reuse with another digest" |
| 8 | rejected Receipt 幂等且不改变 Task revision | **PASS** | conformance test "persists rejected command receipts idempotently without changing task revision" |
| 9 | expected revision / event sequence / checkpoint parent 稳定拒绝过期或跳号写 | **PASS** | conformance test "rejects event sequence gaps" + "rejects optimistic revision conflicts without partial writes" + "rejects checkpoint digest mismatch before writing" |
| 10 | Outbox 唯一性失败让整笔 SQLite 事务回滚 | **PASS** | conformance test "rolls back the whole accepted transaction when outbox uniqueness fails" |
| 11 | SQLite close/reopen 后 Contract 完整往返且 migration 不重复 | **PASS** | `sqlite-persistence.integration.test.ts` "reopens a file database without rerunning migrations and preserves committed state"（关闭后重新打开 `SqliteTaskPersistence`，验证 CommandReceipt 完整保留） |
| 12 | 较新 schema / 缺表缺列 / integrity/foreign key fail closed | **PASS** | sqlite integration test "fails closed when the database schema is newer than the binary" + "fails closed when a claimed current schema is missing required tables"；`schema-preflight.ts:15-18` `current > LATEST → throw` + `verifyRequiredSchema` 检查表存在 |
| 13 | SQLite 只存在于 Adapter，Kernel/Contracts 边界仍通过 | **PASS** | `check-boundaries.mjs` 通过（kernel 规则 `required:true`；contracts 规则 `required:true`）；`node:sqlite` import 只在 `services/core/src/adapters/sqlite/` 内；`packages/contracts/` 无 `node:` import |
| 14 | KAF-0/KAF-1 的 45 项测试和 Core smoke 无回归 | **PASS** | 完整 73 tests 含原 45 个全部通过 |
| 15 | Node 24.13.0 下 `pnpm install --frozen-lockfile && pnpm run clean && pnpm run check` 完整通过 | **PASS** | 本次 QA 全量在 Node 24.13.0 执行；`full-check.log` exit 0 |

## 5. 代码结构核查

### 5.1 新增文件

| 层 | 文件 | 角色 |
| --- | --- | --- |
| Contracts | `packages/contracts/src/persistence/task-event.ts` | TaskEvent Contract（eventId / taskId / type / revision / timestamp / payload） |
| Contracts | `packages/contracts/src/persistence/task-checkpoint.ts` | TaskCheckpoint Contract（checkpointId / taskId / revision / state / digest / parentDigest） |
| Contracts | `packages/contracts/src/persistence/command-receipt.ts` | CommandReceipt Contract（accepted / rejected discriminated union） |
| Contracts | `packages/contracts/src/persistence/effect-attempt.ts` | EffectAttempt Contract |
| Contracts | `packages/contracts/src/persistence/outbox.ts` | OutboxRecord Contract |
| Contracts | `packages/contracts/src/persistence/common.ts` | Sha256Digest / canonicalJson / 共享类型 |
| Core Port | `services/core/src/ports/task-persistence.ts` | 语义化 TaskPersistence Port（7 个方法，无 CRUD） |
| Core Persistence | `services/core/src/persistence/digest.ts` | SHA-256 canonical JSON digest |
| Core Persistence | `services/core/src/persistence/validation.ts` | validateAcceptedCommit / validateTaskCreation / validateRejectedReceipt / validateCommitAgainstCurrent |
| Core Adapter | `services/core/src/adapters/memory/in-memory-task-persistence.ts` | InMemory 实现 |
| Core Adapter | `services/core/src/adapters/sqlite/sqlite-task-persistence.ts` | SQLite 实现（DatabaseSync + enableDefensive + migrateAndPreflight） |
| Core Adapter | `services/core/src/adapters/sqlite/migrations.ts` | Forward-only migration 数组 |
| Core Adapter | `services/core/src/adapters/sqlite/schema-preflight.ts` | WAL/foreign_keys/busy_timeout/synchronous/migration/preflight |

### 5.2 测试文件（3 个新增）

| 文件 | 测试数 | 范围 |
| --- | --- | --- |
| `packages/contracts/tests/persistence-contracts.test.ts` | 5 | canonical JSON / non-JSON reject / event sequence / effect / outbox |
| `services/core/tests/task-persistence.conformance.test.ts` | 17 | InMemory + SQLite 同套 Conformance Suite |
| `services/core/tests/sqlite-persistence.integration.test.ts` | ~5 | close/reopen / newer schema fail / missing tables fail |

### 5.3 SQLite 运行时配置（schema-preflight.ts）

| 配置 | 值 | 目的 |
| --- | --- | --- |
| `journal_mode` | WAL | 并发读 |
| `foreign_keys` | ON | 引用完整性（Outbox → Task Head FK） |
| `busy_timeout` | 5000ms | 短时锁等待 |
| `synchronous` | FULL | Commit 后 fsync |
| `enableDefensive` | true | 禁止 PRAGMA / shadow table 篡改 |

`ExperimentalWarning` 是 `node:sqlite` 的 Node 24 已知行为，DEVELOPMENT-LOG 已声明接受。

## 6. 验收矩阵

| 编号 | 项目 | 结论 | 证据 |
| --- | --- | --- | --- |
| A1 | 依赖干净安装 | PASS | `full-check.log`（无 engine warning） |
| A2 | 编译 / typecheck | PASS | `A3-typecheck.log` |
| A3 | Lint + 边界规则 | PASS | `A4-lint.log` |
| A4 | 格式检查 | PASS | `A4-lint.log` |
| A5 | 单元测试 | PASS | `A6-vitest.log`（11/73） |
| A6 | Core smoke | PASS | `full-check.log`：`{"status":"ready"}` |
| L11-L18 | DEVELOPMENT-LOG 联动 | PASS | 15/15 QA 项 + 回链 |

## 7. 已知缺口确认

DEVELOPMENT-LOG 声明的缺口全部未突破：

| 缺口 | 状态 |
| --- | --- |
| 无 DurableTaskRuntime / tail replay / Outbox Dispatcher / Effect 状态写入 / Recovery Coordinator | ✅ 未实现 |
| TaskPersistence 暂未暴露 Event tail / pending Outbox / Effect 查询 | ✅ 未提前添加 |
| 完整 Checkpoint 写放大未做性能基准 | ✅ KAF-4 前不调整 |
| `node:sqlite` ExperimentalWarning | ✅ 运行时已知，不影响测试 |
| SQLite 同步 API 仅短事务 + 单写入口 | ✅ 当前实现一致 |

## 8. 上游参考验证

| 登记 | 来源 | 采用方式 | 验证 |
| --- | --- | --- | --- |
| UR-004 / AR-007 | OpenHands | append-only Event / 稳定 ID / 重复拒绝 → SQLite Task Event Contract | ✅ 无 Python 源码 |
| UR-005 / AR-008 | LangGraph | Checkpoint Port / SQLite Saver / 幂等 / Conformance | ✅ 无 Python 源码 |
| UR-009 / AR-009 | OpenClaw | Node SQLite / schema preflight / Migration / 单写事务 | ✅ 无 Rust 源码 |

全部 `DESIGN_ONLY` TypeScript 重写，未复制上游 SQL schema。

## 9. 发布结论

| 维度 | 判断 |
| --- | --- |
| P0 | 0 |
| P1 | 0 |
| P2 | 0 |
| P3 | 0（P3-ENV-001 已通过 Node 24.13.0 关闭） |
| 15/15 QA 项覆盖 | ✅ |
| KAF-0/KAF-1 回归 | ✅ 45 tests |
| **最终结论** | **PASS** |

理由：KAF-2.1 在 Node 24.13.0 上全量通过；Persistence Contract 语义清晰拒绝 CRUD pattern；InMemory/SQLite 运行同一 Conformance Suite；原子写入/幂等/冲突检测/回滚/migration preflight/close-reopen 均验证通过。建议发布，进入 KAF-2.2 Durable Command Pipeline。

## 附录 A：证据清单

| 文件 | 内容 |
| --- | --- |
| `evidence/logs/full-check.log` | `pnpm install + clean + check` 完整输出（Node 24.13.0 / 11/73 / no engine warning） |
| `evidence/logs/A3-typecheck.log` | `pnpm run typecheck` |
| `evidence/logs/A4-lint.log` | `pnpm run lint`（boundaries passed） |
| `evidence/logs/A6-vitest.log` | `pnpm run test`（11 files / 73 tests） |

## 附录 B：自检（SKILL.md §7）

- [x] 报告落在 `docs/development/qa/0.0.0-kaf.2.1-claude-qa.md`
- [x] 证据归档到 `qa-reports/<RUN_ID>/evidence/`
- [x] 15/15 QA 项全部覆盖
- [x] Node 24.13.0 环境达标（P3-ENV-001 关闭）
- [x] 未修改任何业务代码
- [x] 发布结论 PASS
