# 0.0.0-apv.1c — Claude Code 独立 QA 报告

## 基本信息

| 属性 | 内容 |
| --- | --- |
| RUN_ID | `2026-08-05-1703-version-0.0.0-apv.1c` |
| 验收对象 | APV-1C：HTML Preview Sandbox（127.0.0.1-only server + CSP + lifecycle） |
| 日期 | 2026-08-05 |
| 验收者 | Claude Code（独立 QA，只读） |
| 触发方式 | 用户显式指令（"APV-1C 已完成实现与自测，IMPLEMENTED / SELF-TEST PASS / INDEPENDENT QA PENDING"） |
| 环境 | Node v24.13.0 / pnpm 11.11.0 / vitest 4.1.10 |
| 开发版本 | Contracts / Core / Desktop `0.0.0-apv.1c`；Root 保持 `0.0.0-dtp.4`；Document Worker `0.0.0-dwe.2` |
| 证据目录 | `qa-reports/2026-08-05-1703-version-0.0.0-apv.1c/evidence/` |

治理说明：APV-1B 已正式 `PASS/CLOSED`。APV-1C 是 APV 系列风险最高的批次（本地 HTML preview server + 沙箱生命周期），本报告逐项核查 127.0.0.1-only、CSP、token/路径拒绝、清理收口与 Renderer iframe 边界。

---

## 一、门禁复跑结果（全部独立执行，未采信自测）

| # | 门禁 | 结果 | 与自测声明对比 |
| --- | --- | --- | --- |
| 1 | APV-1C focused（contracts + sandbox + security-boundary + api + ipc + workbench + audit） | **PASS** 7 files / 42 tests | 一致（本次沙箱内直接通过，未遇 EPERM） |
| 2 | Contracts 全量测试 | **PASS** 14 files / 82 tests | 一致 |
| 3 | Core 全量测试 | **PASS** 77 files / 576 tests | 一致 |
| 4 | Document Worker 全量 | **PASS** 22 files / 168 tests | 一致 |
| 5 | Desktop 全量测试 | **PASS** 23 files / 91 tests | 一致 |
| 6 | lint + architecture boundary | **PASS** | 一致 |
| 7 | `audit:dtp4` | **PASS**（Contracts/Core/Desktop `0.0.0-apv.1c` 版本期望） | 一致 |
| 8 | offline frozen install | **PASS**（Already up to date） | 一致（本机 store 完整） |
| 9 | 完整 `pnpm run check` | **PASS** 150 files / 986 tests ＋ 三项 smoke | 一致 |

回归基线：APV-1B 为 148 files / 978 tests；本次 986 tests（+8）。无既有用例回归。

---

## 二、静态核查（核心关注点全部通过）

| 关注点 | 结论 | 证据 |
| --- | --- | --- |
| **127.0.0.1-only 绑定** | ✅ | `server.listen(0, "127.0.0.1")`（`html-preview-sandbox.ts:75`）；Contracts superRefine 强制 `previewUrl` 为 `http://127.0.0.1` 且无凭据（`task.ts:266-280`） |
| **deny-by-default CSP** | ✅ | `default-src 'none'` + `script-src/object-src/base-uri/form-action/frame-ancestors/connect-src/img-src/media-src/font-src/style-src` 全 `'none'`（L16-28）；响应头与文档 meta 双重下发 |
| **tokenized URL + 请求拒绝** | ✅ | URL 三段精确匹配 `previewSessionId/token/index.html`，否则 404；`Host` 头必须 `127.0.0.1:{port}` 否则 403；仅 GET/HEAD 否则 405；点文件/null byte/遍历拒绝；`realpath` 包含性 + `basename === "index.html"` + 大小 ≤256KiB 校验 |
| **响应安全头** | ✅ | `Content-Security-Policy` + `X-Content-Type-Options: nosniff` + `Cache-Control: no-store` + `Cross-Origin-Resource-Policy: same-origin`（L191-197） |
| **清理与资源收口** | ✅ | 默认 TTL 5min → `close()`（clearTimeout + server.close + `rm` temp dir）；`closeAll()` 供 app 关闭；启动失败（未绑定 TCP/写文件失败）即清理；temp 文件 `wx` no-clobber 创建 |
| **资源收敛测试** | ✅ | `html-preview-sandbox.test.ts` 100 周期循环断言 `activeSessionCount/activeServerCount/activeTimerCount` 全归零、`cleanupCount=100`；deniedRequestCount 断言 |
| **Renderer iframe 安全边界** | ✅ | iframe `sandbox=""`（最严格，禁 script/same-origin/form/popup/top-navigation 等）+ `referrerpolicy="no-referrer"` + 仅加载 tokenized loopback `previewUrl`（`main.ts:747-751`） |
| **Renderer 危险 API 零命中** | ✅ | `innerHTML`/`v-html`/`document.write`/`eval`/`new Function`/`shell.open`/`openPath`/`fetch`/`ipcRenderer`/`node:fs` 静态扫描零命中 |
| **无越界** | ✅ | 无 `artifact.preview`/`tool.artifact` Tool 注册；无 APV-2/overwrite/OS Sandbox/formal installer 实现；Document Worker src / Central / `pnpm-lock.yaml` / 根 `package.json` / 根 `tsconfig.json` 未改 |

---

## 三、发现与分级

### P0 = 0，P1 = 0，P2 = 0，P3 = 0

无代码级、治理级或越界发现。无环境阻塞（本次所有全量门禁在沙箱内直接通过）。

---

## 四、总体结论

```text
结论：PASS（P0 = 0 / P1 = 0 / P2 = 0 / P3 = 0）
```

- APV-1C 的 HTML 预览沙箱安全边界达标：server 仅绑定 127.0.0.1，tokenized URL 三层校验，deny-by-default CSP（全 'none'），Host/方法/路径/点文件/遍历/大小逐项拒绝，响应带 nosniff/no-store/CORP，TTL + close + closeAll 三路清理且 100 周期资源收敛测试通过。
- Renderer 仅用空 `sandbox=""` iframe + no-referrer + tokenized loopback URL，全程无 innerHTML/v-html/fetch/ipcRenderer/node:fs 等危险 API。
- Contracts 层强制 previewUrl loopback-only，与实现双保险。
- 无 `artifact.preview` Tool 注册，无 APV-2/overwrite/OS Sandbox/formal installer 越界，Document Worker/Central/lockfile/根配置未改。
- 全部门禁独立复跑通过，与自测声明一致。
- **APV-1C 可进入用户接受流程。**

## 五、建议的下一步

1. 用户接受 APV-1C QA 后正式关闭 APV-1C，APV 系列（APV-0 → APV-1C）整体收口。
2. 单独授权 APV-2（文件生命周期扩展：open location、delete/pin/export、manual registration）后再进入。
3. overwrite / bulk overwrite、OS Sandbox、formal installer 保持 GATED。

— Claude Code（独立 QA，只读，未修改任何生产代码）
