# 0.0.0-dwo.3 — Claude Code 独立 QA 报告

## 基本信息

| 属性 | 内容 |
| --- | --- |
| RUN_ID | `2026-08-06-1612-version-0.0.0-dwo.3` |
| 验收对象 | DWO-3：XLSX Overwrite Desktop Confirmation / Product E2E |
| 日期 | 2026-08-06 |
| 验收者 | Claude Code（独立 QA，只读） |
| 触发方式 | 用户显式指令（"DWO-3 开发已完成，状态 IMPLEMENTED / DEVELOPER_TESTED / INDEPENDENT QA PENDING"） |
| 环境 | Node v24.13.0 / pnpm 11.11.0 / vitest 4.1.10 |
| 开发版本 | Core / Desktop `0.0.0-dwo.3`；Document Worker `0.0.0-dwo.1`；Contracts `0.0.0-apv.3b`；Root `0.0.0-dtp.4` |
| 证据目录 | `qa-reports/2026-08-06-1612-version-0.0.0-dwo.3/evidence/` |

治理说明：DWO-2 已正式 `PASS/CLOSED`。DWO-3 是 DWO 系列收口批次（Desktop 确认 UI、scripted overwrite 意图、pending ToolCallBatch 恢复、真实产品 E2E）。本报告重点核查确认材料不泄漏、首次确认材料的 drift 绑定、恢复闭环，以及不越界到 gated 能力。

---

## 一、门禁复跑结果（全部独立执行，未采信自测）

| # | 门禁 | 结果 | 与自测声明对比 |
| --- | --- | --- | --- |
| 1 | DWO-3 E2E（dwe3 含 overwrite 5 tests + dtp3b + context） | **PASS** 3 files / 11 tests（本次沙箱内直接通过） | 一致 |
| 2 | Document Worker 全量 | **PASS** 22 files / 174 tests | 一致 |
| 3 | Core 全量测试 | **PASS** 77 files / 584 tests | 一致 |
| 4 | Desktop 全量测试 | **PASS** 23 files / 99 tests | 一致 |
| 5 | lint + architecture boundary | **PASS** | 一致 |
| 6 | `audit:dtp4` | **PASS**（Core/Desktop `0.0.0-dwo.3` 版本期望） | 一致 |
| 7 | offline frozen install | **PASS**（Already up to date） | 一致 |
| 8 | 完整 `pnpm run check` | **PASS** 150 files / 1014 tests ＋ 三项 smoke | 一致 |

回归基线：DWO-2 为 150 files / 1009 tests；本次 1014 tests（+5，全部为 DWO-3 overwrite E2E 用例）。无既有用例回归。

---

## 二、静态核查（DWO-3 收口，全部通过）

| 关注点 | 结论 | 证据 |
| --- | --- | --- |
| **scripted model overwrite 意图** | ✅ | `isOverwriteIntent`（英文 `overwrite`/`replace` + 中文 `覆盖`/`替换`）→ `mode=overwrite_existing`；普通 create 不带 mode，保持 DWE-3 兼容（`desktop-document-scripted-model-provider.ts:131,149-151`） |
| **确认投影不泄漏私有材料** | ✅ | `confirmedOldSha256` 不出现在任何 Desktop 投影/command 面；Renderer 的 `requestDigest` 命中为确认请求自身 digest（确认绑定回显，`main.ts:761`），非 overwrite 私有 requestDigest；无 workspaceRoot/workbook 泄漏 |
| **首次确认材料绑定（drift 防护）** | ✅ | `pendingXlsxOverwriteConfirmations` 进程私有 Map 按 task/toolCall/action 绑定；首次确认请求构建 `buildXlsxOverwriteConfirmationMaterial` 并缓存，确认后 dispatch 复用**同一** `confirmedOldSha256`（`create-desktop-private-runtime.ts:525-550`）；目标漂移 → Worker 用原 confirmedOldSha256 失败关闭（`target_digest_changed`），不覆盖漂移文件 |
| **恢复闭环** | ✅ | 确认决策接 durable ToolCallBatch 恢复，确认后先 recover pending tool-call 再恢复 normal Agent loop；recover 不再等待确认时按 task 清理材料 |
| **Renderer 确认 UI** | ✅ | 确认弹窗复用 consequenceSummary + "确认后只允许执行卡片中描述的这一项操作" no-undo 文案（`main.ts:752`） |
| **E2E 覆盖** | ✅ | 确认后真实 Worker overwrite（含文件可读断言 + 无 temp 残留）+ 确认期间目标漂移 → failed Tool Result（"XLSX overwrite target digest changed"）+ 目标文件不被覆盖（`dwe3-xlsx-write-productization.e2e.test.ts:168,239`） |
| **无越界** | ✅ | Renderer 敏感字段（workspaceRoot/rootRealPath/confirmedOldSha256/workbook）零命中；无 bulk overwrite / manual registration / OS Sandbox / formal installer / APV-3C 实现；`DW_DIAGNOSTIC` 零命中；Contracts / Document Worker / Central / `pnpm-lock.yaml` / 根 `package.json` / 根 `tsconfig.json` 未改 |

---

## 三、发现与分级

### P0 = 0，P1 = 0，P2 = 0，P3 = 0

无代码级、治理级或越界发现。无环境阻塞（本次所有全量门禁在沙箱内直接通过）。

### 已接受残余风险（记录于条目，非新发现）

- pending confirmation material cache 为进程私有：Core 在确认等待期间重启 → 失败关闭或要求用户重发（已文档化）。
- CAS 仍为 DWO-1 Option A（advisory lock + digest best-effort）：平台级 atomic compare-and-replace 属后续独立 hardening（已文档化，DWO-0 评审已接受 Option A）。

---

## 四、总体结论

```text
结论：PASS（P0 = 0 / P1 = 0 / P2 = 0 / P3 = 0）
```

- DWO-3 的 Desktop 确认 UI / scripted overwrite 意图 / pending ToolCallBatch 恢复 / 产品 E2E 收口正确：确认材料不泄漏、首次确认材料的 drift 绑定（确认后漂移 → failed 不覆盖）、恢复闭环（确认后先 recover pending tool-call 再恢复 normal Agent loop）、no-undo 确认文案。
- E2E 覆盖确认后真实 Worker overwrite 与确认期间 drift 失败场景，且目标文件不被覆盖。
- 无越界到 gated 能力，无环境阻塞。
- **DWO-3 可进入用户接受流程；DWO 系列（DWO-0 → DWO-3）整体可收口。**

## 五、建议的下一步

1. 用户接受 DWO-3 QA 后正式关闭 DWO-3，DWO 系列整体 `PASS/CLOSED`；Document Tool 全栈（DTP + DWE + APV + DWO）收口。
2. bulk overwrite、manual artifact registration、OS Sandbox、formal installer、APV-3C 保持 GATED，需单独方案与授权。

— Claude Code（独立 QA，只读，未修改任何生产代码）
