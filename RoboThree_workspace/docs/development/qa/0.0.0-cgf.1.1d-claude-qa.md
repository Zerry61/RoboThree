# CGF-1.1D Independent QA Report

| 项目 | 值 |
| --- | --- |
| RUN_ID | `2026-07-25-cgf.1.1d-qa` |
| 版本 | `0.0.0-cgf.1.1d` |
| 结论 | **PASS** |
| Node | 56 files / 404 tests PASS |
| Java | **50 tests / 0 failures / 0 errors / 0 skipped** (online + offline) |
| JDK | OpenJDK 21.0.12 (Homebrew) |
| Docker | ✅ Testcontainers `postgres:16-alpine` 实际执行 |
| PostgreSQL Embedded | ✅ PostgreSQL 16.14 Embedded 实际执行 |
| P0/P1/P2/P3 | 0 |

## 执行环境

```text
Node.js：v24.13.0 (nvm)
pnpm：11.11.0
Java：OpenJDK 21.0.12 / JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home
Docker Engine：29.6.2
PostgreSQL：16-alpine (Testcontainers) + 16.14 (Embedded)
macOS：Darwin 24.4.0
```

## 执行结果

| 命令 | 结果 | 证据 |
| --- | --- | --- |
| `CI=true pnpm run check` | 56 files / **404 tests PASS** | `evidence/logs/full-check.log` |
| `pnpm run check:central` | BUILD SUCCESS, **50/0/0** | `evidence/logs/check-central.log` |
| `pnpm run check:central:offline` | BUILD SUCCESS, **50/0/0** | `evidence/logs/check-central-offline.log` |

Architecture boundary checks：PASS
Core smoke：ready
Desktop smoke：ready / fixtureOnly=true

## Java 测试明细

| 测试类 | 测试数 | 类型 |
| --- | --- | --- |
| FoundationApplicationSmokeTest | 1 | 回归 |
| FoundationFixtureConformanceTest | 1 | 回归 |
| FoundationFixtureControllerTest | 2 | 回归 |
| TrustedConfigurationSeederTest | 1 | 回归 |
| EnterpriseContractConformanceTest | 7 | 回归 |
| FoundationFakesTest | 1 | 回归 |
| Cgf11aFakesTest | 3 | 1.1A 回归 |
| **PostgreSqlCentralPersistenceIntegrationTest** | **2** | ✅ +1 CGF-1.1D 全链 (Docker Testcontainers) |
| InMemoryCentralPersistenceTest | 2 | 回归 |
| **EmbeddedPostgreSqlCentralPersistenceIntegrationTest** | **4** | ✅ +1 CGF-1.1D 全链 (Embedded PostgreSQL) |
| EnterpriseIdentityControllerTest | 3 | 1.1B 回归 |
| EnterpriseAccessTokenAndConfigurationControllerTest | 3 | 1.1C 回归 |
| EnterpriseIdentityApplicationTest | 9 | 1.1B 回归 |
| AccessTokenAndConfigurationApplicationTest | 11 | 1.1C 回归 |
| **合计** | **50** | **0 failures / 0 errors / 0 skipped** |

## 新增 CGF-1.1D 测试

**Cgf11dPostgreSqlRecoveryConformance**（565 行，`src/test/java/.../persistence/`）：

由 `PostgreSqlCentralPersistenceIntegrationTest`（Docker Testcontainers, 2 tests）和 `EmbeddedPostgreSqlCentralPersistenceIntegrationTest`（Embedded PostgreSQL, 4 tests）共同执行。同一 Harness 在两条 PostgreSQL 路径上实际运行。

## 14 项独立 QA 建议逐条验证

### 1. Node 24.13.0 / Java 21 / Docker 全量重跑

| 门禁 | 结果 |
| --- | --- |
| `CI=true pnpm run check` | 56 files / **404 tests PASS** |
| `pnpm run check:central` | **50 tests / 0 failures / 0 skipped / BUILD SUCCESS** |
| `pnpm run check:central:offline` | **50 tests / 0 failures / 0 skipped / BUILD SUCCESS** |

✅ **PASS** — 三门禁独立重跑，全量通过。

### 2. Docker / Embedded 全链用例 0 skipped

| 测试类 | 测试数 | 后端 | 状态 |
| --- | --- | --- | --- |
| `PostgreSqlCentralPersistenceIntegrationTest` | 2 (含 CGF-1.1D 全链) | Testcontainers `postgres:16-alpine` | ✅ 实际执行 |
| `EmbeddedPostgreSqlCentralPersistenceIntegrationTest` | 4 (含 CGF-1.1D 全链) | PostgreSQL 16.14 Embedded | ✅ 实际执行 |

✅ **PASS** — 两条 PostgreSQL 路径均实际执行 CGF-1.1D 全链恢复矩阵，0 skipped。

### 3. 全链 Fake OA Identity → Configuration Read

`Cgf11dPostgreSqlRecoveryConformance.verify()`（行 74-204）贯通完整链路：

```text
FakeOAIdentityAdapter.verify(OA_FLOW_MATERIAL)
→ insert DeviceEnrollmentGrant (code digest)
→ issue DeviceChallenge (DEVICE_ENROLLMENT purpose)
→ signed DeviceProof (ES256)
→ ManualDeviceEnrollmentService.enroll()
→ save configuration.read + model.use Permission
→ issue DeviceChallenge (TOKEN_ISSUANCE purpose)
→ signed DeviceProof
→ RoboThreeAccessTokenService.issue() (四因素 Token)
→ TrustedConfigurationSeeder.seed()
→ ConfigurationReadService.read() (canonical JSON + ETag)
→ ConfigurationReadService.read() with If-None-Match → 304
```

过程中**没有任何直接插入已信任 Device 或跳过 Challenge/Proof**——每一步都经过完整的 Application Service 路径。

✅ **PASS** — 全链贯通，Fake OA → Enrollment → Challenge/Proof → Permission → Token → Configuration Read。

### 4. 7 个命名故障点 + 重建

| # | FaultPoint | 注入位置 | 重建后验证 |
| --- | --- | --- | --- |
| 1 | `AFTER_IDENTITY_COMMIT` | verify() → insert enrollment grant 后 crash | `runtime()` 重建全部 JdbcAuthenticationPersistence + 所有 Service |
| 2 | `AFTER_ENROLLMENT_CHALLENGE_COMMIT` | enroll challenge issue 后 crash | `runtime()` 重建，enroll 继续 |
| 3 | `AFTER_DEVICE_ENROLLMENT_COMMIT` | enroll + permission save 后 crash | `runtime()` 重建，issue token challenge |
| 4 | `AFTER_TOKEN_CHALLENGE_COMMIT` | token challenge issue 后 crash | `runtime()` 重建，准备 token command |
| 5 | `BEFORE_TOKEN_COMMIT` | 事务内 commit 前 crash | `runtime()` 重建，验证 Challenge + Token 双回滚 |
| 6 | `AFTER_TOKEN_COMMIT_BEFORE_RESPONSE` | Token commit 后响应丢失 | `runtime()` 重建，验证 issuance 保留、旧 Proof 拒绝、新 Challenge 恢复 |
| 7 | `AFTER_CONFIGURATION_SEED_COMMIT` | seed 完成后 crash | `runtime()` 重建，验证 Configuration Read 精确还原 |

每个 `runtime()` 调用（行 313-398）新建：`JdbcAuthenticationPersistence`(new JdbcTemplate)、`JdbcConfigurationPersistence`(new JdbcTemplate)、`JdbcCentralTransactionRunner`(new) 以及所有 Application Service 实例——证明正确性事实不保存在 HTTP Session 或 Adapter 实例中。

✅ **PASS** — 7 个命名故障点齐全，每个点都重建 Application/Port/JDBC Adapter。

### 5. BEFORE_TOKEN_COMMIT 双回滚

`failAfterWorkBeforeCommit()`（行 464-475）在委托事务完成业务写入后、commit 前抛 `NamedCrash`。

`BEFORE_TOKEN_COMMIT` 验证（行 132-148）：
- `assertThat(consumedAt).isNull()` —— Challenge 消费回滚 ✅
- `assertThat(rowCount("access_token_issuance")).isZero()` —— Token issuance 回滚 ✅
- 两次断言都在**新 Runtime 实例**中查询数据库，证明回滚不是内存副作用而是真实 DB 事务回滚 ✅

✅ **PASS** — BEFORE_TOKEN_COMMIT 同时回滚 Challenge consume 和 Token issuance。

### 6. Commit 后响应丢失：issuance fact 保留 + 恢复

`AFTER_TOKEN_COMMIT_BEFORE_RESPONSE` 场景（行 150-168）：

1. Token commit 成功，但 HTTP 响应丢失 → `lostResponse` 有 `accessToken` 但客户端从未收到
2. 重建 Runtime 后：`rowCount("access_token_issuance") == 1` —— issuance fact 已持久保留 ✅
3. 用**同一旧 Proof** 重放 → `device_challenge_replayed` ✅
4. 签发**新 Challenge** → 用新 Proof → 成功获得可用 Token ✅

✅ **PASS** — commit 后响应丢失保留 issuance fact，旧 Proof 拒绝，新 Challenge 可恢复。

### 7. 32 并发 / 8 工作线程 → 恰好 1 成功

`verifyBoundedConcurrentTokenIssuance()`（行 207-248）：

- `Executors.newFixedThreadPool(8)` —— 8 工作线程 ✅
- 32 次并发 `tokenService.issue(command)` ✅
- `assertTimeout(Duration.ofSeconds(30), ...)` —— 30 秒有界 ⏱ ✅
- `outcomes.filter(true).hasSize(1)` —— 恰好 1 成功 ✅
- 其余 31 次 `device_challenge_replayed` ✅

✅ **PASS** — 32 并发 / 8 工作线程 / 30s 超时，恰好 1 成功。

### 8. now == expiresAt 失败关闭

`verifyExpiryBoundaryAfterRestart()`（行 250-275）：

- Challenge TTL 默认 60s
- `clock.advanceSeconds(challengeTtl.toSeconds())` —— `now == expiresAt`
- 重建 Runtime 后签发 → `device_challenge_expired` ✅
- `consumedAt.isNull()` —— Challenge 未消费 ✅

✅ **PASS** — now == expiresAt 失败关闭且不消费 Challenge。

### 9. Configuration restart：canonical JSON + ETag + 304

`AFTER_CONFIGURATION_SEED_COMMIT` 后验证（行 175-189）：

- 重建 Runtime → `configurationRead.read(token, null)`：
  - `notModified == false` ✅
  - `documentJson.equals(seed.snapshot().documentJson())` —— 精确 canonical JSON ✅
- `configurationRead.read(token, previousEtag)`：
  - `notModified == true` ✅
  - `documentJson == null` —— 无正文 304 ✅
  - `etag.equals(previousEtag)` —— ETag 稳定 ✅

✅ **PASS** — restart 后精确 canonical JSON、稳定 ETag、bodyless 304。

### 10. 数据库 / Surefire / Fixture 明文 0 命中

`assertDatabaseDoesNotContain()` 逐表扫描（行 487-510）：

| 明文 | 扫描表数 | 结果 |
| --- | --- | --- |
| `OA_FLOW_MATERIAL` ("cgf11d-raw-oa-flow-material-must-never-persist") | 8 表 | 0 命中 ✅ |
| `ENROLLMENT_CODE` ("cgf11d-enrollment-code-must-never-persist") | 8 表 | 0 命中 ✅ |
| `lostResponse.accessToken()` (Bearer JWS) | 8 表 | 0 命中 ✅ |
| `usableToken.accessToken()` (Bearer JWS) | 8 表 | 0 命中 ✅ |

8 个表：`enterprise_verified_identity`, `enterprise_user_permission`, `enterprise_device`, `device_enrollment_grant`, `device_challenge`, `access_token_issuance`, `enterprise_configuration_snapshot`, `enterprise_package_document`

Surefire report 扫描：
- `target/surefire-reports/*.xml` 不包含 `Bearer`、`eyJ`(JWS prefix)、`access_token`、`password` ✅

Fixture 扫描：
- 无新增 Fixture，contract `fixtures/` 未修改 ✅

✅ **PASS** — 数据库逐表 / Surefire / Fixture 三维扫描，OA 原始材料、Enrollment Code、Bearer Token 明文均为 0 命中。

### 11. OpenAPI / Schema / Fixture + V1～V5 未改写

| 资源 | MD5 | 状态 |
| --- | --- | --- |
| `contracts/enterprise-gateway/v1alpha1/openapi.yaml` | `8948e5cd1cc4871388c6b87bb5c50184` | ✅ 与 CGF-1.1C QA 基线一致 |
| 9 个 `.schema.json` 文件 | 见 evidence | ✅ 未修改 |
| 34 个 Fixture JSON | — | ✅ 未修改 |

| Migration | 行数 | MD5 | 状态 |
| --- | --- | --- | --- |
| V1__verified_identity_and_permissions.sql | 34 | `c32d2f2a43256c65f16b25ca1bd5561c` | ✅ 未改写 |
| V2__device_registration_enrollment_and_challenge.sql | 73 | `10784542d9b59cf142f3d00c36fc464d` | ✅ 未改写 |
| V3__token_issuance.sql | 23 | `3f6df8f13601dc0c2cb34fe946ce1655` | ✅ 未改写 |
| V4__immutable_configuration.sql | 42 | `ac96006b10b770bf4a0cc3fde57d063d` | ✅ 未改写 |
| V5__challenge_consumption_idempotency.sql | 14 | `61902dd0a88c6ac0e06f267e9ad7d817` | ✅ 未改写 |

**无 V6+ migration**。所有 MD5 与 CGF-1.1C QA 基线完全一致。

✅ **PASS** — OpenAPI / Schema / Fixture / V1～V5 Migration 全部未改写，无 V6。

### 12. Fault Hook 只在测试源码

`FaultPoint` 枚举和 `NamedCrash` 类定义在 `Cgf11dPostgreSqlRecoveryConformance.java`（`src/test/`）内：

- 行 525-533：`FaultPoint` enum ✅
- 行 535-547：`NamedCrash` private class ✅
- 行 464-475：`failAfterWorkBeforeCommit()` —— 仅用于测试 ✅

生产 `src/main/` 扫描：
- `grep -rn "FaultPoint\|NamedCrash\|failAfterWork\|BEFORE_TOKEN\|AFTER_IDENTITY"` → **0 命中** ✅
- 生产 Application Service 代码不含任何故障注入、测试恢复或调试分支 ✅

✅ **PASS** — 命名 Fault Hook 只在 test source，生产代码不含测试恢复分支。

### 13. 无超前实现

| 检查项 | 结果 | 状态 |
| --- | --- | --- |
| 生产 JWS/JOSE 库（nimbus/jose4j/jjwt） | pom.xml 无依赖 | ✅ |
| 真实 OA Adapter 实现 | main 源码无 `implements OAIdentityAdapter` | ✅ |
| 生产 Signer | main 源码无 `implements Signer` 或 KeyStore/Vault | ✅ |
| 生产 SecretStore | main 源码无 `implements EnterpriseSecretStore`（仅有 Port 接口） | ✅ |
| OpenAI / Anthropic / MCP SDK | pom.xml / main 源码均无 | ✅ |
| Model Gateway / Tool Gateway | 包目录仅有 `package-info.java` | ✅ |
| 配置写 API | 无 Admin CRUD | ✅ |
| Runtime Activation | main 源码无 | ✅ |
| CGF-1.2 内容 | 不存在 | ✅ |

✅ **PASS** — 无任何超出 CGF-1.1D 范围的实现。

### 14. P0 / P1 / P2 / P3 结论

| 级别 | 数量 | 明细 |
| --- | --- | --- |
| P0（阻断合并） | 0 | — |
| P1（阻断发布） | 0 | — |
| P2（应修复） | 0 | — |
| P3（非阻断改进） | 0 | — |

## 结论

**P0=0 P1=0 P2=0 P3=0。CGF-1.1D PASS。**

14 项独立 QA 建议范围全部覆盖且验证通过：
- 三门禁独立重跑全量通过 ✅
- Docker Testcontainers + Embedded PostgreSQL 均执行全链恢复矩阵，0 skipped ✅
- Fake OA → Challenge/Proof → Device Trust → Permission → Token → Configuration Read 全链贯通 ✅
- 7 个命名故障点各重建 Application/Port/JDBC Adapter ✅
- BEFORE_TOKEN_COMMIT 双回滚 ✅
- 响应丢失 → issuance fact 保留 → 旧 Proof 拒绝 → 新 Challenge 恢复 ✅
- 32 并发 / 8 线程 / 30s / 恰好 1 成功 ✅
- now == expiresAt 失败关闭 + Challenge 不消费 ✅
- Configuration restart：精确 JSON + 稳定 ETag + 304 ✅
- 数据库逐表 / Surefire / Fixture 三维明文扫描 0 命中 ✅
- Canonical Schema / Fixture / V1～V5 未改写，无 V6 ✅
- Fault Hook 只在 test source ✅
- 无超前实现 ✅

**CGF-1.1 建议关闭，但不自动进入 CGF-1.2，等待用户明确接受。**

## 证据目录

`qa-reports/2026-07-25-cgf.1.1d-qa/evidence/`
