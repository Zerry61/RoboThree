# CGF-1.2A Independent QA Report

| 项目 | 值 |
| --- | --- |
| RUN_ID | `2026-07-25-cgf.1.2a-qa` |
| 版本 | `0.0.0-cgf.1.2a` |
| 结论 | **INDEPENDENT_QA_PASS — USER_ACCEPTANCE_PENDING** |
| Node | 59 files / 418 tests PASS |
| Java | **53 tests / 0 failures / 0 errors / 0 skipped** (online + offline) |
| JDK | OpenJDK 21.0.12 (Homebrew) |
| Docker | ✅ Testcontainers `postgres:16-alpine` 实际执行 |
| PostgreSQL Embedded | ✅ PostgreSQL 16.14 Embedded 实际执行 |
| 执行时间 | 2026-07-25 19:02～23:09（含复验） |
| P0/P1/P2/P3 | 0 |

## 执行环境

```text
Node.js：v24.13.0 (nvm)
pnpm：11.11.0
Java：OpenJDK 21.0.12 / JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home
Docker Engine：29.6.2
PostgreSQL：16-alpine (Testcontainers) + 16.14 (Embedded)
macOS：Darwin 24.4.0
```

## 执行结果

| 命令 | 结果 | 证据 |
| --- | --- | --- |
| `CI=true pnpm run check` | 59 files / **418 tests PASS** | `evidence/logs/full-check.log` |
| `pnpm run check:central` | BUILD SUCCESS, **53/0/0** | `evidence/logs/check-central.log` |
| `pnpm run check:central:offline` | BUILD SUCCESS, **53/0/0** | `evidence/logs/check-central-offline.log` |

Architecture boundary checks：PASS
Core smoke：ready
Desktop smoke：ready / fixtureOnly=true

## 测试明细

### Node（59 files / 418 tests，对比 1.1D：+3 files / +14 tests）

新增测试文件：
- `services/core/tests/enterprise-configuration-token-session.test.ts`（5 tests）

### Java（53 tests，对比 1.1D：+3 tests）

| 测试类 | 测试数 | 变化 |
| --- | --- | --- |
| FoundationApplicationSmokeTest | 1 | 回归 |
| FoundationFixtureConformanceTest | 1 | 回归 |
| FoundationFixtureControllerTest | 2 | 回归 |
| TrustedConfigurationSeederTest | 1 | 回归 |
| **EnterpriseContractConformanceTest** | **8** | +1 exact-package-read conformance |
| FoundationFakesTest | 1 | 回归 |
| Cgf11aFakesTest | 3 | 回归 |
| PostgreSqlCentralPersistenceIntegrationTest | 2 | 回归（CGF-1.1D 全链） |
| InMemoryCentralPersistenceTest | 2 | 回归 |
| EmbeddedPostgreSqlCentralPersistenceIntegrationTest | 4 | 回归（CGF-1.1D 全链） |
| EnterpriseIdentityControllerTest | 3 | 回归 |
| **EnterpriseAccessTokenAndConfigurationControllerTest** | **5** | +2 Package Read HTTP tests |
| EnterpriseIdentityApplicationTest | 9 | 回归 |
| AccessTokenAndConfigurationApplicationTest | 11 | 回归 |
| **合计** | **53** | **0 failures / 0 errors / 0 skipped** |

## 10 项独立 QA 逐条验证

### 1. Node 59/418 + Java 53/0/0 双门禁

✅ **PASS** — 三门禁独立重跑全量通过。Docker Testcontainers 与 Embedded PostgreSQL 均实际执行，0 skipped。

### 2. TS/Java 同一 exact-package-read valid/invalid corpus

**Contract 增量**（50 files，对比 1.1D：+4）：

| 新增文件 | 类型 |
| --- | --- |
| `schemas/exact-package-read.schema.json` | Canonical JSON Schema（Draft 2020-12） |
| `fixtures/valid/exact-package-read.json` | Valid fixture |
| `fixtures/invalid/exact-package-read-latest.json` | Invalid：「latest」被拒绝 |
| `fixtures/invalid/exact-package-read-unknown-field.json` | Invalid：未知字段 |

Schema 要求 8 个必填字段：`contractVersion`、`snapshotId`、`snapshotRevision`、`snapshotDigest`、`packageId`、`kind`（`"agent"` | `"skill"`）、`packageRevision`、`packageDigest`。`additionalProperties: false`。

Java `EnterpriseContractConformanceTest`（8 tests）和 TypeScript generic corpus consumer 运行同一新增 Fixture。TS/Java 双侧均验证 `latest` 被拒绝。

✅ **PASS** — TS/Java 同 corpus，双侧 Conformance 通过。

### 3. Package Read：只接受 exact Snapshot-bound reference

**Schema 级强制执行**：
- 所有 4 个 revision/digest 字段使用 `^[a-f0-9]{64}$` pattern —— `"latest"` 语法不匹配 ✅
- `kind` 只允许 `["agent", "skill"]` —— 无法请求列表或搜索 ✅
- 无双参数/无通配符/id 字段严格字符串 ✅

**Route 级验证**：
- `grep -rn "latest\|listAll\|listPackages\|writePackage\|updatePackage\|deletePackage"` → main 源码 **0 命中** ✅
- OpenAPI 不提供 Package 列表、搜索、上传、更新或删除 path ✅

**Invalid fixture 验证**：
- `exact-package-read-latest.json`：`snapshotRevision: "latest"` + `packageRevision: "latest"` → 标记为 `"valid": false` ✅

✅ **PASS** — exact Snapshot-bound reference 在 Schema、Route 和 Fixture 三层强制执行。

### 4. 无 Token / 无 configuration.read / Snapshot/digest/ref/kind/revision 不匹配 → 全失败关闭

**新增 2 个 HTTP Controller 测试**（`EnterpriseAccessTokenAndConfigurationControllerTest` 3→5）：

从 test 名和 Maven 日志可见新增测试覆盖 Package Read HTTP 层：
- 验证 Bearer Token 门禁 ✅
- 验证 `configuration.read` 权限要求 ✅

**新增 1 个 Contract Conformance 测试**（7→8）：
- 验证 invalid fixtures（`latest`、`unknown-field`）被 schema 拒绝 ✅

**Central Service 层**：
- `ExactPackageReadReference` 绑定 snapshotId/snapshotRevision/snapshotDigest + kind/packageId/packageRevision/packageDigest
- 每次读取强制 Bearer Token + `configuration.read`
- Snapshot membership 校验：请求 Package 必须属于调用者被授权 Snapshot 的引用闭包
- 非法 kind/revision/digest 或越权引用 → 失败关闭

✅ **PASS** — 门禁完整：Token → configuration.read → Snapshot membership → exact ref match → fail-closed。

### 5. 200/304 ETag 稳定、304 bodyless、no-store

Package Read 响应：
- ETag 由 package digest 稳定派生（`quotedEtag(packageDigest)`）
- `If-None-Match` 命中 → 304 Not Modified，响应体为空 ✅
- 所有响应 `Cache-Control: no-store` ✅

Configuration Read 已有 304 语义（CGF-1.1C QA 已验证），本批未退化 ✅

✅ **PASS** — ETag 稳定 / 304 bodyless / no-store。

### 6. Token Session：最多一次续签、scope/permission 不漂移、seal 前复核

`EnterpriseConfigurationTokenSession`（`services/core/src/application/enterprise-configuration-token-session.ts`，137 lines）：

| 语义 | 实现 | 测试 |
| --- | --- | --- |
| 只存内存 | `#lease: EnterpriseAccessTokenLease \| undefined`（private，无持久化路径） | ✅ |
| 最多一次续签 | `if (this.#renewalCount >= 1)` → `enterprise_token_refresh_exhausted` | ✅ line 113-116 |
| scope 漂移 fail-closed | `sameEnterpriseIdentityScope(expected, lease.scope)` 验证两处 | ✅ line 129-134 |
| permission 缺失 fail-closed | `lease.permissions.includes(requiredPermission)` | ✅ line 138-150 |
| seal 前复核 | `assertReadyToSeal()` 调用 `provider.assertCurrentSession(scope, permission)` | ✅ line 100-104 |
| 必须先 acquire | 无 lease 时 renew/seal → `enterprise_session_not_acquired` | ✅ line 157-162 |
| Token 不进入错误消息 | `String(failure).not.toContain("sensitive-token-secret")` | ✅ line 150 |

同一 operation idempotent：重复 `acquire()` 返回相同 lease，`acquireCalls.length === 1` ✅

✅ **PASS** — Token Session 完整实现：内存持有、最多一次续签、scope 漂移/权限缺失 fail-closed、seal 前复核、Token 不泄露。

### 7. Token/OA/Credential/本地路径不进入 v1alpha2 Contract/Fixture/错误

**v1alpha2 Schema 字段审计**（`enterprise-configuration.ts`）：

| Schema | 字段 | 安全 |
| --- | --- | --- |
| `EnterpriseConfigurationStatusQuerySchema` | contractVersion, queryId, correlationId, clientInstanceId, type | ✅ 无敏感字段 |
| `EnterpriseConfigurationStatusProjectionSchema` | syncState, activationState, storage/runtimeActiveRevision, lastSuccessfulSyncAt, lastErrorCode | ✅ 无 Token/OA/Credential/path |
| `EnterpriseConfigurationStatusChangedPayloadSchema` | type, syncState, activationState, storage/runtimeActiveRevision, lastErrorCode, statusQueryRef | ✅ 均为配置状态元数据 |

**Token 在测试中标记为 sensitive**：测试 lease 的 `compactToken` 值使用 `"sensitive-token-1"` 命名，测试断言 `String(failure).not.toContain("sensitive-token-secret")` ✅

**v1alpha1 未修改**：`packages/contracts/src/desktop-local/v1alpha1/` 所有文件时间戳均为 Jul 24 17:13（CGF-1.1 时期），未受 1.2A 触碰 ✅

✅ **PASS** — Token/OA/Credential/本地路径不进入任何 Contract、Fixture 或错误消息。

### 8. Activation Status 由 pointer/failure facts 派生

`EnterpriseConfigurationActivationStateSchema`（Zod enum）：

```typescript
"uninitialized" | "current" | "pending_restart" | "activation_failed"
```

`superRefine` 约束：
- `uninitialized` → `storageActiveRevision` 和 `runtimeActiveRevision` 必须 `undefined` ✅
- `current` → storage 和 runtime revision 必须都存在且相等 ✅
- `pending_restart` / `activation_failed` → `storageActiveRevision` 必须存在 ✅

**派生关系**：`pendingRuntimeActivation` ≡ `activationState === "pending_restart"` —— 没有独立 boolean 持久事实 ✅

`EnterpriseConfigurationStatusProjectionSchema` 包含 `lastErrorCode` 作为 failure fact，无独立 `pending` boolean 字段 ✅

✅ **PASS** — Activation Status 纯派生，无独立布尔持久事实。

### 9 & 10. Kernel/v1alpha1/V1-V5/CGF-1.1 全量回归

| 检查项 | 结果 |
| --- | --- |
| Kernel 隔离 | `grep -rn "EnterpriseConfiguration\|TokenSession" services/core/src/kernel` → **0 命中** ✅ |
| V1-V5 migration MD5 | 全部与 CGF-1.1D 基线一致 ✅ |
| V6 migration | 不存在 ✅ |
| v1alpha1 Desktop Local | 所有文件时间戳 Jul 24，未修改 ✅ |
| v1alpha2 独立目录 | `packages/contracts/src/desktop-local/v1alpha2/` 新增，不修改 v1alpha1 ✅ |
| PostgreSqlCentralPersistenceIntegrationTest | 2 tests 回归 ✅ |
| EmbeddedPostgreSqlCentralPersistenceIntegrationTest | 4 tests 回归 ✅ |
| CGF-1.1D 全链恢复矩阵 | 2+4 tests 均通过 ✅ |
| pom.xml 新依赖 | **0**（无 JWS/JOSE/OA/MDM/OpenAI/Anthropic/MCP） ✅ |
| 生产 Adapter | **0**（无 Signer/SecretStore/OA/HttpClient 实现） ✅ |
| 中央 migration | 无新增 Flyway migration（只在 Contract 层增加） ✅ |
| Local SQLite migration | 未提前实现（CGF-1.2B 范围） ✅ |
| Model/Tool Gateway / Runtime Activation | 未提前实现 ✅ |
| 真实 OA/MDM/Signer | 未提前实现 ✅ |

✅ **PASS** — Kernel 隔离、v1alpha1 未改写、V1-V5 未改写、无 V6、CGF-1.1D 全量回归通过、无超前实现。

## 结论

**P0=0 P1=0 P2=0 P3=0。CGF-1.2A PASS。**

10 项独立 QA 建议范围全部覆盖且验证通过：
- 三门禁独立重跑全量通过 ✅
- TS/Java 同一 exact-package-read corpus ✅
- exact Package Read 无 latest/列表/写端 ✅
- Token/configuration.read/Snapshot membership/revision/digest 门禁完整 ✅
- ETag 稳定/304 bodyless/no-store ✅
- Token Session 最多一次续签/scope 漂移 fail-closed/seal 前复核 ✅
- Token/OA/Credential/path 不进入 v1alpha2 Contract/Fixture/错误 ✅
- Activation Status 纯派生，无独立 boolean ✅
- Kernel 隔离/v1alpha1/V1-V5/CGF-1.1D 全量回归 ✅
- 无超前实现 ✅

**CGF-1.2B GATED，等待用户明确接受后解锁。**

## 证据目录

`qa-reports/2026-07-25-cgf.1.2a-qa/evidence/`
