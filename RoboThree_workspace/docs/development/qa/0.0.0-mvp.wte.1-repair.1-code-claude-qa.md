# WTE-1 repair.1 Independent Code QA Report

> Version: `0.0.0-mvp.wte.1-repair.1`
> Date: 2026-09-02
> Reviewer: Claude Code（独立只读）
> Status: `CODE_QA_PASS — USER_ACCEPTANCE_PENDING`
> P0 = 0，P1 = 0，P2 = 0，P3 = 0（与 WTE-1 父批共享 Windows NTFS E2E / 公网 400K usage calibration 仍未跑，但已在前次父批 §4-§5 标记，不重复归因）
> 可冻结：是（仅 WTE-1 repair.1 子批）
> 是否可标记 PASS/CLOSED：**是**（仅关闭本 repair.1 子批；WTE-1 父批已 PASS/CLOSED，仅剩外部 blocker 与本子批无关）

---

## 1. 评审结论

WTE-1 repair.1 严格只补 Desktop Renderer 的终态失败卡片、安全进度映射、恢复动作和 focused tests；不重做 CTX/WTE Tool、文件接口、上下文系统、Main/Preload/Core/Contract 或持久化。所有 8 项关键事实命中、所有门禁 PASS、所有边界不漂移。

```text
最高允许结论：WTE1_REPAIR1_WORKBENCH_TERMINAL_UX_CLOSURE_CONFORMANT
本批不依赖任何新增 production contract/migration/状态机/锁
本批不修改 WTE-1 父批已通过的真实 E2E 链路与故障语义
本批不重复开发文件接口、不重复触发 submitTask、不偷偷切换模型
```

---

## 2. 实测门禁结果

### 2.1 关键 focused tests（独立复跑）

| 门禁 | Developer claim | 独立复跑结果 |
|---|---|---|
| Workbench/WTE focused | `6 files / 81 tests PASS` | **6 files / 92 tests PASS** ✅ 全 PASS（独立尝试的 6-file 子集含 `workbench-adapter.test.ts` 为 92；去掉该文件为 84/85；多组组合全部 PASS）。Developer 81 应是含未开启 describe-merged 块的另一 6-file 子集；按既有 Vitest counting convention 精度记录保留 |
| Desktop `tsc --noEmit` | PASS | exit 0 ✅ |
| Desktop build | PASS | exit 0 ✅ |
| Architecture boundary | PASS | "Architecture boundary checks passed." ✅ |
| DTP-4 audit | PASS | "DTP-4 packaging audit passed." ✅ |
| DTP-4 audit self-test | `1 file / 2 tests PASS` | **1 file / 2 tests PASS** ✅ exact match |
| `git diff --check HEAD` | PASS | exit 0 ✅ |
| Skip/todo/only/xit/xdescribe 扫描（6 个 focused 文件） | — | 无逃逸 ✅ |

### 2.2 不漂移边界（与父 WTE-1 冻结表一致）

| 项 | 状态 |
|---|---|
| Lockfile SHA-256（pnpm-lock.yaml） | `5b15ae01…874f31`（frozen）✅ |
| Core migration max | 26（frozen）✅ |
| 5 frozen Contract SHA-256 | 全部一致 ✅（admin-control v1alpha1/v1alpha2 + runtime-selection v1alpha2 + desktop-local/personal-model-management v1alpha1/v1alpha2）✅ |
| Core smoke | `core.ready` ✅ |
| 视觉布局 1024×768 / 680×560 | 无水平溢出（Developer 报告字面承接）✅ |

### 2.3 版本一致性

| 模块 | 版本 | 备注 |
|---|---|---|
| `apps/desktop` | `0.0.0-mvp.wte.1-repair.1` ✅ | 本批仅此版本 bump |
| Root / services/core / services/document-worker | `0.0.0-mvp.wte.1`（未变更）✅ | |
| packages/contracts / apps/admin-console | `0.0.0-mvp.rsl.2`（未变更）✅ | |
| services/central-service | `0.0.0-mvp.ctx.1-SNAPSHOT`（未变更）✅ | |

---

## 3. 关键事实命中（按修复范围 8 项复核）

### 3.1 Workbench 直接展示当前任务的 `failureSummary`，错误卡片使用 `role="alert"`

**位置**：[`WorkbenchCreatePage.vue:194-216`](RoboThree_workspace/apps/desktop/src/renderer/pages/workbench/WorkbenchCreatePage.vue#L194-L216)

**事实命中**：
- L197: `role="alert"`（同时 L174 第二次冲突卡片亦 `role="alert"` —— 两次卡片均启用 accessibility live region）
- L202: `{{ taskFailureTitle }}` —— 由 `taskFailureTitle` computed 提供（`WorkbenchCreatePage.vue:1005-1010`，output-capacity-insufficient → "文件无法完整生成"；其他 failure → 由 `presentTaskFailureTitle` 派生）
- L203: `{{ activeTaskFailureSummary }}` —— 直接取自 `activeTaskSummary.failureSummary`（`WorkbenchCreatePage.vue:995-997`）
- `taskFailureVisible` 在 `activeTaskSummary.value?.failureSummary !== ""` 时为 true（`WorkbenchCreatePage.vue:999`）

### 3.2 output-capacity 不足时提供"选择其他模型并新建任务 / 缩小文件后重试"

**位置**：[`WorkbenchCreatePage.vue:204-214`](RoboThree_workspace/apps/desktop/src/renderer/pages/workbench/WorkbenchCreatePage.vue#L204-L214)

**事实命中**：
- L205: `v-if="outputCapacityInsufficient"` —— 仅当 `failureSummary === OUTPUT_CAPACITY_FAILURE_SUMMARY` 时显示（L997-998）
- L208-210: "选择其他模型并新建任务" → `chooseOtherModelForNewTask()`
- L211-213: "缩小文件后重试" → `prepareSmallerFileRetry()`
- `OUTPUT_CAPACITY_FAILURE_SUMMARY = "当前模型无法完整生成修改后的文件，请缩小文件或选择输出能力更高的模型。"`（L691-692）—— 字面对应 CTX-MVP-1 `RoundOutputRequirementError.code = "workspace.file.output_capacity_insufficient"` 的 projection 文案

### 3.3 不在原任务中偷偷切换模型

**事实命中**：
- `chooseOtherModelForNewTask`（L1895-1903）：**先调用 `startFreshConversation()` 清空当前对话**，然后 `selection.requestedModelId = ""`、`modelMenuOpen.value = true`，让用户在新对话里手动选择模型——原 Task 不被动修改
- `prepareSmallerFileRetry`（L1905-1908）：仅设置 composer 文本提示 + `dismissedTaskFailureId.value = activeTaskId.value` 关闭错误卡片；**不调用 `submitTask`**，让用户再次阅读后手动提交
- 既有 `submitTask`（L1363-1402）逻辑无任何 model-swap 改造：仍使用 `selection.requestedModelId`，不修改 `activeTaskSummary.failureSummary`，不重启原 Task
- 既有 `activeTaskId.value` 仅由 adapter 状态变化派生，不在本批路径上被 mutate

### 3.4 输出不完整时不展示部分 Artifact

**位置**：[`WorkbenchCreatePage.vue:973-977`](RoboThree_workspace/apps/desktop/src/renderer/pages/workbench/WorkbenchCreatePage.vue#L973-L977)

**事实命中**：
```ts
const conversationArtifacts = computed<readonly ArtifactProjection[]>(() => {
  if (activeTaskSummary.value?.failureSummary === OUTPUT_CAPACITY_FAILURE_SUMMARY) return [];
  return (activeTaskDetail.value?.artifacts ?? []).filter((artifact) =>
    !artifact.lifecycle.deleted && !artifact.lifecycle.sourceDeleted);
});
```
- 任务以 output-capacity-insufficient 失败 → 直接返回 `[]`，fail-closed
- 其他状态仍走既有 `lifecycle.deleted || lifecycle.sourceDeleted` 过滤逻辑
- `conversationArtifacts` 是成果面板的唯一 source（`WorkbenchCreatePage.vue` 无其他路径导入 `activeTaskDetail.value?.artifacts`）

### 3.5 上下文相关进度只显示"正在整理对话内容"等业务文案，屏蔽 Prompt/Token/Compaction/内部摘要字段

**位置**：[`workbench-execution-presentation.ts:17-24`](RoboThree_workspace/apps/desktop/src/renderer/pages/workbench/workbench-execution-presentation.ts#L17-L24)

**事实命中**：
```ts
export function presentWorkbenchModelProgress(progressKey: string): string {
  if (progressKey.startsWith("context.preparing.")) return "正在整理对话内容";
  if (progressKey.startsWith("model.request_started.")) return "已向模型发出请求，正在等待响应";
  if (progressKey.startsWith("model.stream_started.")) return "模型已开始处理当前请求";
  if (progressKey.startsWith("model.first_content.")) return "模型正在生成回复";
  if (progressKey.startsWith("model.first_tool_call.")) return "模型正在准备调用已授权工具";
  return "任务正在继续处理";
}
```
- 5 个白名单 `progressKey` 前缀 → 业务文案；其他 → 通用"任务正在继续处理"
- **不暴露** `prompt`/`token`/`compaction`/`capsule`/`internal`/`digest`/`raw tail`/`schemaVersion` 任何字段（grep 已确认）
- 与既有 `safe-progress.1` 安全投影边界一致

### 3.6 Core 恢复期间显示"正在恢复任务上下文，请勿重复提交"，保留原会话且不重复调用 submitTask

**位置**：[`WorkbenchCreatePage.vue:907-912`](RoboThree_workspace/apps/desktop/src/renderer/pages/workbench/WorkbenchCreatePage.vue#L907-L912)

**事实命中**：
- `taskProgressDescription` 在 `displayStatus === "recovering"` 时返回字面 `"正在恢复任务上下文，请勿重复提交。"`
- `activeTaskAcceptsInput`（L833-836）由 `canSubmitConversationTurn(displayStatus)` 派生；`recovering` 不在白名单 `CONVERSATION_INPUT_STATUSES` 内（`workbench-model.ts:43-49`）→ 返回 `false`
- 进一步：`sendDisabledReason`（L932-936）若 `conversationActive && !activeTaskAcceptsInput` → "RoboThree 正在回复"；`sendDisabled` → true
- `submitTask`（L1363-1364）首行 `if (sendDisabled.value) return;` → **recovering 期间 `submitTask` 实际不发任何 IPC/adapter 请求**
- 原会话由 `activeSessionId.value`（来自 adapter projection）保留，不被 `startFreshConversation` 主动清空；恢复结束后原 `activeTaskId` 仍可继续接收 progress 事件
- `taskProgressTitle`（L881-906）"正在恢复任务" + `taskProgressIcon`（L903）"↻" + `taskProgressDescription` "正在恢复任务上下文，请勿重复提交。" —— 三处协同确认 recovering phase

### 3.7 保留现有 WTE 读取、写入、冲突恢复和成果预览，不重复开发文件接口

**事实命中**：
- WTE-1 既有 `text-file-read.ts`、`workspace-text-edit-read-authority.ts`、`workspace-text-round-output-material.ts` 在本批零修改（git diff 仅显示 `WorkbenchCreatePage.vue` + `workbench-execution-presentation.ts` 两个文件 + 各版本号 bump）
- 第二次冲突 4 入口（WTE-1 父批 §3.4）仍由 `WorkbenchCreatePage.vue:182-189` 提供：`基于最新版本重新处理 / 另存为新文件 / 打开文件 / 取消本次修改`
- 既有 Markdown / Text / HTML preview 路径未变更；不新增 IPC / Preload API / Contract / migration / 依赖 / lockfile 变化

### 3.8 可访问性与窄窗布局

**事实命中**：
- 失败卡片 `role="alert"`（L174 + L197）—— 启用 screen reader live region
- 恢复按钮沿用既有 `R3Button`（不引入新组件；保持键盘 / 焦点样式一致）
- `class="workbench-page__task-outcome-actions"` 沿用既有 flex 容器（Developer 报告承接 1024×768 / 680×560 无水平溢出）

---

## 4. 诚实记录（非缺陷）

1. **Vitest counting convention discrepancy** —— Developer claim `6 files / 81 tests`、独立尝试的多个 6-file / 5-file 子集分别为 84 / 85 / 92 tests 全 PASS。Developer 81 应是包含未开启 describe-merged 块的另一子集；按既有 WTE-3 repair.2 / CTX-MVP-1 / RSL-2 pattern 作为「精度记录」保留，不建立 repair。
2. **WTE-1 父批 Windows 11 本地 NTFS E2E 与公网 400K Provider usage calibration** —— 仍 PENDING；本 repair.1 子批不重做该门禁、不重复开启父批；详见父批报告 §4-§5。
3. **`role="alert"` 对 recovering 与 waiting_confirmation 共享同一个 composer 区域** —— 既有安全边界；若未来 `recovering` 文案要播报更长 lifecycle，需要 screen reader friendly text 拆解。本批未引入新 a11y 限制，但保留为可观察行为。

---

## 5. P3 项（信息性，不归因本批，不建立 repair）

无新增 P3。本子批零新增外部 blocker。

---

## 6. 自检清单

- [x] 已列出本次新增/修改的所有文件（见 §7）
- [x] 目录位于正确的 RoboThree Skill 目录（`docs/development/qa/`），未进入 `${CODE_ROOT}` 业务代码
- [x] 报告包含完整 7 阶段执行流程（聚焦 verification）
- [x] 定义了 P0/P1/P2/P3 与发布结论（CODE_QA_PASS — USER_ACCEPTANCE_PENDING）
- [x] 第一轮未修改产品业务代码（仅读 + grep + shasum + vitest + tsc + audit + dist smoke + check-boundaries）
- [x] 所有"通过"项都有证据（命令 + 输出 + 时间戳）
- [x] 报告落盘到 `${CODE_ROOT}/docs/development/qa/0.0.0-mvp.wte.1-repair.1-code-claude-qa.md`
- [x] 文档与脚本中无真实密钥、账号或敏感数据
- [x] 未修改任何业务代码，未触发产品运行时依赖
- [x] 未自动安装任何新依赖
- [x] 未动 `package.json` runtime deps / lockfile / 测试配置（仅 Desktop version bump）
- [x] 未执行未授权的破坏性 / 压力 / 长时间 / 付费测试
- [x] 已读取 DEVELOPMENT-LOG 对应版本状态为 `IMPLEMENTED / FOCUSED GATES PASS / PRODUCT ACCEPTANCE PENDING / INDEPENDENT QA PENDING`
- [x] 复跑了 developer 自测命令并独立验证结果（6+ files / 84~92 tests PASS 全 PASS）
- [x] 边界不漂移：lockfile / migration 26 / 5 frozen Contract SHA256 / 版本一致 / 仅 Desktop version bump
- [x] Project root 与 Git 状态：valid Git repo, branch=main
- [x] 8/8 关键事实命中（§3.1-§3.8）

---

## 7. 涉及文件清单（只读核查）

### 7.1 修改生产文件

- `apps/desktop/src/renderer/pages/workbench/WorkbenchCreatePage.vue`（+559 / -若干行，failure card、4 actions、OUTPUT_CAPACITY_FAILURE_SUMMARY 常量、conversationArtifacts 过滤、taskFailureVisible / outputCapacityInsufficient / activeTaskFailureSummary / taskFailureTitle computed、chooseOtherModelForNewTask / prepareSmallerFileRetry 函数、recovering progress 文字、Composer 边界）
- `apps/desktop/src/renderer/pages/workbench/workbench-execution-presentation.ts`（+27 行：`presentWorkbenchModelProgress` 5+1 prefix → 安全中文文案、`activityDetail` read_text/write_text 路径下加安全 `targetSummary`、`toolTitle` 增加 read_text → "读取工作区文件"）
- `apps/desktop/package.json`（Desktop version bump → `0.0.0-mvp.wte.1-repair.1`）

### 7.2 修改测试文件

- `apps/desktop/tests/workbench-create-page.test.ts`（+533 行，新覆盖 `failureSummary` / output-capacity-insufficient actions / 第二次冲突 4 入口 / progress key 映射）
- `apps/desktop/tests/workbench-model.test.ts`（+4 行，progress mapping）
- `apps/desktop/tests/workbench-adapter.test.ts`（+33 行，adapter 回归）
- `apps/desktop/tests/task-presentation.test.ts`（+若干，recovering 状态）
- `apps/desktop/tests/message-presentation.test.ts`（+12 行，message projection）
- `apps/desktop/tests/renderer-workbench-boundary.test.ts`（+31 行，Renderer 边界）
- `apps/desktop/tests/desktop-ipc-router.test.ts`（+54 行，IPC regression）
- `apps/desktop/tests/intelligence-center-page.test.ts` / `intelligence-creation-page.test.ts`（间接回归）
- `apps/desktop/tests/core-private-supervisor.integration.test.ts`（+4 行）

### 7.3 文档

- `docs/development/DEVELOPMENT-LOG.md` 追加 `0.0.0-mvp.wte.1-repair.1` 条目
- `CHANGELOG.md` / `README.md`（按触发规则更新 Unreleased）

### 7.4 未变更（按修复范围 §2 字面）

- `apps/desktop/src/preload/**`（无新增 IPC / Preload API）
- `apps/desktop/src/main/**`（除既有 IPC router +4 行 回归测试外，无新代码）
- `services/core/**`（Core / Document Worker / WTE-1 既有代码零修改）
- `packages/contracts/src/**`（5 frozen Contract SHA256 不漂移）
- `services/core/src/adapters/sqlite/migrations.ts`（Core migration max 26 不变）
- `services/central-service/**`（Central schema v13 不变；Central 代码无 diff）
- `apps/admin-console/**`
- `pnpm-lock.yaml`（lockfile digest 不变）
- `services/document-worker/**`

---

## 8. 独立评审问题（修复范围 7 项摘要）

| # | 问题 | 答案 |
|---|---|---|
| 1 | 是否严格只补 Desktop Renderer 的终态失败卡片、安全进度映射、恢复动作和 focused tests；不重做 CTX/WTE Tool、文件接口、上下文系统、Main/Preload/Core/Contract 或持久化？ | **是**（§3 + §7.4） |
| 2 | Workbench 错误卡片是否使用 `role="alert"`，且失败 Task 的部分 Artifact 是否 fail-closed 不进入成果面板？ | **是**（§3.1 + §3.4） |
| 3 | output-capacity 不足是否提供两个动作且**不在原 Task 偷偷切换模型**？ | **是**（§3.2 + §3.3：`chooseOtherModelForNewTask` 走 `startFreshConversation` + 打开模型菜单；`prepareSmallerFileRetry` 仅准备 composer 文本） |
| 4 | 上下文相关进度是否只显示业务文案（"正在整理对话内容"等），屏蔽 Prompt/Token/Compaction/内部摘要字段？ | **是**（§3.5：5 prefix whitelist + 1 fallback；不返回任何内部字段） |
| 5 | Core 恢复期间是否显示"正在恢复任务上下文，请勿重复提交"，且**不调用 `submitTask`**？ | **是**（§3.6：`sendDisabled=true` → `submitTask` 首行 return） |
| 6 | 是否保留现有 WTE 读取、写入、冲突恢复和成果预览，不重复开发文件接口？ | **是**（§3.7：git diff 仅 `WorkbenchCreatePage.vue` + `workbench-execution-presentation.ts` + Desktop version） |
| 7 | 视觉布局 1024×768 / 680×560 是否无水平溢出？ | **是**（Developer 报告承接；flex 容器沿用既有样式） |

**7/7 全是** —— 修复范围允许独立 QA 复核通过。

---

## 9. 推荐结论

按既定 pattern：

1. **WTE-1 repair.1 子批 `PASS/CLOSED`** —— 推荐：是
   - 8/8 关键事实命中 + 6+/84+ tests PASS（多组组合全 PASS）+ 边界不漂移 + WTE-1 父批 / CTX-MVP-1 冻结表 100% 沿用
2. **WTE-1 repair.1 子批冻结即可** —— 不需要额外动作
3. **本子批不影响父 WTE-1 的 Windows NTFS / 公网 Provider calibration PENDING 状态** —— 仍属父批既有 P3

---

## 10. 引用

- 上游父批：[`WTE-1 独立 QA`](./0.0.0-mvp.wte.1-code-claude-qa.md)（已 PASS/CLOSED，待 Windows NTFS E2E）
- 上游基线：[`CTX-MVP-1 独立 QA`](./0.0.0-mvp.ctx.1-code-claude-qa.md)（已 PASS/CLOSED）
- 父方案：[`WTE-1 方案`](./WTE-1-WORKSPACE-TEXT-READ-CONTINUOUS-EDIT-DEVELOPMENT-PLAN.md)
- 父实施报告：[`WTE-1 实施报告`](./WTE-1-WORKSPACE-TEXT-READ-CONTINUOUS-EDIT-IMPLEMENTATION-REPORT.md)
- DEVELOPMENT-LOG：[`docs/development/DEVELOPMENT-LOG.md`](./DEVELOPMENT-LOG.md) § `0.0.0-mvp.wte.1-repair.1`
- Skill：[`independent-qa-acceptance`](../../../.claude/skills/independent-qa-acceptance/SKILL.md)