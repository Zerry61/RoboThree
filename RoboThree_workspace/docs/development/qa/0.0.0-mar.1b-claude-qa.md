# 0.0.0-mar.1b — Claude Code 独立 QA 报告

## 基本信息

| 属性 | 内容 |
| --- | --- |
| RUN_ID | `2026-08-06-1938-version-0.0.0-mar.1b` |
| 验收对象 | MAR-1B：Manual workspace-file bounded preview |
| 日期 | 2026-08-06 |
| 验收者 | Claude Code（独立 QA，只读） |
| 触发方式 | 用户显式指令（"MAR-1B 已实现并完成治理收口，等待 Claude Code 独立 QA"） |
| 环境 | Node v24.13.0 / pnpm 11.11.0 / vitest 4.1.10 |
| 开发版本 | Core / Desktop `0.0.0-mar.1b`；Contracts `0.0.0-mar.1.0`；Document Worker `0.0.0-dwo.1`；Root `0.0.0-dtp.4` |
| 证据目录 | `qa-reports/2026-08-06-1938-version-0.0.0-mar.1b/evidence/` |

治理说明：MAR-1A 已正式 `PASS/CLOSED`。MAR-1B 为手动登记的 workspace 文件提供**有界预览**（text/markdown/HTML），复用 APV-1B 受限渲染与 APV-1C HTML 沙箱，不引入 parser 或新 renderer。本报告重点核查 previewState 白名单、预览触发条件（仅无 taskId 手动 artifact）、HTML 原文不返 Renderer、预览有界与复用既有安全机制。

---

## 一、门禁复跑结果（全部独立执行，未采信自测）

| # | 门禁 | 结果 | 与自测声明对比 |
| --- | --- | --- | --- |
| 1 | MAR-1B focused（ipc-router + api + presentation + workbench + task-projection + audit） | **PASS** 6 files / 43 tests | 一致 |
| 2 | Core 全量测试 | **PASS** 77 files / 588 tests | 一致（本次沙箱内直接通过） |
| 3 | Desktop 全量测试 | **PASS** 23 files / 104 tests | 一致（本次沙箱内直接通过） |
| 4 | Document Worker 全量 | **PASS** 22 files / 174 tests | 一致 |
| 5 | Contracts 全量测试 | **PASS** 14 files / 87 tests | 一致 |
| 6 | lint + architecture boundary | **PASS** | 一致 |
| 7 | `audit:dtp4` | **PASS**（Core/Desktop `0.0.0-mar.1b` 版本期望） | 一致 |
| 8 | offline frozen install | **PASS**（Already up to date） | 一致 |
| 9 | 完整 `pnpm run check` | **PASS** 150 files / 1024 tests ＋ 三项 smoke | 一致 |

回归基线：MAR-1A 为 150 files / 1021 tests；本次 1024 tests（+3，全部为 MAR-1B 预览用例）。无既有用例回归。

---

## 二、静态核查（manual workspace-file bounded preview 安全边界，全部通过）

| 关注点 | 结论 | 证据 |
| --- | --- | --- |
| **previewState 白名单** | ✅ | `previewStateForManualMedia` → `isManualPreviewMedia`（mediaType `text/plain`/`text/markdown`/`text/html` 或扩展名 `.txt/.md/.markdown/.html/.htm`）→ `available`；PDF/XLSX/DOCX 等其余 → `unsupported`（`desktop-task-projection-service.ts:1785-1811`） |
| **预览仅在无 taskId 手动 artifact 触发** | ✅ | `#previewArtifact` 先试 Core task-scoped preview；未命中且 `resolveArtifactFileSource` 返回 `taskId === undefined` 才进入 manual workspace-file fallback（`desktop-ipc-router.ts:399-412,464-468`） |
| **文件守卫复用 MAR-1A** | ✅ | `resolvePreviewableContainedFile`：safe relativePath + realpath containment + lstat/stat identity + symlink/非 file/hardlink 拒绝 + 扩展名白名单 + `maxSourceBytes=1MiB` |
| **text/markdown 有界稳定读取** | ✅ | `readStableFilePreview`：FileHandle 前后 fstat `dev/ino/size/mtime` 稳定 + `truncated` 标记 + `finally` 关 handle；`byteLimit = min(maxBytes, 4KiB safe summary)`（`desktop-ipc-router.ts:1005-1054`） |
| **HTML 不返原文，走 APV-1C 沙箱** | ✅ | `#startWorkspaceHtmlPreview`：仅无 taskId 手动 artifact + `.html/.htm` + ≤256KiB 读取 → 原文直接交 `#htmlPreviewSandbox.start`（APV-1C loopback 127.0.0.1 + CSP + tokenized URL），Renderer 只拿 tokenized previewUrl，**无 innerHTML/rawHtml 直接渲染** |
| **复用既有安全机制，无新 renderer** | ✅ | text/markdown 内容由 APV-1B `artifact-preview-presentation` sanitizer 渲染；HTML 由 APV-1C sandbox 承载；本批不引入 parser / 新渲染器 |
| **有界** | ✅ | `MAX_WORKSPACE_FILE_PREVIEW_SOURCE_BYTES=1MiB` / `MAX_DESKTOP_SAFE_SUMMARY_BYTES=4096` / `MAX_WORKSPACE_HTML_PREVIEW_BYTES=256KiB` |
| **无越界** | ✅ | 无 bulk registration / drag-drop path / overwrite / Tool 注册；Renderer 无直接 HTML 渲染路径；Contracts / Document Worker / Central / `pnpm-lock.yaml` / 根 `package.json` / 根 `tsconfig.json` 未改 |

---

## 三、发现与分级

### P0 = 0，P1 = 0，P2 = 0，P3 = 0

无代码级、治理级或越界发现。无环境阻塞（本次所有全量门禁在沙箱内直接通过）。

---

## 四、总体结论

```text
结论：PASS（P0 = 0 / P1 = 0 / P2 = 0 / P3 = 0）
```

- MAR-1B 的 manual workspace-file bounded preview 安全边界达标：previewState 白名单（txt/md/html 可预览，PDF/XLSX/DOCX 保持 unsupported）、预览仅对无 taskId 手动 artifact 触发、文件守卫复用 MAR-1A、text/markdown 有界稳定读取（4KiB safe summary）、HTML 原文不返 Renderer 直接交 APV-1C 沙箱。
- 全程复用 APV-1B/1C 既有安全机制，无新 parser / 新 renderer；预览有界（1MiB 源 / 4KiB 摘要 / 256KiB HTML）。
- 无 bulk / drag-drop / overwrite / Tool 注册越界，Contracts/DW/Central/lockfile/根配置未改。
- 全部门禁独立复跑通过，与自测声明一致。
- **MAR-1B 可进入用户接受流程；MAR 系列（MAR-0 → MAR-1B）整体可收口。**

## 五、建议的下一步

1. 用户接受 MAR-1B QA 后正式关闭 MAR-1B，MAR 系列整体 `PASS/CLOSED`；Manual Artifact Registration 能力完整交付。
2. bulk registration、drag/drop path、overwrite 扩展、OS Sandbox、formal installer、APV-3C 保持 GATED。

— Claude Code（独立 QA，只读，未修改任何生产代码）
