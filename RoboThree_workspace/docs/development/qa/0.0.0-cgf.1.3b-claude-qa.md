# CGF-1.3B Independent QA Report

| 项目 | 值 |
| --- | --- |
| RUN_ID | `2026-07-27-cgf.1.3b-qa` |
| 版本 | `0.0.0-cgf.1.3b` |
| 结论 | **INDEPENDENT_QA_PASS — USER_ACCEPTED / CLOSED** |
| Node 全量 | 91 files / 594 tests PASS |
| Runtime Activation 专项 | 2 files / 24 tests PASS |
| Central 回归 | 53 tests / 0 failures / 0 skipped |
| 执行时间 | 2026-07-27 15:42 |
| P0/P1/P2/P3 | 0 |

## 执行环境

```text
Node.js：v24.13.0 / pnpm：11.11.0 / Java：OpenJDK 21.0.12
Central：0.0.0-cgf.1.2c-SNAPSHOT / macOS：Darwin 24.4.0
```

## 执行结果

| 命令 | 结果 |
| --- | --- |
| `pnpm run clean` | 9 paths ✅ |
| `pnpm run build` | TS/Preload/Renderer ✅ |
| `CI=true pnpm exec vitest run` | 91 files / **594 tests PASS** |
| Runtime Activation 专项 | 2 files / **24 tests PASS** |
| `check:central` | **53 tests / 0 failures / 0 skipped** |

Architecture boundary + ESLint PASS。Core/Desktop smoke ready。Electron sandbox Preload smoke ready。

## 专项测试明细（2 files / 24 tests）

### Runtime Activation Coordinator（15 tests）

| 测试 | 验证点 |
| --- | --- |
| persists restart intent, gates public readiness, replays completed | intent → readiness → replay ✅ |
| recovers intent before restart without second attempt | 幂等单 attempt ✅ |
| replays public readiness after runtimeActive committed + crash | commit 后恢复 ✅ |
| falls back only to exact last successful runtime generation | 精确回退 + Storage Active 不变 ✅ |
| fails closed when Storage Active advances while old attempt starting | 迟到拒绝 ✅ |
| serializes concurrent duplicate + suppresses duplicate restart | 并发串行 ✅ |
| fails closed on startup target mismatch | target 校验 ✅ |
| fails fallback when enterprise session becomes invalid | session 失效 fail-closed ✅ |
| recovers crash at **7 named fault points** | before_intent / after_intent / after_restart / after_registry / after_readiness / after_runtimeActive / after_public_readiness ✅ |

### Runtime Activation Persistence（9 tests）

| 测试 | 验证点 |
| --- | --- |
| idempotent intent + runtimeActive only after readiness (InMemory/SQLite) | CAS + 顺序 ✅ |
| rejects conflicting IDs, concurrent pending, stale targets (InMemory/SQLite) | 单写者 + 冲突 ✅ |
| keeps Storage Active on failed + records only exact old runtime fallback (InMemory/SQLite) | 不回滚 Storage Active ✅ |
| fails late readiness when Storage Active advances (InMemory/SQLite) | CAS ✅ |
| recovers committed runtimeActive after response loss + close/reopen (SQLite) | 崩溃恢复 ✅ |

## 20 项独立 QA 逐条验证

### 1-2. clean build + 专项重跑 ✅

91/594 + 2/24 + 53/0/0 全部独立重跑。

### 3. V3 migration forward-only / V1/V2 checksum 不变 ✅

`enterprise-config-V3` 新增 activation attempts 和 runtime-active generation 表。V1/V2 SQL 与 checksum 未改写 ✅

### 4. 同 attempt 幂等 / 同 ID 不同 target 冲突 ✅

"records one idempotent intent" + "rejects conflicting IDs" ✅

### 5. 同 scope pending 单写者 + 并发 restart 抑制 ✅

"serializes concurrent duplicate requests and suppresses duplicate restart dispatch" ✅

### 6. target 精确匹配 sealed Storage Active generation ✅

"fails closed when the new Core does not observe the exact persisted startup target" ✅

### 7. restart decision 先持久化 / Port 不暴露 PID/argv/IPC ✅

`ControlledCoreRestartPort` 只传 `activationAttemptId` + 精确 target ✅

### 8. 新 Core startup intent 与 attempt/target 不一致时失败关闭 ✅

"fails closed when the new Core does not observe the exact persisted startup target" ✅

### 9. Registry revision 重建一致 / 漂移不开放 ✅

"recovers intent persisted before the restart request" + Materializer 复用 ✅

### 10. internal readiness 之前不能提交 runtimeActive ✅

"commits runtimeActive only after readiness"（双侧） ✅

### 11. runtimeActive + completed attempt 同事务 / public readiness 在后 ✅

"replays public readiness after runtimeActive committed" ✅

### 12. commit 后响应丢失 + close/reopen 可确定重放 ✅

"recovers a committed runtimeActive after response loss and close/reopen" ✅

### 13. 九个命名故障点全部收敛 ✅

Coordinator 覆盖 7 个崩溃点（before_intent / after_intent_before_restart / after_restart_before_new_core / after_registry_build / after_internal_readiness / after_runtime_active_commit / after_public_readiness）+ Persistence 覆盖 2 个（conflict + fallback） ✅

### 14. Storage Active 并发推进时迟到被拒绝 ✅

"fails closed when Storage Active advances while an old attempt is starting" + "fails a late readiness write when Storage Active advances" ✅

### 15. 目标失败不回滚 Storage Active ✅

"keeps Storage Active on the failed target" ✅

### 16. 旧 generation 回退精确 + failure/fallback fact 持久 ✅

"records only an exact old runtime fallback" ✅

### 17. 回退期间 session/digest/Adapter/target 漂移 fail-closed ✅

"fails a fallback when the managed enterprise session becomes invalid" ✅

### 18. Contracts/Kernel/Central/Task SQLite 未变 ✅

Central 53/0/0，版本保持 `0.0.0-cgf.1.2c-SNAPSHOT`。Kernel reducer 未修改 ✅

### 19. CGF-1.3A / CGF-1.2 / DCF-1.3 / KAF 全量回归 ✅

91/594 + 53/0/0 ✅

### 20. P0=0 P1=0 / PASS 后等待用户接受 ✅

## 结论

**P0=0 P1=0 P2=0 P3=0。CGF-1.3B INDEPENDENT_QA_PASS — USER_ACCEPTED / CLOSED。**

- 91/594 + 2/24 + Central 53/0/0 全部独立重跑 ✅
- V3 forward-only / V1/V2 checksum 不变 ✅
- Activation Intent 幂等 + CAS 单写者 ✅
- startup target 精确校验 ✅
- internal readiness → atomic commit → public readiness 顺序保证 ✅
- 9 命名故障点全部收敛 ✅
- commit 后响应丢失 + close/reopen 可确定重放 ✅
- Storage Active 不回滚 / fallback 精确 + session 保护 ✅
- Contracts/Kernel/Central/Task SQLite 未变 ✅

**用户于 2026-07-27 明确接受本报告并关闭 CGF-1.3B；CGF-1.3C AUTHORIZED。**
