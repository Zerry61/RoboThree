# Security Model

> RoboThree 安全模型。
> 阶段二由 `security-reviewer` + `robothree-architect` 在综合 `research/*/permission-security.md` 后填写。

## 状态

- ⏳ 待启动

## 必含内容（占位）

- 信任边界
- 权限模型（deny-by-default / 显式 allowlist）
- Shell / 文件系统 / 网络访问策略
- Secret 管理（输入 / 输出 / 持久化）
- Prompt Injection 防御
- 远程执行与外部模型调用的隐私边界
- 审计与可观测性
