# License Notes — <Project>

> 在开始研究本项目之前，必须完成本文件。
> 必须由 source-mapper 在第一次拉取源码时填写并归档。
> 任何代码片段引用前，对应 license 必须在 `analysis.json` 的 `project.license` 字段中明确标识。

## 仓库信息

- 项目名:
- 仓库 URL:
- 主分支 / 默认分支:
- 最近一次拉取的 commit:

## 许可证

- LICENSE 文件路径:
- SPDX 标识符:
- 是否商业使用友好:
- 是否要求署名:
- 是否要求公开衍生作品源码:
- 是否禁止再许可:
- 是否有专利授权:

## 对 RoboThree 的影响

| 用途 | 是否允许 | 限制 | 替代方案 |
| --- | --- | --- | --- |
| 阅读学习 | ✅ / ❌ | ... | ... |
| 接口借鉴 | ✅ / ❌ | ... | ... |
| 算法借鉴 | ✅ / ❌ | ... | ... |
| 整段代码搬运 | ✅ / ❌ | ... | ... |
| 反编译 / 反汇编 | ✅ / ❌ | ... | ... |

## 第三方依赖的传染性

- 是否使用 GPL 类强传染依赖:
- 是否使用 AGPL 类网络服务传染依赖:
- 是否使用 SSPL / BUSL 类限制性依赖:
- 备注:

## 引用纪律

- 引用代码片段 > 20 行时，必须在报告显著位置注明 "引用自 <project>@<sha>:<path>:<line>" 并附带仓库 URL。
- 禁止未审阅 LICENSE 就复用上游代码。
- 任何 `robothree/` 下设计文档借用上游思路时，必须在本文件登记并在 ADR 中追溯。
